diff --git a/node_modules/react-native-reanimated/src/specs/NativeReanimatedModule.ts b/node_modules/react-native-reanimated/src/specs/NativeReanimatedModule.ts
index 0000000..0000001 100644
--- a/node_modules/react-native-reanimated/src/specs/NativeReanimatedModule.ts
+++ b/node_modules/react-native-reanimated/src/specs/NativeReanimatedModule.ts
@@ -8,2 +8,16 @@
 
-export default TurboModuleRegistry.get<Spec>('ReanimatedModule');
+// Patched: TurboModuleRegistry.get('ReanimatedModule') at module-eval time on Hermes
+// BG thread can cause instability on iOS 26 New Architecture (Bridgeless). Defer.
+let _m: Spec | null | undefined;
+function _get(): Spec | null {
+  if (_m === undefined) _m = TurboModuleRegistry.get<Spec>('ReanimatedModule');
+  return _m;
+}
+export default new Proxy({} as Spec, {
+  get(_t, prop, receiver) {
+    const m = _get();
+    if (!m) return undefined;
+    const v = Reflect.get(m, prop, receiver);
+    return typeof v === 'function' ? v.bind(m) : v;
+  },
+}) as unknown as Spec | null;
