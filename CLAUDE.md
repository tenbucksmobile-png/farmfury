# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## Project Overview

**Farm Fury** is an Angry Birds-style physics destruction game. Farm animals launch from a cannon to wreck robot fortresses and reclaim the farm.

The repo contains two parallel codebases:
- `index.html` — **Phaser 3.60 prototype** (frozen at Prompt 12, kept as a reference/physics-validation artifact only — not under active development)
- `unity/` — **Unity 6.5 URP 2D port** (active development target for iOS/Android release)

This is the single source of truth for the Unity port. Detailed reasoning behind current values/conventions (root causes, dead ends, superseded fixes) lives in **`docs/HISTORY.md`** — read it when you need the "why," not just the "what." This file only states current truth.

Full GDD: `C:\Users\Personel\Desktop\FarmFury_GDD_v2.docx`

---

## Git Repository Scope

`Desktop/FarmFury/.git` is now a standalone repo (single remote: `origin` →
`github.com/tenbucksmobile-png/farmfury.git`) — safe to use ordinary `git add -A`/`git commit -a`
from inside it. This was NOT always true: until 2026-07-09 this directory was part of a single
git repo covering the entire home directory (`C:\Users\Personel\.git`), shared with CryptoAlgoBot,
Kaya, and an old IndabaCares copy, pushed to both `farmfury.git` and (accidentally)
`crypto-algo.git`. That history was extracted via `git subtree split --prefix=Desktop/FarmFury`
and force-pushed as this repo's current `master`, so FarmFury's commit history before that date is
preserved intact, but the home-dir mega-repo no longer exists for this project. If you ever find
`.git` missing from this directory or discover FarmFury files tracked from a parent directory's
repo again, something has regressed — this should be a normal, self-contained repo going forward.

---

## Current Status & Known Issues

- **All 18 World 1 levels (L01-L18) redesigned from fresh `LevelLayoutDumper` dumps against the rebuilt parallax backdrop — 2026-07-19.** Continuation of the 2026-07-18 session's plan (see that entry below): the parallax stack's composition/scale differed enough from what the original layouts were built against that every level needed a fresh dump, not just a coordinate nudge. L01-L09 were redesigned earlier in the session; L10-L18 (including L18's Commander boss staircase) followed the same workflow — dump pasted directly into `LevelDataGenerator.cs`, each level's comment updated to reflect the new robot/block composition. One data quality flag carried through rather than silently fixed: L18's dumped Commander robot had `scale (1, 1)` — every other robot in this pass (and the previous L18 Commander) used scale 4.5-12 — almost certainly the Commander wasn't scaled up in the Scene view before the dump was taken; pasted as dumped with a comment flagging it, not guessed at. **Not visually verified in Play mode** (no Play-mode access here) — worth a live pass, especially L18's Commander scale.
- **Two real gameplay bugs found and fixed — 2026-07-19, user report: "1) sometimes structure is still floating in the air when struck 2) from level 8 the animal no longer fires (as if the game is stuck)."** Root-caused via a background investigation (not guessed), both fixes are shared-class changes so they apply to every level automatically:
  - **Floating structure**: `BlockBase.CheckForBlocksOnTop()`/`CheckForRobotsOnTop()` (the "wake whatever's resting on a destroyed block" cascade) shrank their overlap-detection box to 90% of the support block's own width, on both methods — anything resting near the very edge of its support (an overhanging plank, a `Skew`/`Diagonal`-art block whose physics collider is a plain axis-aligned box under its angled sprite, anything placed slightly off-centre from a dump) could fall outside that band and never get woken when its support was destroyed, leaving it permanently `Static`/floating. The box's bottom edge already sits exactly at the support block's top surface, so a same-level sideways neighbour was never at risk of being falsely woken by widening X — the 90% shrink was never actually protecting against that. Widened both to the full block width.
  - **Cannon stuck from L08 onward**: `CatapultLauncher` only ever clears its "busy" flag (`_activeAnimal = null`) via `AnimalBase.OnAnimalDestroyed`, which previously only fired from a genuine `OnCollisionEnter2D`. If a shot's arc cleared every collider in a level (structure geometry, gaps between columns, or Cluck's own pass-through haybale punches, which deliberately skip `base.OnCollisionEnter2D`) and also missed the ground plane, the animal fell forever, never collided, never destroyed itself, and every subsequent tap silently no-op'd. This was always a latent, level-agnostic bug, not something specific to L08's code — L08's redesigned taller/denser 3-column layout just raised the odds of triggering it. Added a flight-time cap (10s) and a far-below-ground fallback (`transform.position.y <= -30`) to `AnimalBase.Update()` that force-destroys the animal via the same `DestroyAnimal()` path a real impact uses, so `CatapultLauncher.OnAnimalLanded()`'s existing "rare edge case" camera-recovery logic (already had a comment anticipating exactly this) picks it up for free.
  - Both compile-checked clean. **Not visually verified in Play mode** (no Play-mode access here).
- **World 2 "Frozen Tundra" front-end scaffolding — 2026-07-19, built incrementally across the session as the user supplied new art.** No real World 2 levels exist yet — this is the shell the first real levels will need once built, same "front-end before content" sequencing this project already used for World 1's map/menu before L01 existed.
  - **`LevelData.world` field added** (`int`, defaults to `1`) — zero migration needed, every existing L01-L18 asset regenerates with `world: 1` automatically. `LevelDataGenerator.Make()` gained a matching optional `world` param.
  - **`EnvironmentDepthSystem` gained a World-2 branch**: `HandleLevelStarted()` reads `data.world` and swaps FarHills/Midground to World-2 art (`ParallaxFarHills_W2.png`/`ParallaxMidGrounds_W2.png` — note the real on-disk filename is "MidGrounds" with a capital G and trailing s, not "Midground") with their own clip values, and deactivates the Foreground layer entirely (World 2 has no opaque Foreground painting by design — ground-seam dressing is a `SceneryBuilder` prop scatter instead, see below). Midground's sky-line clip (`0.21`) was measured directly against the source art via pixel classification, not guessed — the naive approach (raw luminance jump, then simple hue-window classification) kept false-positiving on the aurora's own colour bands and the terrain's dark-navy shadow fill (both share similar hue to open sky), so the final classifier gates on saturation+value together, cross-checked by re-rendering the detected boundary back onto the source image and confirming it against real pixel samples before trusting it. Also added two Editor-only menu items, **FarmFury → Debug → Preview World 1/2 Backdrop (Editor)**, so the swap can be seen live in the Scene view without Play mode or any real World-2 `LevelData` — reuses `SceneSetup`'s existing `CoverFitLayer`/`ApplyClipEditor` Editor-time-preview helpers directly rather than the runtime component's own fields, since `Awake()` doesn't run in Edit mode for an already-existing component.
  - **`World2LandingController` + `World2MapController`** (new files, `Scripts/UI/`) mirror `MainMenuController`/`WorldMapController`'s own patterns. Reachable via a new post-L18 flow: `HUDController.GoToWorldLandingAfterTransition()` (renamed from `GoToMenuSkippingWorldMap`) now lands on `World2LandingController` (`FarmFury_W2.png`, same PLAY/SETTINGS icon art/position as the main menu) instead of the main menu once the L18 transition video finishes. `World2LandingController`'s SETTINGS button opens the *same* settings popup as the main menu's gear icon — required refactoring `MainMenuController`'s settings popup onto its own independent Canvas (it previously lived as a child of `MainMenuController`'s own landing-page Canvas, so it could only ever be shown while that landing page itself was active); `MainMenuController.OpenSettings()` is now public and is the one shared entry point both screens call, with zero behaviour change to the main menu's own gear icon. `World2MapController`'s 22 level markers (per the GDD's World 2 level count) are laid out along a path hand-traced against the actual `FrozenTundra.png` art (pixel-classified the tan path colour, isolated its connected component, extracted waypoints, resampled evenly by arc length) — **re-traced once already**, after the user supplied an updated `FrozenTundra.png` with the path extended further to the fortress entrance; the first trace assumed the path was Y-monotonic (true for the original art) and broke silently on the revised art's back-on-itself bend, caught by re-rendering the result and visually checking it before trusting it, not shipped blind. Both screens are deliberately NOT reactive to `GameState.Idle` the way `MainMenuController`/`WorldMapController` are — they're only ever shown via the explicit L18-transition callback or the new Play-mode debug menu items, so they can never race the World 1 screens to show themselves on an unrelated Idle transition. **Known gap, flagged not solved**: there's currently no way back INTO World 2's screens once you leave them (Home always returns to `MainMenuController`, matching every other Home button's existing convention) — a real "which world is the player currently in" concept doesn't exist anywhere yet; that's a bigger decision than this session covered.
  - **Match-up screen re-skinned for World 2**: background → `Matchup_W2.png`; robot cards → `FrostRobot_card.png`/`IceHarvestor_card.png`/`GlacierRobot_card.png`, mapped onto the *existing* `RobotType.Basic`/`Harvester`/`SemiHarvester` slots (a best-guess mapping — flagged, not confirmed with the user — based on `IceHarvestor`/`GlacierHarvestor`'s naming matching World 1's own "Harvestor = Harvester tier, no distinguishing prefix" convention); animal cards for Percy/Woolly picked up automatically once `Percy_Pig.png`/`Woolly_Sheep.png` landed in the same `MatchUp` folder, since the existing keyword-based wiring already expected those exact filenames. No World 2 Commander/boss card exists yet — that slot is explicitly cleared (not left stale) rather than reusing World 1's.
  - **Real gameplay robot prefabs**: `FrostRobot.prefab`/`IceHarvestorRobot.prefab`/`GlacierHarvestorRobot.prefab` (HP 35/28/26, matching the Basic/Harvester/SemiHarvester tiers they reskin) — deliberately NOT new `RobotType` enum values (avoids any risk to L01-L18's already-serialized robot data); `LevelLoader.SpawnRobot()` now takes the spawning level's `world` and picks the World-2 prefab for a given tier when one exists and `world == 2`, falling back to the World-1 prefab otherwise (Commander has no reskin yet, so World-2 boss levels would still use the World-1 Commander art). Both left/right walking-patrol art wired per robot (matching real on-disk filename casing exactly — `Iceharvestor_right.png` lowercase h, `GlacierHarvestor_Right.png` capital R — since `AssetDatabase` paths are effectively case-sensitive on iOS/Android builds even though Windows tolerates the mismatch).
  - **`SceneryBuilder`** gained a World-2 branch (`BuildIceGroundProps()`) — same seeded-`System.Random`-per-level-index pattern as World 1's own (currently disabled) RNG scatter, plus new rotation randomization World 1's `Place()` never needed. No real World 2 `LevelData` exists yet to know the actual ground-seam extent, so the X range is a placeholder matching World 1's typical level-content span.
  - **`Scenery_World1`**: grouped World 1's 18 hand-placed decorative props (Windmill/OldBarn_Right/OakTree/3×WoodenFence/2×Rock/5×GrassTuft/2×WildFlowers/3×WoodenBarrel — the exact list read directly from the live scene, not from this file's older, partially-stale history below) under one new parent GameObject at scene root, so they can be hidden as a single unit (Scene Visibility, non-destructive) while placing new World 2 scenery against the backdrop preview. Done via a real Unity `SetParent(..., worldPositionStays: true)` call (a one-off batch-mode Editor method), not hand-edited scene YAML — every prop's world position is unchanged, verified directly against the saved scene.
  - Compile-checked clean throughout; `Wire Scene References` re-run after every wiring change, each one verified directly against the saved `Game.unity`, not just assumed. **Not visually verified in Play mode** (no Play-mode access here) — worth a full live pass once real World 2 level content exists.
- **User confirmed happy with the parallax backdrop rendering — 2026-07-18, end of session.** All 4 of today's rounds on this system (backdrop-coverage fix, full 3-layer FarHills/Midground/Foreground rebuild, Midground clip-line seam fix, Play-mode black-gap fix) are now considered settled from the user's own visual check in the Editor — this is the first of today's several "looks right?" iterations to get an explicit sign-off rather than another round of "still wrong." **Next session: the user will redesign every one of the 18 World 1 levels from scratch** to accommodate the new scenery (composition/scale of the parallax stack differs enough from what L01-L18 were originally laid out against that a fresh `LevelLayoutDumper` pass per level is expected) — treat any existing per-level `LevelData` positions as provisional/likely-stale going into that session, not as a baseline to preserve.
- **`EnvironmentDepthSystem` black-gap-in-Play-mode bug found and fixed via direct code-order proof, not a guess — 2026-07-18, same-day follow-up.** User report + screenshots: Scene view showed the parallax stack covering the full frame correctly, but Play mode (with real HUD visible) showed black gaps at the top/right edges — "stretch out the canvas... don't guess and overdo it as before." Root cause confirmed by reading the actual execution-order attributes and event-subscription code (not speculation): `EnvironmentDepthSystem` has `[DefaultExecutionOrder(-80)]`, `CatapultLauncher` has none (defaults to `0`) — since `-80 < 0`, `EnvironmentDepthSystem.OnEnable()` runs first and subscribes to `GameManager.OnLevelStarted` BEFORE `CatapultLauncher` does. C# multicast delegates fire subscribers in subscription order, so `EnvironmentDepthSystem`'s handler read `_cam.orthographicSize` and cover-fit against it BEFORE `CatapultLauncher.OnLevelStarted()` had actually set that level's real zoom via `ComputeOrthoSizeForLevel()` — every level was sized against the stale previous/rest orthoSize, undersizing the stack whenever a level's real zoom was larger. This is the identical bug shape `BackgroundController` already hit and fixed once before (see that class's own `_lastOrthoSize` comment, unrelated to any of today's parallax work) — applied the same proven fix rather than inventing a new one: added a `_lastOrthoSize` field + `LateUpdate()` that calls `RescaleToCamera()` whenever orthoSize has actually changed since last checked (cheap float compare, not a full recompute every frame). Also corrected this class's own `RescaleToCamera()` comment, which had previously (incorrectly) claimed "relative firing order between them doesn't matter here" — it does, and now doesn't need to, since `LateUpdate()` self-corrects regardless of subscription order. Verified via `.\Run-Unity.ps1 check` (clean) → `setup` (clean, no regressions — this is a runtime-only fix, no scene wiring changed). **Not visually verified in Play mode** (no Play-mode access here) — this should resolve the black-gap symptom on the very next Play; worth a live check across a couple of levels to confirm no gap appears even momentarily on level load.
- **`_midgroundClipAbove` lowered 0.55 → 0.23, real windmill/hill-ridge conflict found and reported (not silently resolved) — 2026-07-18, same-day follow-up.** User report + screenshot: Midground's 0.55 reveal band included part of its own independently-painted sky, visibly seaming against `Layer_FarHills`' differently-painted sky behind it. Measured directly against the source art (pixel sampling, cross-checked against direct visual crops with row-number gridlines — not guessed): because `ClipAbove` is a single UNIFORM horizontal line, the safe value must clear the LOWEST dip in the hill ridge across the full width, not the highest peak. Found the ridge's highest point at x=0 (row 958/1536, v≈0.38) — an earlier, wrong-direction pass briefly targeted this before realizing a uniform line needs the opposite extreme — and its deepest dip at x≈1440/2720 (row 1168, v≈0.24, confirmed via the bright-yellow-rim color signature that marks the sky/hill boundary in this art style, since raw "is it green" color heuristics gave false readings — the atmospheric haze band above the ridge is muted/brownish, not green, and the windmill's grey tower fooled a naive scan into reporting spurious deep rows). Landed on 0.23 (row ≈1180, small margin past the measured 1168 dip).
  - **Real conflict found**: the windmill's own blade-tip height is row 1042 (v≈0.32) — HIGHER (smaller row) than the 0.23 cutoff needed to guarantee zero sky leakage. No single uniform `ClipAbove` value satisfies both "windmill fully visible" and "never any sky," since the windmill pokes up above the ambient terrain level elsewhere in the same flattened painting. Reported this to the user with exact numbers rather than picking a silent compromise (per their own "report back rather than papering over it" instruction, extended to this related finding) — user's response: offered to pull the windmill out as its own standalone sprite (a separate follow-up, needs new art + a new unclipped prop layer) rather than solve it via the clip value. Proceeded with 0.23 (prioritizing the core "zero sky" bug fix) on that basis — the windmill's blades/roof are now clipped in the interim, expected to be resolved once the standalone sprite exists.
  - **Seam-color check at the new line (row 1180)**: sampled both paintings' actual pixel colors there — both muted/warm-toned (Midground's yellow-rim hues, FarHills' blue-to-tan haze), not the kind of saturated clash the original bug had, except at the very left edge (x=0) where Midground shows dark ground detail against FarHills' lighter blue haze. A faint "double ridge line" (two independently-painted silhouette contours meeting) is expected and inherent to theatre-flats layering, not a defect.
  - Applied in 3 places: `EnvironmentDepthSystem.cs`'s field default, `SceneSetup.cs`'s first-creation stamp default, and the already-serialized value in `Game.unity` (hand-edited directly, since the `isNewComponent` guard means `setup` won't touch an already-existing component's tuned values). Verified via `.\Run-Unity.ps1 check` (clean) → `setup` (clean) → direct scene inspection (`_midgroundClipAbove: 0.23` confirmed on disk). **Not visually verified in Play mode** (no Play-mode access here) — worth a live check per the same L01/L18 guidance as the entry below, specifically looking for: no sky leak anywhere, the expected clipped windmill (blades/roof missing, tower base likely still visible), and whether the left-edge (x=0) seam is noticeable in practice.
- **`EnvironmentDepthSystem` rebuilt a THIRD time the same day as a real 3-layer masked parallax stack (FarHills/Midground/Foreground), this time reactive to per-level camera zoom — 2026-07-18, explicit user spec.** Context check before implementing: the user's task description assumed EnvironmentDepthSystem.cs still existed and auto-rescaled per level — it didn't; both had already been removed earlier the same session (see the two entries below) after repeated single-painting attempts failed to cover the camera. Confirmed with the user first (three-way architecture question — full replace vs. layer-on-top vs. other) before writing any code, since blindly following the stale premise would have put a second scenic system in front of `Background`'s already-working `ParallaxMidground.png` full-cover. User chose full replace.
  - **Rebuilt `Scripts/Core/EnvironmentDepthSystem.cs`** (re-created after being deleted) as 3 layers — `Layer_FarHills`/`Layer_Midground`/`Layer_Foreground`, sortingOrder -40/-30/-20 respectively, each the shared `FarmFury/ParallaxBandClip` shader/material (also still on disk, untouched) — each reveals only its own bottom `[0, ClipAbove]` UV band via a `MaterialPropertyBlock`, so a nearer layer's transparent upper region lets the layer behind it show through ("theatre flats"). `_farHillsClipAbove`/`_midgroundClipAbove`/`_foregroundClipAbove` are `[Range(0,1)]` Inspector sliders (defaults 1.0/0.55/0.30 — FarHills fully uncropped, its own painted sky fills the top) so the split point can be tuned live without re-exporting art; an `OnValidate()` re-applies them immediately when tweaked in Play mode.
  - **The critical difference from every earlier attempt**: `RescaleToCamera()` re-fits all 3 layers to the camera's ACTUAL current `orthoSize`/`aspect`/position, called once at `Awake()` (for the rest/menu framing) AND on every `GameManager.OnLevelStarted` (same event `CatapultLauncher` uses to set `orthoSize` via `ComputeOrthoSizeForLevel()` — order between the two subscribers doesn't matter since both read the camera fresh, not a cached value). No hand-placed literal, no frozen transform — this directly targets the root cause that broke every prior version (a static transform sized once against one assumed zoom can't cover `orthoSize` 4.5–8.0). `SceneSetup.EnsureEnvironmentDepthSystem()` mirrors the same cover-fit math for the Editor-time preview (`CoverFitLayer()`), and — unlike the `FarmCannon`/old-`Layer_Midground` "only stamp on creation" pattern — runs this UNCONDITIONALLY every `Wire Scene References` pass, since being camera-computed (not hand-placed) is the entire point this time. Only the 3 `ClipAbove` slider defaults are still first-creation-only, so hand-tuning survives future `setup` runs.
  - **Real gap found while wiring**: `Background_SkyV1.png` (the file `EnsureBackground()`/`EnsureCelebrationVideoBackground()` both referenced) has been deleted from disk at some point during this session — confirmed via a direct folder listing (`Assets/Sprites/Environment/Skies/` now only contains the 3 new Parallax paintings), not assumed. Both call sites repointed at `ParallaxFarHills.png` instead (same file as the new top parallax layer — thematically consistent, guaranteed to exist, and avoids any seam if the razor-thin soft-edge fade the `ParallaxBandClip` shader applies even at `ClipAbove=1.0` — see that shader's own comment — ever becomes visible at an unusual aspect ratio). `Background`/`BackgroundController` itself is otherwise unchanged — still a defensive fallback sky sitting at sortingOrder −100, still auto-covered every frame by its own proven `ScaleToFillCamera()`, now normally fully hidden behind the new stack.
  - Zero changes to camera-size logic (`ComputeOrthoSizeForLevel`), cannon position, or launch-speed constants, per the spec — additive only. Zero per-level `LevelData` changes — this is the one persistent scene object, so it applies to all 18 levels automatically.
  - Verified via `.\Run-Unity.ps1 check` (clean) → `setup` (clean; log confirms all 3 sprites wired, `Background: ParallaxFarHills.png wired (defensive fallback)`, no more missing-file warnings) → direct `Game.unity` inspection (confirmed all 3 `Layer_*` GameObjects exist at the correct sorting orders, all reference the shared `ParallaxBandClip.mat` by GUID, `Layer_FarHills` computed to position `(0, -2)` / scale `0.5859375` — the exact cover-scale for `orthoSize=4.5`, confirming the Editor-time preview math is correct at the rest zoom). **Not visually verified in Play mode** (no Play-mode access here) — see the two levels/criteria flagged below for the user's own in-Editor check.
  - **Where to check first, and what "looks right" means**: **L01** (small level, `orthoSize` stays at the `4.5` floor) and **L18** (Commander boss fight, the densest layout in the game — almost certainly clamps to the `8.0` ceiling). At both: (1) no gap/seam of the old flat sunset sky anywhere at the frame edges — the 3-layer stack (or its `ParallaxFarHills.png` fallback) should fill edge-to-edge; (2) a visible sense of depth — foreground (wildflowers/rocks/grass) closest and occupying roughly the bottom third, midground (windmill/stone wall/trees) a band above that, far hills filling the rest; (3) at L18 specifically, check whether the upscaled paintings (native 2720×1536 stretched past 1:1 at `orthoSize=8.0`) look visibly soft/blurry — if so, flag it back rather than treating it as expected, per the spec's own explicit call-out.
- **CommanderRobot given a continuous 3-pose taunting loop, swapping to a 4th pose on death — 2026-07-18, same-day follow-up to the robot patrol animation below, reusing the exact 4 images already wired for the old health-threshold pose system (Commander.png/Commander_Alert.png/Commander_Hit.png/Commander_Explode.png — see `EnsureCommanderRobotPrefab`).** User request: "animate the robot between these three images as a loop - so it looks like the commander is taunting the animals... once hit and exploding change to [Commander_Explode]." No new sprite wiring needed — `RobotEnemy` gained `_isTauntLoop` (auto-detected as `_alertSprite != null`, which today is unique to Commander) and `TauntLoopRoutine()`, which cycles `_robotSprite -> _alertSprite -> _criticalSprite -> repeat` on a fixed 0.6s-per-frame timer for as long as the robot is alive, **regardless of health** (unlike the patrol/look-around routines, this does NOT stop on damage — it taunts through the whole fight). This fully replaces the old discrete "swap once and stay" health-threshold pose progression for taunt-loop robots — `TakeDamage()`'s alert-threshold block was dead code the instant `_isTauntLoop` was introduced (the only robot with `_alertSprite` wired now always takes the taunt-loop branch instead) and was deleted outright, along with the now-unused `_isAlert`/`AlertHealthFraction`. The critical-threshold block and `FlashDamage()`'s per-hit sprite flash are both still skipped for taunt-loop robots specifically (both would otherwise fight `TauntLoopRoutine()` for `_sr.sprite`) but remain fully intact and unchanged for every other robot type (Harvester/SemiHarvester still use `_criticalSprite`/`_robotDamagedSprite` the old way). `Die()` now sets `_sr.sprite = _deathExplosionSprite` directly on the robot's own body for taunt-loop robots, before the squish/destroy sequence — `TauntLoopRoutine()`'s `while (!IsDestroyed)` check only re-evaluates after its current frame's `WaitForSeconds` elapses, so this doesn't get raced/overwritten (verified by reading the coroutine's control flow, not assumed). This is in addition to, not instead of, the existing separate `SpawnDeathExplosion()` burst overlay every robot type already gets. Verified via `.\Run-Unity.ps1 check` (clean) → `setup` (clean, no regressions — Commander's existing sprite wiring untouched since no new fields were needed). **Not visually verified in Play mode** (no Play-mode access here) — worth a live check on L18 that the taunt loop reads as intended (not too fast/slow at 0.6s/frame) and that the death-sprite swap lands cleanly alongside the squish/burst.
- **Robots given a left/right walking patrol animation — 2026-07-18, user-supplied `Robot_Pawn_right.png`/`Robot_SemiHarvestor_right.png`/`Robot_Harvestor_right.png` (mirrored right-facing art for Basic/SemiHarvester/Harvester; no Commander variant supplied), user request: "apply to every robot sprite in all levels as animation movement... appears as if the robots are alive and moving from one direction to another. left and right."** `RobotEnemy` gained a new `_sprFacingRight` field and `IdlePatrolRoutine()`/`WalkTo()` — while a robot is undamaged and Static (the same `IsIdle()` gate the pre-existing `IdleLookAroundRoutine` already used), it now shuffles ±0.3 world units either side of its spawn X at 0.2u/s, pausing between walks, and swaps `_sr.sprite` between `_robotSprite` (left-facing, the existing base art) and `_sprFacingRight` (right-facing) to match its current direction of travel. `Awake()` now branches: a robot with `_sprFacingRight` wired gets the new walking patrol; one without it (currently only `CommanderRobot`, no right-facing art exists for it) keeps the older in-place turn-and-glance animation unchanged. Confirmed via user follow-up ("was the robot is damaged ofcourse it cannot move - then the damaged sprite appears") that stopping the patrol the instant any damage is taken (existing `IsIdle()` gate, unchanged) is the correct, already-intended behavior — the persistent alert/critical/damaged-flash sprite logic in `TakeDamage()` already overrides `_sr.sprite` before the next patrol-loop check would run, so the two systems don't fight.
  - **Deliberately kept the Rigidbody2D as `Static`, not `Kinematic`**, even though it now moves every frame — `BlockBase.OnCollisionEnter2D` (and this class's own effective-mass formula) special-case a collision partner's `bodyType == RigidbodyType2D.Static`; switching to Kinematic would silently change how blocks compute damage/collision response against a patrolling robot. Directly driving `transform.localPosition` on a Static-bodied Rigidbody2D for cosmetic idle animation is the same pattern this class's own `RotateTo()`/`IdleLookAroundRoutine` (rotation) already used successfully — this just extends it to translation.
  - **Patrol range (0.3 world units) deliberately conservative** — robots guard specific positions in already-tuned level layouts (checked several hand-built dumps; robot-to-robot spacing is typically ≥1 unit), so a wide patrol risked walking off a support ledge or visually overlapping a neighbour in a dense level (e.g. L13's 10 robots). Not derived from a per-level check of every robot's actual clearance — if a future level places robots unusually close together, this may need a per-instance override.
  - `SceneSetup.cs` gained a shared `WireRobotFacingRight()` helper (same PPU=1746 import convention as every other robot sprite), called from `WireRobotSprite()`/`EnsureHarvesterRobotPrefab()`/`EnsureSemiHarvesterRobotPrefab()` — NOT from `EnsureCommanderRobotPrefab()`, since no right-facing Commander art exists. Verified via `.\Run-Unity.ps1 check` (clean) → `setup` (clean; log confirms `Wired _sprFacingRight <- ...` for all three, and direct prefab inspection confirms `CommanderRobot.prefab`'s `_sprFacingRight` stayed `{fileID: 0}` while the other three prefabs got real sprite GUIDs). **Not visually verified in Play mode** (no Play-mode access here) — worth a live check that the patrol reads as "alive," doesn't visually clip into neighbouring blocks/robots on a dense level, and that the sprite swap timing (starts moving and swaps sprite in the same frame) looks right rather than lagging.
- **`EnvironmentDepthSystem`/`Layer_Midground` removed entirely; backdrop consolidated onto `Background`'s own proven auto-cover — 2026-07-18, fourth round the same day, user report: "stop guessing and fix the scene level - the sky background is still showing (this should have been removed) - also the new backdrop is not stretching across the screen."** Three same-day attempts (see the two entries below) all tried to hand-place or compute a transform for a SECOND static layer (`Layer_Midground`, rendered in front of `Background`) to fully cover the camera — every attempt failed at some aspect/zoom because a single static transform, however carefully computed, can't cover every per-level auto-zoom (`orthoSize` 4.5–8.0) and every real device aspect ratio at once. Rather than attempt a fourth manual number, removed the whole parallel system: `Scripts/Core/EnvironmentDepthSystem.cs` deleted outright, its self-bootstrap in `CatapultLauncher.Awake()` removed, and `SceneSetup.EnsureBackground()` now points the existing `Background` GameObject's own sprite directly at `ParallaxMidground.png` (previously `Background_SkyV1.png`) instead of running a separate layer in front of it. `Background`'s `BackgroundController` already had a proven, live per-frame cover-scale (`ScaleToFillCamera()`/`LateUpdate()`, unchanged, in production since long before any of this session's parallax work) — pointing it at the new art gets guaranteed no-gap coverage at any zoom/aspect for free, with no hand-tuned literal to ever go stale again. `SceneSetup.cs` gained `CleanupLegacyEnvironmentDepthSystem()` (one-time removal of any leftover `EnvironmentDepthSystem`/`Layer_Midground` GameObject from an older scene save) in place of the old `EnsureEnvironmentDepthSystem()`/`WireParallaxLayer()`/`EnsureParallaxBandClipMaterial()`/`WireParallaxPainting()` methods, all deleted. `Background_SkyV1.png` is kept on disk and still referenced once, deliberately, by `EnsureCelebrationVideoBackground()` (the Level Complete/Failed video overlay wants a plain clean sky behind a close-up character, not the busier full composition). Verified via `.\Run-Unity.ps1 check` (clean) → `setup` (clean; log confirms `Background: ParallaxMidground.png wired` and `Removed leftover 'EnvironmentDepthSystem' GameObject`; direct scene inspection confirms zero remaining `EnvironmentDepthSystem`/`Layer_Midground` references and `Background`'s `_skySprite` now points at `ParallaxMidground.png`'s GUID). **Not visually verified in Play mode** (no Play-mode access here) — this should be the last round needed on this particular bug; worth a live check across several levels of differing size to confirm.
- **`Layer_Midground` composition finalized by the user — 2026-07-18, third round the same day.** (Superseded by the entry above — `Layer_Midground` no longer exists at all, so this hand-placed composition is no longer in effect. Kept for history.) After two automated cover-scale attempts (rest-only, then worst-case-zoom — see the entry directly below), user report: "you have once again changed the entire scene positioning. As I have saved the scene now is how it must be applied to all levels." The user then hand-placed and saved the definitive composition directly in the Editor: `Layer_Midground` position `(0.1, -0.38)`, scale `(0.7831451, 0.80434096)`. **This is now the authoritative value — do not recompute it from a cover-scale formula again.** `SceneSetup.cs`'s `WireParallaxLayer()` fallback (only used if the GameObject is ever deleted and needs recreating) was changed from a computed formula to a literal snapshot of these two values, matching the same pattern already used for `FarmCannon`'s fallback stamp. Note this composition is smaller than a full per-level-zoom cover would require (it comfortably covers the camera's rest framing at `orthoSize=4.5` with only a thin margin at the bottom edge, but does not fully cover the camera's maximum per-level zoom-out at `orthoSize=8.0` — see `CatapultLauncher.ComputeOrthoSizeForLevel()`) — flagged, not fixed, since the user has explicitly chosen this exact composition; if the sky shows through at the edges on a very dense/zoomed-out level (L13/L18 territory) going forward, that's this known trade-off, not a new bug — raise it with the user before touching this transform again rather than "fixing" it unilaterally.
- **`Layer_Midground` backdrop coverage bug fixed properly (previous fix was incomplete) + stray decorative barrel removed — 2026-07-18, same-day follow-up.** User report: "you have not applied the bug fix to all scenes - the skybackground is still showing and mid parallax is not stretched across the entire area." Root cause found by reading `CatapultLauncher.ComputeOrthoSizeForLevel()` directly (not guessed): the earlier same-day fix sized `Layer_Midground`'s cover-scale against `cam.orthographicSize` at Editor time — but `PositionCamera()` always runs immediately before and resets the camera to its REST size (`_minOrthoSize = 4.5`), so that calculation was always sized for the smallest possible framing. Denser levels (L09-L18) auto-zoom the camera out up to `_maxOrthoSize = 8.0` (`Mathf.Clamp(..., 4.5, 8.0)`) — at that zoom the visible area is ~1.78x bigger per axis than a 4.5-sized backdrop covers, so `Background_SkyV1` showed through at the edges on any level that zoomed out past rest, exactly as reported. Fixed in two places: `SceneSetup.EnsureEnvironmentDepthSystem()` now sizes the cover-scale calc against a new `WorstCaseOrthoSize = 8.0f` constant (hardcoded to match `CatapultLauncher._maxOrthoSize` — no shared constant between the two files, so keep them in sync if that default ever changes) instead of the live camera value; the already-existing `Layer_Midground` in `Game.unity` was hand-updated to match (position `(0, -2)`, uniform scale `1.0457517` — up from the still-undersized `0.5882353` rest-only value from the first pass). This guarantees full coverage at every level's actual auto-zoom range, not just the world map/menu rest framing.
  - **Also removed in this pass**: a 4th, previously-undocumented `WoodenBarrel (3)` GameObject at `(7.25, -4.54)`, sitting immediately next to the re-added `GnarledTree (8.22, -3.94)` — user report: "there is now a random barrel that appears on all levels by the gnarled tree." Since L1 scenery is a permanent part of the single shared `Game.unity` scene (not per-level `LevelData`), any stray hand-placed prop like this renders on every level regardless of which one is loaded — confirmed via direct `Game.unity` inspection (not guessed) that `SceneryBuilder`'s own RNG scatter is unconditionally disabled (`return;` before it ever runs), ruling that out as the source. Deleted the GameObject/SpriteRenderer/Transform blocks and its `SceneRoots` entry; the 3 intentional `WoodenBarrel`/`(1)`/`(2)` instances near the barn are untouched.
  - Verified via `.\Run-Unity.ps1 check` (clean) → `setup` (clean, both the corrected `Layer_Midground` transform and the barrel deletion confirmed still present in the saved scene afterward — the `isNew` guard means an existing layer's transform is never re-stamped, so this holds going forward). **Not visually verified in Play mode** (no Play-mode access here) — worth a live check across several levels of differing size (e.g. L01 vs. L13/L18) to confirm the backdrop now covers edge-to-edge at every zoom level, not just rest.
- **L02 "Harvest Yard" redesigned — 2026-07-18, user request: "i have redesigned level2 - delete old level2 - update with new dump."** Old layout (4 haybale + 2 SemiHarvester, no wood/barrels) fully replaced via a fresh `LevelLayoutDumper` dump: 3 haybale + a small 3-plank wood cluster (2 vertical `Plank_Shork` + 1 horizontal `Plank_Horizontal` bridging them) + 2 `ExplodingBarrel` props, guarded by 3 SemiHarvester (up from 2). Kept the same asset filename (`L02_StoneWall`) and `id`/`name` — no separate deletion was needed, `Generate All Level Data` overwrites the existing asset in place; `DeleteStaleAsset` only matters when a `Make(...)` call is removed or renamed entirely. Verified via `.\Run-Unity.ps1 levels` → `setup` → `check`, all clean. **Not visually verified in Play mode** (no Play-mode access here).
- **`Layer_Midground` backdrop-coverage bug fixed, then further hand-tuned + new L1 scenery added — 2026-07-18.** User reported the new `ParallaxMidground.png` backdrop wasn't stretching across the visible screen and the old sunset sky was still peeking through at the top. Root cause: `Layer_Midground`'s transform (see the 2026-07-17 entry below, real bug #3) was still the tiny hand-placed value from the OLD 3-painting "theatre flats" design (`0.7371198, 0.7160633` scale, off-centre) — correct for a partial-band reveal layered with 2 other paintings, but never recomputed when the design collapsed to one single full-screen backdrop, so it left gaps where `Background_SkyV1` (sitting behind it, `sortingOrder -100`) showed through. Fixed by setting it to the same cover-scale formula `WireParallaxLayer()` already uses for a freshly-created layer (fills the camera's rest view at orthoSize 4.5, 16:9 → position `(0, -2)`, uniform scale `0.5882353`); confirmed a fresh `Wire Scene References` pass left the edit untouched (existing-layer transforms are still never re-stamped, per the `isNew` guard). The user then further hand-tuned it to an asymmetric `(0.77127695, 0.5882353)` scale at `(-0.5507, -2)` — wider on X than the plain cover formula, not a bug.
  - **Same session, user hand-edited the scene further**: added 3 new `WoodenBarrel` decorative props clustered near the barn (~X -1.1 to -1.7), **re-added `GnarledTree`** (removed 2026-07-17 when the windmill silhouette moved into the backdrop painting itself; brought back separately at `(8.22, -3.94)` scale `(2.675, 3.602)`, far right of the level), repositioned/rescaled `GrassTuft (6)` and both `WildFlowers` clusters, and nudged `FarmCannon` (`Y -4.74 → -4.7032`, scale Y `1.3868444 → 1.3316845`). None of these needed code changes to survive a `Wire Scene References` pass — L1 scenery has never been tracked by name in any of `SceneSetup.cs`'s cleanup/rename passes, so hand-added or hand-moved props are always safe. `SceneSetup.cs`'s `FarmCannon` fallback creation-stamp (only used if the GameObject is ever deleted and needs recreating) was re-synced to the current live position — it had been silently stale since the 2026-07-17 "only stamp on creation" fix stopped it from ever running again against the live scene's cannon, so it still held the old pre-barn-door-alignment `(-7.54, -5.03)` value.
  - **Flag, not fixed:** both `WildFlowers` clusters were also given `sortingOrder = 10` in this pass, which is well past the documented "Scenery sortingOrder rule" ceiling (decorative props must be `≤ 1`, since blocks=2/robots=3/cannon=4/smoke=5/animals=6 all sit above that). At 10 these wildflowers now render in front of every gameplay object in the scene, including the cannon and any animal/robot standing near them. Not reverted since it may be a deliberate foreground-detail choice (flowers drawn in front of the cannon for depth) rather than an accidental drag — worth confirming intent before the next content session, since this is the exact class of sorting-order bug (`docs/HISTORY.md`) that has caused real invisible-object issues here before.
  - **Not visually verified in Play mode** (no Play-mode access here) — worth a live check that the backdrop now fills edge-to-edge with no sky sliver, and that the new barrels/tree/wildflowers read correctly at the new sorting order.
- **New hand-composed parallax backdrop (`EnvironmentDepthSystem`) + 3 real "Wire Scene References stomps hand-placed Editor state" bugs found and fixed — 2026-07-17, long session.** Started as a 3-painting parallax stack (`ParallaxFarHills`/`Midground`/`Foreground.png`, each a full opaque scene incl. its own sky) revealed via a new `FarmFury/ParallaxBandClip` shader (`_ClipAbove` cutoff, reveals only the bottom band of a sprite's own local UV space so a nearer opaque layer doesn't fully hide the ones behind it) — see `docs/HISTORY.md`-style reasoning below and the shader/class comments for the full "theatre flats" design. **Simplified to a single frozen `ParallaxMidground.png` layer per user decision** partway through: the user is now hand-composing the whole scene (barn/tree/cannon/backdrop as one fixed arrangement, redesigning levels from scratch via fresh `LevelLayoutDumper` dumps against it) rather than wanting per-level auto-zoom to drive the backdrop. `EnvironmentDepthSystem.cs` no longer has any runtime rescale logic at all — `Layer_Midground`'s transform is whatever is saved in `Game.unity`, permanently; `CatapultLauncher.OnLevelStarted()` no longer calls into it.
  - **Real bug #1 — `FarmCannon` position/scale stamp regressed.** `SceneSetup.cs`'s `WireLauncher()` had a documented (from 2026-07-13) "only stamp on creation" fix that had regressed back to running unconditionally — every Wire Scene References pass silently reverted the user's own hand-repositioned cannon (they were aligning it to the barn door) back to the old `(-7.54, -5.03)` literal. Fixed by moving the position/scale assignment inside the `if (cannonGO == null)` branch, matching `CatapultLauncher.BuildCannon()`'s own already-correct runtime creation logic.
  - **Real bug #2 — `GroundVisual_Placeholder` kept reappearing after deletion.** `EnsureGroundVisual()` (procedural flat grass-strip stand-in, added 2026-07-26) unconditionally recreated the GameObject if missing, with no way to know a deletion was deliberate — the user kept deleting it (superseded by the new backdrop's own ground-level art) and it kept coming back on the next `setup` run. Method removed entirely; one-time cleanup added to `WireAll()`'s existing placeholder-deletion loop.
  - **Real bug #3 — `Layer_Midground`'s own transform got stomped too**, caught mid-session: the Editor-time preview sizing in `SceneSetup.EnsureEnvironmentDepthSystem()`/`WireParallaxLayer()` duplicated the same cover-scale formula the (by-then-removed) runtime rescale used, and still ran unconditionally — so freezing the runtime side alone wasn't enough, one `setup` run still silently overwrote the user's hand-placed position/scale back to an auto-computed value. Fixed the same way as the cannon (`isNew` guard); the one already-clobbered instance was restored by hand from a position/scale recorded earlier in the same conversation (`0.1986, -1.22` / `0.7371198, 0.7160633`).
  - **Real bug #4 — a `GameObject.Find("Background")` name collision** between the real world sky (renamed `Background_SkyV1` → `Background` by `EnsureBackground()` itself) and `VideoChromaKey`'s own celebration-overlay child (also literally named `"Background"`, built by `VideoChromaKey.FindOrCreate()`). Once ambiguous, this could attach a stray `BackgroundController` (a SpriteRenderer-cover-scale script, meaningless on a UI `Image`) to the wrong object, whose `Image.sprite` was never set at Editor time (`VideoChromaKey` only assigns it at Play-mode runtime) and whose Canvas (`sortingOrder=150`) was left active by default — rendering as a plain white box on top of everything any time the scene was viewed outside Play mode (user-reported "white square anomaly," confirmed via direct scene-file inspection, not guessed). Fixed by scoping `EnsureBackground()`'s fallback lookup to root-level, non-`RectTransform` objects only; `EnsureCelebrationVideoBackground()` now also force-deactivates the overlay's `Background`/`RawImage` children in the *saved* scene (not just at runtime) and removes any already-existing stray `BackgroundController`.
  - **L01 "First Contact" redesigned** from a fresh `LevelLayoutDumper` dump against the new backdrop: 4 haybale + 1 SemiHarvester only (2 `WoodenFence`-sourced Wood blocks in the raw dump were deliberately excluded — they sit almost exactly on top of the permanent decorative `WoodenFence` scene props, the same "decorative prop accidentally swept into a dump" bug already hit once on L02). **SemiHarvester scale `(3.564, 3.975)` recorded as the new standard size** — every other level's SemiHarvester is currently a much larger, inconsistent per-level value; flagged as needing a consistency pass once more dumps arrive.
  - Compile-checked clean and `Wire Scene References` re-run after every fix, each one verified directly against the saved `Game.unity` (grep/PowerShell YAML inspection), not just assumed. **Not visually verified in Play mode** (no Play-mode access here) — worth a live check that the frozen backdrop composition actually reads correctly at runtime, not just in the Editor.
- **Monetisation (Phase 6) planning session — 2026-07-16, no code written, advisory only.** Reviewed the GDD's §05 (7 revenue streams: Rewarded Ads/Starter Pack/Season Pass/Coin Shop/Power-Ups/Cosmetic Shop/Ad Removal) and §06 (Firebase-based infra: AdMob+AppLovin MAX, Unity IAP+RevenueCat, Firestore/Cloud Functions/Storage/Remote Config, UGS leaderboards) against the current 0%-built state, and worked out an adjusted plan with the user: **Supabase (Postgres/Auth/Edge Functions/Storage) replaces Firebase for the economy/data-layer foundation** (AdMob stays AdMob either way — independent choice), and **Firebase Crashlytics used standalone** (free, uncapped, doesn't require adopting the rest of Firebase) instead of Sentry (capped free tier, becomes a real recurring cost at volume — the original suggestion was wrong to present as a given). Recommended sequencing: front-load the Supabase auth/data-sync foundation soon (cheap while save data is still just World 1's 18 levels; expensive to migrate later across 6 worlds), defer ads/IAP/season-pass/cosmetics until closer to actual release readiness, with server-side coin/receipt/score validation (Edge Functions) required alongside IAP, not after. Full plan with reasoning saved to this session's memory (`monetisation_plan.md`) for continuity — read that before starting the Supabase setup.
- **Level Complete/Failed "Home" button replaced with "Back" (`Btn_back.png`), now returns to the World 1 map instead of skipping to the main menu — 2026-07-16, user request.** `HUDController._homeButtonSprite` renamed `_backButtonSprite`; `OnLevelCompleteHomeClicked()`/`OnMenuClicked()` no longer call `WorldMapController.SkipToMainMenu()` — `GameManager.LoadMenu()` alone is enough since `WorldMapController` already shows itself on `GameState.Idle`. `WorldMapController`'s own separate Home button (on the map screen itself) and Quit still go straight to the main menu, unchanged. **Not visually verified** (no Play-mode access here).
- **Settings page rebuilt again — 2026-07-16, several rounds of user screenshots/feedback in one session, `MainMenuController.BuildSettingsPopup()`.** Cumulative changes, in order:
  - **Scoreboard.png re-supplied larger, now 2:1 (2048x1024, was 4:3 1600x1200)** — popup box resized 1333x1000 → 1800x900 to match undistorted; since the new box is noticeably *shorter*, every vertical position/size in the popup is scaled by a new `VScale=0.9f` constant so nothing that fit the old box spills past the new backdrop.
  - **Tab headings re-skinned onto `Btn_plaque.png`** (was a flat colour box) via a new optional `bgSprite` param on `MakeToggleButton`; existing `TabOnColor`/`TabOffColor` tint logic carries over unchanged as the selected/unselected indicator.
  - **Music/SFX/Left-Right-Handed toggles re-skinned onto `Btn_off.png`/`Btn_on.png`** star icons (was a text "ON"/"OFF" pill) via new `offSprite`/`onSprite`/`iconSize` params on `MakeStateToggle` — volume sliders explicitly left untouched per user instruction.
  - **"New page" navigation**: tapping a tab now hides the tab list entirely and shows that tab's content as a distinct page (`SelectTab`/`ShowTabList`, `_settingsTabGridRoot`/`_settingsContentRoot`), instead of showing content inline below the tab grid — content area enlarged to use the room the list vacates.
  - **GAMEPLAY tab removed entirely** per explicit user request ("remove 'Gameplay'") — Language cycle button, Left/Right Handed toggle, and Tutorial Replay button are gone with it, no other home found for them yet; `SettingsTab` enum, `_setLanguageVisual`/`_setHandedVisual` fields, and `OnTutorialReplayClicked()` all removed. Remaining 6 tabs realigned from a 2-column grid to a single centred column, widened+heightened (540x64 → 460x92) so the plaque reads as a real button instead of a stretched strip.
  - **Close/Back consolidated to one `Btn_quit.png` icon** (was two separate icons — a text "X" close button, later a duplicate `Btn_quit` Back button — both visible at once whenever a tab's content page was open, reading as a redundant "secondary" icon). Now a single always-visible icon whose tap target is contextual (`OnQuitOrBackClicked()`): backs to the tab list if a tab's content is showing, else closes Settings. Enlarged 56x56 → 150x150 (this project's standard icon size) and repositioned twice (first to clear the frame's top corner medallion art, then nudged further inward) to sit on the actual parchment board rather than the wooden border.
  - **Every tab given a large-uppercase page header** ("MUSIC"/"STATS"/"SCORES"/"STORY"/"ACCOUNT"/"ABOUT", 76pt bold — roughly 2x the row-label font size) and re-spaced rows to clear it. Found and fixed in passing: the **Scores tab had zero content at all** (not even a placeholder) — a pre-existing gap from before this session, now given the same header + "Coming Soon" treatment as Account.
  - **Real bug found and fixed: `MakeToggleLabel()` was silently discarding every custom text colour.** It called `StyleAsGameFont()` unconditionally after applying the requested `color` param, and that method force-overwrites `tmp.color` back to white plus a gold vertex gradient — every "darken the text" edit made earlier in this same session had visually done nothing (confirmed via a user screenshot showing the "STATS" header still gold despite being coded dark). Added a `useGameFont` param (defaults `true`, so button/plaque labels keep the gold bubble-lettering look) and set it `false` on ~24 body-text call sites across every tab plus the Story Reader popup, which now actually render in the new dark ink colour (`SettingsTextColor`/`SettingsSubTextColor`).
  - **Story Reader popup** (separate popup, opened from Story/Credits rows): fixed a "secondary board" bug — its box was a different size (1600x900) than the Settings box behind it (1800x900, stays active underneath by design), so the wider box's frame peeked out from behind, reading as a double border; matched both to 1800x900. Also wrapped its header/title/body in a new `RectMask2D`-clipped container so text can't visually bleed onto the wood frame regardless of exact border-thickness assumptions.
  - **Not visually verified end-to-end** (no Play-mode access here) — this was a lot of coordinate/colour work across every tab with no way to render it; worth a full live pass before considering it settled.
- **Cluck's Level Complete celebration video hardened against a stale-frame race — 2026-07-16, user asked whether the reported "old translucent chicken video" (turned out to be level-independent — the wiring is global by `AnimalType`, not per-level, confirmed by reading `LevelCompleteManager`/`SceneSetup`) could be a caching issue.** `VideoChromaKey` shares one `VideoPlayer`/`RenderTexture` between the Level Complete (Cluck) and Level Failed (robot, still chroma-keyed) sequences, and its `frameReady`/`prepareCompleted` callbacks were subscribed once permanently — a late-arriving event from a superseded clip's decode could in theory fire after a newer `Play()` call started, revealing stale/wrong-shader content. Fixed by subscribing a fresh single-shot handler per `Play()` call, tagged with a monotonic `_playToken`; any handler whose token no longer matches the current one is ignored. Compile-checked clean; root cause of the original report was never conclusively reproduced, this closes the most plausible latent bug found by reading the code.
- **L18 → "Frozen Tundra" (World 2) transition video wired — 2026-07-16, user-supplied `TransitionVideo_Draft.mp4`.** New `WorldTransitionManager` (self-bootstraps via `CatapultLauncher.Awake()`, same pattern as `LevelCompleteManager`) plays the clip after L18's scoreboard panel, then lands on the main menu — **not** a real World 2 map, since World 2 doesn't exist yet (0% built). `HUDController.OnLevelCompletePlayClicked()` checks `WorldTransitionManager.TryPlayTransition(levelIndex, ...)` before falling back to its normal immediate `LoadMenu()`; every level except L18 is unaffected. Swap the completion callback to a real World 2 map once that content exists — no change needed to the video-playing logic itself.
- **L11: stray floating `RobotType.Basic` robot removed — 2026-07-16, user report: "small robot rendering in the middle of the air."** It had no custom scale in the original scene dump (a known, previously-documented gap), so it spawned at the Robot prefab's tiny default size at X=7.155, well past the rest of L11's structure (tops out ~X=5.92) — since robots spawn `Static`/no-gravity, it just sat there with nothing built underneath it. Deleted rather than repositioned (no record of its intended spot); this was also `RobotType.Basic`'s documented "actual gameplay debut" in World 1, so L11 no longer introduces that type — flag if it should be re-added elsewhere.
- **Structure "hangs in mid-air" collapse-cascade bug found and fixed globally — 2026-07-16, user report on L12/L14, generalized after investigation.** Root cause: `HaybaleBlock.prefab` had `_stayKinematic=true` set *prefab-wide* (added 2026-07-26 to stop L01's decorative ground pile shifting when a sibling bale was hit — that original bug is actually already fixed by TakeDamage() only ever waking the block actually hit, making the prefab-wide flag obsolete for its original purpose). The flag applied to *every* Haybale placement in *every* level, not just L01's pile — a haybale stacked on a taller structure (L12/L14) had its support destroyed but, being `_stayKinematic`, never woke to `Dynamic` and just floated in place. Fixed by flipping the prefab default to `false` and adding a new per-instance `LevelData.BlockSpawnData.forceStayKinematic` override, set `true` only on L01's 4 ground-pile bales (`LevelDataGenerator.cs`). Confirmed Stone/`ExplodingBarrelBlock` already had no such exemption and already cascade correctly (partial damage wakes and cascades on every hit, not just the lethal one). Also added `CollisionDetectionMode2D.Continuous` to every block's `Rigidbody2D` (`BlockBase.Awake()`) — with more things now genuinely falling, a dense stack toppling at once could tunnel through the Ground collider's thin (0.5-unit) profile under the default Discrete mode in one physics step; same fix `AnimalBase`/`EggProjectile` already use. **Not visually verified** (no Play-mode access here).
- **L15: 4 near-duplicate `Plank_2DShork` blocks removed — 2026-07-16, user report: "the sprites are not rendering positioning correctly."** 4 of the level's Shork2D wood blocks were each paired with a near-identical twin only ~0.24-0.26 units apart on a 1-unit-wide sprite (~75% overlap) — compared against every other Shork2D/stacked-block placement in the file (all use ≥0.5 unit gaps), this reads as an accidental double-drag from the original Scene-view dump, not deliberate layering. Kept the lower-X block of each pair, removed the duplicate.
- **L17 match-up header art wired — 2026-07-16, user-supplied `level17.png`.** Was the one remaining gap in `_levelHeaderSprites[]` (L12-L16/L18 already had dedicated art); index 16 now points at it, confirmed via GUID match in `Game.unity`.
- **Commander toughness split into direct-hit vs. structural resistance — 2026-07-14, same-day follow-up, user: "make the commander stronger - it should take all three sprites to destroy - even with falling structure."** The single `_damageResistance` multiplier from the prior fix applied equally to Explosion/Fall damage (from L18's collapsing staircase and 2 dynamite barrels) as to real direct hits — since `ExplosionDamageFraction` equals `DirectHitDamageFraction`, an incidental barrel blast near the Commander could contribute as much as a real hit, letting 2 birds + 1 lucky explosion finish him instead of requiring all 3. Split into `_damageResistance` (direct hit/egg only, `0.61` on Commander — 3 solid hits to kill) and a new `_structuralDamageResistance` (explosion/fall only, `0.1` on Commander — structural collapse now chips only token damage, can't shortcut the fight). Default `1f` on both fields for every other robot type — no behaviour change elsewhere. See `docs/HISTORY.md` Round 21. Verified via `.\Run-Unity.ps1 check`/`setup`; confirmed both values on `CommanderRobot.prefab` on disk. **Not visually verified** (no Play-mode access here).
- **L18 sprites fixed (missing `artVariant` tags) and Commander made genuinely tougher — 2026-07-14, user report: "level 18 sprites have not rendered properly... also the commander must be stronger than it is."** Two real bugs: (1) `LevelLayoutDumper`'s raw-sprite path had no `"skew"` keyword and never inferred art variant for Stone at all, so 23 of the redesigned level's 25 blocks (all the `Stone_Square`/`Plank_Skew`/`Stone_Skew` pieces) silently rendered as the wrong generic sprite — fixed in the dumper (both the Wood and now-also-Stone branches route through an extended `InferWoodArtVariant()`) and hand-corrected in the already-pasted `L18_CaptainsLastStand` data. (2) `RobotEnemy`'s damage model scales every hit as `_maxHealth * fixed fraction`, so Commander's `_maxHealth=90` never actually took more hits to kill than a 26-35 HP grunt (~2 direct hits either way) — added a new `_damageResistance` multiplier (default 1, no change for existing robots), wired to `0.6` on Commander (~3 hits now). See `docs/HISTORY.md` Round 20 for the full write-up. Verified via `.\Run-Unity.ps1 check`/`levels`/`setup`. **Not visually verified** (no Play-mode access here).
- **L18 redesigned by the user into a Commander-solo boss fight — 2026-07-14, dumped fresh via `LevelLayoutDumper` ("I have re-designed level 18 - see dump; override current and replace with new").** Old layout (giant indestructible `StoneTower` + 2 Basic + 2 SemiHarvester + hand-placed Commander) fully replaced: a destructible ascending staircase of Stone/Barrel/Wood-Skew pieces (no indestructible structure this time) with the Commander as the ONLY robot in the level. Along the way, found and fixed a real bug in `LevelLayoutDumper.cs`: neither of its two robot-detection paths (prefab-instance or raw-sprite) had ever handled `RobotType.Commander` — a placed `CommanderRobot.prefab` silently dumped as the wrong type (Basic) with no warning, and a raw Commander sprite was dropped entirely — which is why the user's first dump attempt for this redesign came back with an empty `robots[]`. Both paths now have an explicit Commander branch (ordered before the generic Basic/`"robot"` catch-all, same pattern as the existing SemiHarvester-before-Harvester ordering). See `docs/HISTORY.md` Round 19 for the full write-up. Verified via `.\Run-Unity.ps1 check`/`levels`/`setup` (clean compile, level regenerated, scene re-wired). **Not visually verified** (no Play-mode access here).
- **L18 fixed: oversized `StoneTower` was engulfing the level, match-up header/Commander card wired — 2026-07-14, user report: "the design is not rendering correctly" + "wire up matchup card for the commander robot... remove the other cards and only slide the commander into place."** Three issues, one investigation: (1) L18's `StoneTower` was sized 7.33x7.499 (nearly double every earlier StoneTower this project has had) centred in the middle of the level, so its bounding box engulfed almost every other block/robot instead of standing beside them like L10/L11's smaller, edge-positioned towers did — resized to 3.6x6.9 at (6.8, -3.1), Commander re-anchored to the new top edge (6.8, 0.81); see `LevelDataGenerator.cs`'s L18 comment and `docs/HISTORY.md` Round 18. (2) L18's match-up header (`level18.png`) was already correctly coded (2026-07-13) but the live scene hadn't been re-wired since — fixed by re-running Wire Scene References. (3) The Commander boss card (`Commander_robot.png`, user-supplied) is now wired into `_robotCardSprites[3]` (was pointed at a nonexistent `Commander.png`), and `MatchUpScreen.Show()` now shows ONLY the Commander card for a boss level (breaks out of the robot-card-priority loop the instant Commander is the primary card), instead of stacking it with the mook robots' cards like a normal multi-robot level. **Not visually verified** (no Play-mode access here).
- **Cannon aim (drag angle/power) fixed — 2026-07-14, user report: "there is now almost no directional trajectory, nor strength."** Root cause: `CatapultLauncher.PivotPos()` still anchored the drag-angle/power math to the abstract Launcher's own `transform.position` (`X≈-2.327`), never updated when `FarmCannon` was repositioned to `X≈-7.54` the previous day ("cannon too close" fix) — the exact same class of bug already caught and fixed same-day for the camera auto-zoom (`ComputeOrthoSizeForLevel`), just missed here. With the pivot ~5 units from where the player actually drags (near the visually loaded bird), `_pullDistance` (power) was already near max on the first drag frame regardless of actual drag length, and `_dragAngle` barely swept at all for a normal local drag — power pinned near max, direction unresponsive. Fixed by making `PivotPos()` return `_cannonRestPos + CannonLoadedBirdOffset` (the loaded bird's real rest position) instead of the stale anchor calculation — now tracks the cannon wherever it's placed, with no distance to re-tune if it moves again. Verified via `.\Run-Unity.ps1 check` (clean compile). **Not visually verified** (no Play-mode access here) — worth a live drag-and-fire check across the full power/angle range. Full root-cause writeup in `docs/HISTORY.md` Round 17.
- **Settings popup redesigned from a flat heading list into a 7-tab screen — 2026-07-13, user-supplied ASCII mockup ("let's quickly look at settings: see how I want to redesign...").** `MainMenuController.BuildSettingsPopup()` previously listed 5 static headings down the `Scoreboard.png` backdrop (`Player`/`High Scores`/`Worlds`/`Saved Game`, only `Music Toggle` real). Now: title bar with a top-right `[X]` close, a 2-column grid of 7 tabs (`SettingsTab` enum: `Audio/Gameplay/Stats/Scores/Story/Account/About`), a divider, and a swappable content area below (`_tabPanels` dictionary, `SelectTab()` toggles active panel + tab highlight colour). Built one tab at a time per explicit user direction ("will build each one separately") across the same session:
  - **AUDIO (real)** — Music and SFX are now independent ON/OFF toggles (were one combined "Music Toggle" that flipped both together) plus a 0-100 volume slider each. `AudioManager` gained `MusicVolume`/`SfxVolume` static properties + `SetMusicVolume()`/`SetSfxVolume()` (same static-property + `PlayerPrefs` pattern as the existing `MusicEnabled`/`SfxEnabled`) — `SfxVolume` is a global multiplier applied in `Play()`/`PlayClip()`/`PlayFalling()` on top of each sound's own already-tuned per-sound `VolumeScale`, so it doesn't disturb any past balance work (e.g. the cannon-shot-vs-falling-scream mix), just scales the whole mix. A `Voice/Dialogue` toggle was in the original mockup but dropped per user decision — no voice-line audio content exists anywhere in this project to back it. New `MakeStateToggle()`/`MakeVolumeSlider()` helpers (procedurally-built ON/OFF pill + a hand-built `Slider`, since this project has no prefabs and builds all UI at runtime) support this and every tab after it.
  - **GAMEPLAY (real, with caveats)** — Language: a tap-to-cycle button through 6 languages (English/Spanish/French/German/Portuguese/Japanese), persisted via new `GameplaySettings.cs`, but **no localization framework exists anywhere in this codebase** — every string is hardcoded English, so selecting a non-English option has no visible effect yet; built this way deliberately per the user's own framing ("even if only English at launch, show it so players know more will come"). Chose a cycle-button over a real `TMP_Dropdown` per user decision — a true dropdown needs a hand-built template/viewport/item hierarchy with no prefabs to lean on, higher risk of a broken list with no Play-mode access to verify it. Left/Right Handed: toggle persists the preference (`GameplaySettings.LeftHanded`) only — **no actual mirroring is implemented** (camera position, `CatapultLauncher`'s drag-angle/aim math, and launch trajectory are all untouched); real mirroring was scoped out as a separate, bigger engineering pass per explicit user decision. Tutorial Replay: jumps straight into L01 via `GameManager.ForceStartLevel(0)` (same path `MatchUpScreen`'s own Skip button uses) — there's no dedicated tutorial overlay/hint system in this project, L01 itself is the de facto tutorial, so this is really "restart at Level 1," not a richer guided replay.
  - **STATS (real)** — Two columns: Statistics (Total Stars Earned, Levels Completed, Total Score, Cannonballs Fired, Robots Destroyed, Favourite Animal, Time Played) and Personal Records (Highest Level Score, Perfect Levels). Some rows were already computable from existing `ScoreManager.GetBestScore()`/`GetBestStars()` per-level data (Stars/Completed/Score/Highest/Perfect) with zero new tracking; others needed a brand-new counter system — new `PlayerStatsTracker.cs` (self-bootstraps via `CatapultLauncher.Awake()`, same null-safety fallback pattern as `AudioManager`/`LevelCompleteManager`) hooks `RecordCannonballFired()` into `CatapultLauncher.Fire()` and `RecordRobotDestroyed()` into `RobotEnemy.Die()`, plus a `Time Played` accumulator that only ticks while `GameState.Playing` (not menu/map browsing time), periodically flushed to `PlayerPrefs`. Denominators (`X / 18` levels, `X / 54` stars) use `GameManager.Instance.TotalLevels` — auto-discovered, not hardcoded — rather than the full 6-world design target (`108`/`324`) the user's original mockup numbers implied, since only World 1 exists today and showing progress against unbuilt content would be permanently unreachable; this was an explicit scoping decision, not a bug. "Longest Combo Chain" (no combo mechanic exists anywhere in this game) and "Most Stars in One Attempt" (redundant with Perfect Levels) were both dropped per user decision rather than invented.
  - **SCORES (placeholder)** — stays a "Coming Soon" panel per explicit user decision. The mockup's 4 rows (Personal Best Scores, Global Leaderboard, Top 100, Friends Leaderboard via Facebook/Game Center, Weekly Challenge Winners) mostly need a real backend — monetisation/backend is explicitly 0% built (see Phase 6 below), no Firebase/UGS/server of any kind exists. Only "Personal Best Scores" would've been real (same local `ScoreManager` data Stats already uses); left unbuilt anyway since the user chose to placeholder the whole tab rather than split it.
  - **STORY (real, first draft)** — New `StoryContent.cs` holds first-draft lore: The Farm Fury Story (origin story — robots invade, Cluck's crate-refusal sparks the rebellion), Character Profiles (all 8 animals, each tied to their real gameplay ability), Robot Enemy Files (the 4 real robot **types** — Basic/Harvester/SemiHarvester/Commander — not "18 pages, one per robot" as the original mockup specced, since there are only 4 distinct robot types reused across 18 levels, no 18 unique named robots to profile), and World Journal (all 6 planned worlds, using the launcher/mechanic names already in the World table below, even though only World 1 exists in-game — Worlds 2-6 are 0% built). Built a single reusable full-screen paginated reader popup (`BuildStoryReaderPopup()`/`OpenStoryReader()`/`RefreshStoryReaderPage()` — title, Prev/Next, auto-sizing body text) shared by all 4 categories rather than 4 separate panels, since they're all the same "title + paginated body" shape; also reused by the About tab's Credits row. This is a first draft meant to establish tone/structure — review and rewrite freely, nothing here is final.
  - **ACCOUNT (placeholder)** — still a bare "Coming Soon" panel, not yet built out.
  - **ABOUT (real, with explicit blockers flagged)** — App Version is fully real (`Application.version`, live). Credits opens the shared story reader with only verifiably-true project facts (Unity 6.5, Kling AI-generated art) plus placeholder `[Add team credits here]`/`[Add special thanks here]` sections — per explicit user decision, real names weren't invented. Privacy Policy, Terms of Service, Support/Contact, and Rate The App are all inert placeholder rows (dimmed non-interactable button + explanatory subtitle) — no hosted legal pages, no support email, and no live store listing exist yet; deliberately not faked (a placeholder legal-document URL is real compliance risk if it ever ships by accident). **Restore Purchases is flagged more seriously than the others** — both in the UI subtitle and in code comments — as a genuine Apple App Store submission blocker, not just a "coming soon": there is currently no IAP system of any kind in this project (0% built, see Phase 6), and Apple's own review guidelines require a working Restore Purchases flow before an app with IAP can ship to iOS.
  - New helper methods on `MainMenuController` supporting all of the above: `MakeStateToggle()`, `MakeVolumeSlider()`, `MakeCycleButton()`, `AddStatRow()`, `AddStoryRow()`, `AddInertRow()` — all procedurally-built (no prefabs, matching this project's existing UI convention), documented inline with why each exists.
  - **Not visually verified** (no Play-mode access here) — worth a live check on tab-grid spacing/text sizing at actual screen size, especially the Stats tab's two-column layout and the Story/Credits reader's auto-sizing body text with the longest entry (the ~280-word origin story).
- **Cannon repositioned by hand + camera auto-zoom and launch power re-tuned to match — 2026-07-13, user report: "the cannon is too close."** User moved `FarmCannon` from its 2026-07-03 "ground truth" position (`X-3.0012, Y-5.1223`) to `X-7.54, Y-5.03` directly in the Editor (along with `OakTree`/`OldBarn_Right`, both purely decorative background dressing), giving real distance between the cannon and the structures it fires at. Two real problems this surfaced and fixed the same session:
  1. **`CatapultLauncher.ComputeOrthoSizeForLevel()` (the per-level camera auto-zoom, added 2026-07-11) was seeding its bounding box from `transform.position`** — the abstract "Launcher" aim-math anchor (`X-2.327`, independent of the visual cannon, see Coordinate System below) — not the real `FarmCannon` GameObject. Previously the two sat only ~0.7 units apart so it didn't matter; after the reposition they're ~5.2 units apart, so the auto-zoom had no awareness the cannon reached that far left and risked clipping it off-screen (worse on any aspect narrower than 16:9, where the fixed-camera-center visible range is smaller). Fixed to seed from the real `_cannonGO`'s `SpriteRenderer.bounds`, and to compute required zoom from the camera's actual FIXED rest position outward in both directions (asymmetric-aware — cannon far left, structures far right is not a "symmetric content span," it needs the bigger of the two one-sided distances doubled, not the two-sided span halved).
  2. **`SceneSetup.cs` was unconditionally stamping the cannon's position/scale back to the old 2026-07-03 values on every single Wire Scene References pass**, even when the `FarmCannon` GameObject already existed with a different, deliberately hand-placed position — a real bug caught live when running `setup` (needed to re-sync the launch-speed values below) silently reverted the user's just-made reposition. Fixed by updating the hardcoded literal in `SceneSetup.cs` to the new position; flagged in a code comment that this line runs unconditionally and must be updated here (not just in the Editor) if the cannon ever needs to move again.
  - **Verified the reposition's actual gameplay impact wasn't just cosmetic** — scanned every block/robot in all 18 levels (script, not eyeballed) against the new launch point using the same projectile-height formula `y(x) = x·tanθ − g·x²/(2v²·cos²θ)` the 2026-07-11 speed tuning used. Moving the launch point ~4.5 units further from the same absolute-X level content meant every level now needed roughly that much more real range. Worst case by a wide margin: **L18's Commander boss fight** — at the old `_maxLaunchSpeed=8.5`, max power at the best available angle fell a full unit short of reaching the boss's height at the new distance (margin `+1.38 → -1.00`, i.e. mathematically unreachable, not just harder). Several other levels (L17, L13, L16, L08/L09, L07, L11) had their margins roughly halved too. Raised `_minLaunchSpeed`/`_maxLaunchSpeed` `3.5/8.5 → 3.8/9.3` (proportional), restoring a same-or-better safety margin than the original 2026-07-11 tuning everywhere checked (L18 boss: `+1.69`). Re-synced in `SceneSetup.cs`'s existing stale-value-trap re-sync block (same pattern as the 2026-07-11 entry below) and confirmed both the new cannon position and the new launch speeds landed correctly in `Game.unity` after a fresh `setup` run.
  - **Not visually verified** (no Play-mode access here) — worth a live playthrough of L18 in particular to confirm the arc feels right, not just mathematically reachable, and a general check that the wider auto-zoom range doesn't make early, small-content levels (L01-L02) feel too zoomed-out now that the bounding box always includes the cannon's real, farther-back position.
- **World 1 is now complete: L01-L18, no gaps — 2026-07-12 session.** L10-L18 hand-built via the established `LevelLayoutDumper` workflow (dump → paste into `LevelDataGenerator.cs` → `check`/`levels`/`setup`), continuing the plan from the previous session: L10 introduces Bessie (3 Harvester + 3 SemiHarvester + a new `indestructible` StoneTower), L11 introduces the third robot type (`RobotType.Basic`/"Robot_Pawn" art) alongside Harvester/SemiHarvester as "Full Roster", L12-L17 continue mixing all three at increasing density (up to 10 robots in L13), and **L18 "Captain's Last Stand" adds a new boss robot type, `RobotType.Commander`** (see below). `LevelDataGenerator.cs` now generates 18 `LevelData` assets; `GameManager` confirmed wiring all 18 with no gaps.
  - **`WoodArtVariant` enum massively expanded** (`Auto/Flat/Horizontal/Vertical` → +`Short/VerticalShort/Horizontal2D/Shork2D/Shork/Cart/Barrel/Square/Block/RuinedWall/Tower/Skew/Diagonal`) — the original 4 generic buckets collapsed ~13 visually distinct named prop shapes down to reusing 2-3 images per block type, so diagonal/skewed pieces placed to bridge gaps in the Scene view rendered as plain flat rectangles in-game, leaving visible gaps (user report: "it doesnt seem like the sprites that I made the scene with are the correct ones... there are too many gaps"). `BlockBase` gained ~13 new dedicated `_spr*` fields (one per named shape, shared between Wood/Stone where the shape name is the same but the sprite differs per subclass — e.g. `Skew`/`Diagonal`), all wired in `SceneSetup.WireBlockSprites()`. Also fixed mid-build: `WoodArtVariant.Flat` was silently aliasing to `_sprNormal` (`Plank_Horizontal.png`) instead of the real `2D_Block_Wood_Flat.png` asset L17 actually used — added a distinct `_sprFlat` field.
  - **`BlockBase.Indestructible`** (new bool field) — a block that never takes damage, never wakes/falls via the collapse cascade, and needs a real destruction trigger (see `ForceDestroy()` below) — used for L10 and L18's `StoneTower` fixed-scenery structures. `LevelData.BlockSpawnData.indestructible` threads it through from level data.
  - **Block collapse-cascade made same-frame recursive** — `BlockBase.CheckForBlocksOnTop()` previously only flipped a supported block to Dynamic and relied on its own eventual landing-collision impulse to continue the chain upward; a gentle settle often never cleared `minDamageImpulse`, silently breaking the cascade partway up a tall stack ("structure fall and break all the way to the ground, blocks can't just fall over and hang in the middle of the air" — user report). Now recursively calls the newly-woken block's own `CheckForRobotsOnTop()`/`CheckForBlocksOnTop()` immediately in the same frame.
  - **`RobotEnemy.EggDamageFraction` raised 0.12→0.16** ("give the eggs slightly stronger damage") — still the deliberately weakest per-hit category.
  - **MatchUpScreen "deck of cards" generalized** — up to 3 robot cards (`RobotCardOrder` static priority array, was per-pair-swap logic that only handled 2) and a second animal card (Bessie overlaying Cluck, mirroring the robot deck) for whenever a level's `birds[]`/`robots[]` contain more than one distinct type. Offsets tuned down from an initial overcorrection (final: `Animal2RestOffset=(-70,50)`, `Robot2RestOffset=(70,50)`, `Robot3RestOffset=(140,95)`) after a user screenshot showed cards rendering too high instead of overlapping like a real deck.
  - **Icon buttons re-wired to user-renamed files** — `Btn_quite.png`→`Btn_quit.png`, `NoSound.png`→`Btn_nosound.png`; skip button now uses real `Btn_skip.png` art instead of a plain box + "SKIP" text label.
  - **Real, long-standing prefab-wiring bug found and fixed**: `SceneSetup.SetPrefab()`'s `AssetDatabase.FindAssets(prefabName + " t:Prefab", ...)` call is a FUZZY name search, not exact — searching `"Robot"` also matches any prefab whose name merely *contains* "Robot" (`HarvesterRobot`, `SemiHarvesterRobot`, and now `CommanderRobot`), and silently picks whichever sorts first alphabetically among the matches. This meant `_robotPrefab` (meant for `RobotType.Basic`) had actually been wired to `HarvesterRobot.prefab` for this entire session (H < R) — **every `RobotType.Basic`/"Robot_Pawn" spawn across L11-L17 was silently spawning a Harvester instead** (wrong art, wrong HP) — and would have silently flipped to `CommanderRobot.prefab` once that prefab existed (C < R too). Fixed by requiring an exact filename match among the fuzzy results before falling back to the old fuzzy behaviour. Confirmed fixed via a fresh Wire Scene References run: `_robotPrefab -> .../Robot.prefab`, `_commanderPrefab -> .../CommanderRobot.prefab`.
  - **New boss robot type: `RobotType.Commander`** (L18 only, so far) — `CommanderRobot.prefab` (HP=90, well above every other type) wired from user-supplied `Commander.png`/`Commander_Alert.png`/`Commander_Hit.png`/`Commander_Explode.png`. **3-pose progression** (user request: "the commander has three poses. when first hit change to Commander_Alert; when hit again change to Commander_hit"): `RobotEnemy` gained a second, EARLIER persistent pose tier (`_alertSprite`/`AlertHealthFraction=0.66`) layered on top of the existing single-tier critical-pose system (`_criticalSprite`/`CriticalHealthFraction=0.4`, wired to `Commander_Hit.png` here) — null on every other robot type, so this is a no-op everywhere else. **Boss-defeat destroys its guarded structure** (user request: "when commander explodes the whole tower should destruct"): new `BlockBase.ForceDestroy()` (bypasses both the `IsDestroyed` and `Indestructible` guards — a deliberate scripted exception, not a change to how `Indestructible` behaves under normal damage) plus `RobotEnemy._destroyOnDeath`/`AddDestroyOnDeath()`, fired from `Die()`. Linked at RUNTIME in `LevelLoader.SpawnRobot()` (not prefab-time — both the boss and its guarded structure are freshly spawned per level from `LevelData`, not static scene objects): any `RobotType.Commander` automatically collects every already-spawned `Indestructible` block in the same level (blocks always spawn before robots in `LoadLevel()`) via `AddDestroyOnDeath`, generic enough to reuse for any future boss+guarded-structure level. L18's dump didn't include the Commander (user hadn't placed it in the Scene view yet) — hand-placed at X=6.76, Y=0.85 (directly atop the StoneTower's computed top edge) per explicit user follow-up ("place the commander at the top of the tower").
  - **None of L10-L18 visually verified** (no Play-mode access here) — worth a live playthrough, particularly the Commander's pose transitions, the tower-collapse timing on his death, and whether the newly-fixed `RobotType.Basic` prefab bug changes the felt difficulty of L11-L17 (they were all tuned/played against the wrong, tankier Harvester art+HP standing in for Basic robots until this fix).
- **Grass placeholder given actual texture — 2026-07-11, user report: "the grass we inserted... appears as a green bar... is there a way to give it texture like a meadow feel."** `GroundVisual_Placeholder` (see the 2026-07-26 entry below) was a single white 1×1 pixel stretched via `transform.localScale` to 40×6.7 units with one flat tint — mathematically guaranteed to read as a solid bar, not grass. `SceneSetup.EnsureGroundVisual()` now generates a small (64×64) seamless-tileable procedural texture (`MakeMeadowGrassSprite()`) — 3 green shades with per-pixel brightness jitter plus ~40 scattered short darker "blade" streaks, Y-wrapped so the tile repeats cleanly — and renders it via `SpriteRenderer.Tiled` (`sr.drawMode`, `sr.size`) instead of scale-stretching a single pixel, so it actually repeats across the strip rather than blurring into a gradient. Same procedural-placeholder pattern already used elsewhere in this codebase (`BlockBase`'s crack overlays, `CatapultLauncher`'s dot/smoke textures) — still a stand-in until real Kling-generated meadow art exists, at which point delete `GroundVisual_Placeholder` per the existing instruction. **Not visually verified** (no Play-mode access here).
- **Per-level camera auto-zoom added — 2026-07-11, user report: "as the scenes progress it appears as if the cannon is very close to the robot structures, and the structures are only going to grow all the way to level 18... zoom out the camera."** Discussed flat-zoom vs per-level scaling; user chose per-level. `orthoSize` was a hardcoded `4.5` regardless of level. `CatapultLauncher.OnLevelStarted(data)` now calls a new `ComputeOrthoSizeForLevel(data)` every level load — computes the actual bounding box of that level's `blocks[]`/`robots[]` (block bounds from `size`, robot bounds from `scale` or the default 0.6×0.9 footprint) plus the cannon's own position, then solves the orthoSize needed to fit both axes with `ZoomPadding` (1.2u) breathing room. Clamped to `_minOrthoSize=4.5` (floor — preserves L01's explicit user-verified framing exactly, never zooms in tighter than before) and `_maxOrthoSize=8.0` (cap, so a future sprawling level doesn't shrink everything to illegibility). No per-level data/fields needed — fully automatic, applies to L01-L09 today and every future level. **Not visually verified** (no Play-mode access here).
- **Launch power/angle raised — 2026-07-11, user report: "with structures beginning to stack it is impossible to have the animal fly over... revisit the dynamics — allow for high and strong firing."** `CatapultLauncher._minLaunchSpeed`/`_maxLaunchSpeed` raised `3.0-6.0 → 3.5-8.5`; max launch angle raised `80° → 85°` (`MaxLoadAngle` 60→65, drag-angle cone 200°-260° → 200°-265°). Sized against the projectile-height formula `y(x) = x·tanθ − g·x²/(2v²·cos²θ)` (`g = -20 · 0.18` gravityScale): at the OLD max speed, the best-available high-arc angle could only just barely *reach* this level set's far edge (~X 9.9 from the launch point) at roughly launch height — mathematically incapable of clearing anything stacked above ground there. New max comfortably clears ~6.7 units of height at that same distance. Re-synced the scene's already-serialized `_minLaunchSpeed`/`_maxLaunchSpeed` values in `SceneSetup.WireLauncher()` (the `[SerializeField]` stale-value trap — confirmed `3.5`/`8.5` now on disk in `Game.unity`); `MaxLoadAngle`/`_armRestAngle` are `private const`, so no scene re-sync was needed for the angle change. Trajectory preview and the real fired shot share the same `LaunchVelocity()` math, so the dotted arc preview reflects the new range for free. **Not visually verified** (no Play-mode access here) — worth checking full-power near-vertical shots still feel controllable, and that moderate-angle full power doesn't consistently overshoot past a level's structures.
- **Two fundamental collapse/explosion rules added to the shared block/robot classes — 2026-07-11, user request: "when structure (wood, ice and further worlds structural building blocks) are hit... the structure naturally collapses and whatever it hits on the way down naturally also topples (like in real life)... if a robot falls and lands on a haybale or dynamite barrel this will automatically explode."** Both live in `BlockBase`/`WoodBlock`, not per-level `LevelData`, so they apply to every current AND future level/material automatically:
  1. **`BlockBase.CheckForBlocksOnTop()`** (new, called from `TakeDamage()` alongside the existing `CheckForRobotsOnTop()`) — a robot resting on a destroyed block already fell via the existing robot check, but a BLOCK resting on another block had nothing waking it (Static rigidbodies ignore gravity regardless of missing support), so a stacked tower's upper planks just floated in place when the base was destroyed. Wakes (Static→Dynamic) any block directly above (short 1.5u check column — block colliders are already correctly sized to their visuals via `Initialise()`'s native-bounds fix, unlike `CheckForRobotsOnTop()`'s deliberately generous 8u column, which exists only to compensate for a robot collider/visual-scale mismatch). Once Dynamic, gravity + real physics collisions carry the collapse further up a tall stack with no extra code — a newly-falling block's own eventual landing runs through the same `TakeDamage()` → `CheckForBlocksOnTop()` path for whatever rests on IT. `HaybaleBlock`'s `_stayKinematic=true` deliberately opts it out (unchanged design — haybale piles stay visually fixed even when their support is removed; only structural Wood/Stone/Barrel topple).
  2. **`WoodBlock.OnCollisionEnter2D()`** override — any collision where `_explodesOnRobots` is true (Haybale, `ExplodingBarrelBlock`) AND the other collider has a `RobotEnemy` now calls `TakeDamage(MaxHealth)` unconditionally, bypassing `BlockBase`'s normal impulse-threshold math entirely. Previously a robot landing on an explosive prop only detonated it if the impact happened to clear `minDamageImpulse` — a gentle settle (e.g. dropped via `MakeDynamicFromSupportLoss`, not a hard fall) might not have. Now it's guaranteed regardless of impact force.
  - Already covered, no change needed: `WoodBlock.DamageNearby()` already chain-damages nearby blocks (structural) and robots (direct) when an explosive prop dies — matches "haybale and dynamite barrels are the objects that explode causing chain reaction collapse" as-is.
  - **Not visually verified** (no Play-mode access here) — worth a live check on L08/L09's stacked towers, particularly whether a hit at the base now visibly topples the whole column rather than each block vanishing independently.
- **Match-up screen: Cluck's egg-burst teaser removed, Bessie now flies the opposite direction crossing Cluck — 2026-07-11, user request: "we introduced Bessie in level 7 — we now need to shoot Bessie across the screen. make Bessie come in from the other side and cross over Cluck. Cluck now no longer needs to shoot the eggs on the match up scene."** `MatchUpScreen.SpawnEggBurst()`/`AnimateBurstEgg()` deleted entirely, along with the `_eggSprite` field threaded through `WorldMapController`/`SceneSetup.WireMatchUpCards()` — Cluck's fly-by (`CluckFlyBy()`) now just flies, no cosmetic egg fan. New `BessieFlyBy()` coroutine mirrors `CluckFlyBy()`'s exact arc formula but reversed start/end X (right-to-left instead of left-to-right) — since both start simultaneously with the same duration, at any instant Bessie's X is Cluck's X mirrored around screen centre, so they visibly cross paths right at the arc's peak. Gated on `_bessieFlyEnabledForLevel` (computed in `Show()`: true only when `data.birds[]` actually contains `AnimalType.Bessie`, i.e. L07+) — L01-L06 show only Cluck's pass, unchanged. Own `AudioSource`/falling-loop (`_bessieFallingClip`) mirroring Cluck's, no separate launch SFX (Cluck's own cannon-shot sound already covers the pass). Art/audio wired via `SceneSetup.WireMatchUpCards()` from `Bessie_InFlight.png` / `Bessie_falling.mp3`. **Not visually verified** (no Play-mode access here).
- **Bessie's own gameplay flight/impact SFX wired — 2026-07-11, user request: "wire up bessie in flight sound effect [Bessie_falling.mp3]... this is when Bessie gets fired out of the cannon. use the same sound as cluck when Bessie hits."** Previously `CatapultLauncher.Fire()`'s `AudioManager.Instance?.PlayFalling()` call only ever fired for Cluck (`if (birdType == AnimalType.Cluck)`) — Bessie launched silently (beyond the generic one-shot cannon-fire sound). `AudioManager.PlayFalling()` now takes an `AnimalType animal = AnimalType.Cluck` param and swaps `_fallingSrc.clip` between `_fallingClip` (Cluck) and the new `_bessieFallingClip` (Bessie) per call; `CatapultLauncher.Fire()` now calls it for both Cluck and Bessie. The "hit" side needed no new code — `StopFallingFade()` already fades whatever clip is *currently* playing in the shared `_fallingSrc` generically, so it already applies identically to Bessie once she has her own loop playing; this satisfies "same sound as Cluck when Bessie hits" as-is. Wired via `SceneSetup.EnsureAudioManager()` from `Assets/Audio/Bessie_falling.mp3`.
- **Bessie's Ground Slam earthquake SFX wired — 2026-07-11, user request: "wire up Bessie_Earthquake.mp3 every time Bessie power is triggered and they hit the floor for earthquake."** New `BessieAnimal._earthquakeClip` field, played via `AudioManager.PlayClip()` at the top of `Shockwave()` — that coroutine is only ever started from `OnCollisionEnter2D` when the ability was actually triggered (`_slammed`) AND she hit the ground or a structure, so this fires exactly once per real tremor, never on a plain untriggered impact or a mid-air tap. Wired via a new `SceneSetup.WireBessieAudio()` helper (same `LoadPrefabContents`/`SaveAsPrefabAsset` shape as `SetFloatField`, generalised to an `AudioClip` on `BessieAnimal` specifically) from `Assets/Audio/Bessie_Earthquake.mp3`, called from `WireAll()` right after `WireLevelLoader()`.
- **L09 "Stone Bastion" added 2026-07-11** via `LevelLayoutDumper` — heaviest robot count yet: 8 robots (3 Harvester + 5 SemiHarvester, 5 of them lined up on a top platform row) + 23 blocks. First real use of `BlockType.Stone` as a genuine obstacle (3-piece rubble wall) rather than the single decorative piece L07 used. Raw dump was the widest yet (X 0.206-8.027, 7.821 units) — compacted with the same pivot-and-compress method as every prior level (pivot = leftmost element held fixed, factor 0.9198, landing the widest element at X~7.40). `birds[]` stays Cluck/Cluck/Bessie. Confirmed via `.\Run-Unity.ps1 levels` + `setup` — `GameManager` reports "wired 9 LevelData assets". **Not visually verified** (no Play-mode access here) — worth a check on whether 5 SemiHarvesters packed along the same top row read as distinct targets at this density.
- **L08 "Fortress Assault" added 2026-07-11** via `LevelLayoutDumper` — 6 robots (2 Harvester + 4 SemiHarvester) + 23 blocks (4 haybale, 16 wood, 3 barrel), two mirrored towers each topped by a barrel row. Raw dump spanned X 2.21-7.64 — compacted with the standard pivot-and-compress method (pivot = leftmost element, factor 0.9558, landing the widest element at X~7.40). `birds[]` stays Cluck/Cluck/Bessie, matching L07. Confirmed via `.\Run-Unity.ps1 levels` + `setup` — `GameManager` reports "wired 8 LevelData assets". **Not visually verified** (no Play-mode access here).
- **Match-up header art wired for L06/L08/L09 — 2026-07-11, user-supplied `level6.png`/`level8.png`/`level9.png` in `Assets/Sprites/UI/MatchUp/` (also re-supplied `level4.png`/`level5.png` in place, same filenames, needing no code change beyond re-import).** `SceneSetup.WireMatchUpCards()`'s `_levelHeaderSprites` array now covers indices 0-8 (L01-L09) with real dedicated art — L06 previously had none and fell back to `LevelHeader1.png`; that fallback is now gone. Confirmed via `.\Run-Unity.ps1 setup`.
- **Damage-model audit found and fixed 3 real bugs — 2026-07-10, user request: "review that you have attached all our changes on damage to all levels."** All prevented by the documented "`[SerializeField]` stale-value trap" (changing a C# field's `default=` never retroactively updates an already-serialized prefab):
  1. `WoodBlock.prefab`/`HaybaleBlock.prefab` were both stuck at `_areaDamage: 20` on disk — several balance passes since had moved the class default `20→35→25`, but nothing had ever re-written the live prefabs, so every spawned Wood/Haybale block was silently chaining 20 damage to neighbours instead of the intended 25. Fixed by adding explicit `SetFloatField`/`SerializedObject` re-syncs to `WireBlockSprites()`/`EnsureHaybaleBlockPrefab()`, confirmed via `grep` after a fresh `Wire Scene References` run (`_areaDamage: 25` now on both).
  2. All 3 robot prefabs (`Robot`, `HarvesterRobot`, `SemiHarvesterRobot`) were stuck at `_robotContactDamage: 15`, even though the class default (and the documented "raised 15→18" balance-pass history) says `18`. Same root cause, same fix pattern — explicit re-sync added to `WireRobotSprite()`/`EnsureHarvesterRobotPrefab()`/`EnsureSemiHarvesterRobotPrefab()`, confirmed `18` on all 3 prefabs after re-running setup.
  3. `ExplodingBarrelBlock.prefab` shows stale `_explodesOnRobots: 0`/`_areaDamage: 20` in its raw serialized file, but this is NOT a bug — `ExplodingBarrelBlock.Awake()` unconditionally overwrites both to `true`/`55` (and `_explosionStrengthMultiplier` to `2f`) on every spawned instance, so the prefab's serialized starting value is irrelevant at runtime. Confirmed this one needs no fix, only documented here so a future audit doesn't "fix" it into a double-assignment.
  - Since every one of these lives in a shared prefab/class (not per-level `LevelData`), all 7 levels (L01-L07) get the corrected values automatically — no per-level changes were needed or made.
- **L07's match-up screen was previewing Cluck, not Bessie — 2026-07-10, user report: "level 7 — bessie_card does not swipe in from the left."** Root cause: `MatchUpScreen.Show()` always read `data.birds[0]` for the animal card, but L07's `birds[]` is `Cluck, Cluck, Bessie` — Bessie fires last, not first, so her actual debut level was showcasing Cluck's card instead of hers (nothing was visually broken; the wrong animal was simply being shown, so no Bessie card ever appeared to "swipe in"). Fixed by scanning `birds[]` for the first entry that differs from `birds[0]` and showing THAT instead — mirrors the already-existing "second distinct RobotType" convention just below it in the same method. L01-L06 (all-Cluck) find no divergent entry and are unaffected.
- **Bessie was rendering with a solid pink tint over every pose — 2026-07-10, user report: "check why Bessie is pink?"** Root cause: `AnimalBase.HasRealSprites` only checked `_sprIdle`, and every subclass's `Awake()` uses `!HasRealSprites` to decide whether to apply a placeholder color tint (Bessie's is pink) — but that tint, once set at `Awake()`, is never reset when a later REAL sprite (Loaded/InFlight/Impact/Trigger) gets assigned. Bessie's character folder has real art for every pose EXCEPT `Bessie_Idle.png` (never supplied — confirmed via `ls`, only Celebrate1/2, Defeat, Impact, InFlight, Launch, Loaded, Trigger, Trigger1 exist), so the one missing slot poisoned the tint for her entire lifetime even though she has real art for everything she actually shows in-game. Two-part fix: (1) `HasRealSprites` broadened to check `_sprIdle || _sprLoaded || _sprInFlight || _sprImpact || _sprAbility` instead of `_sprIdle` alone — Bessie now correctly evaluates `true`, no pink tint at all; (2) `SpriteWiring.WirePrefab()` now falls back to reusing `_sprLoaded` for `_sprIdle` whenever no dedicated idle art exists, so `AnimalBase.IdleSprite` (used elsewhere, e.g. HUD card fallback) isn't left null either. Confirmed via `grep` on `BessieAnimal.prefab` after a fresh setup run — `_sprIdle` now shares the same GUID as `_sprLoaded`. Neither fix affects Percy/Woolly/Ducky/Horace/Gerald/Billy (all fields null for all of them — `HasRealSprites` still correctly evaluates `false`).
- **Bessie's Ground Slam reworked into 3 distinct outcomes — 2026-07-10, user request: "bessie_trigger... causes a tremor earthquake when hitting the ground, or crashes through a structure. Also if trigger hits a robot it destroys. But if trigger is not activated apply normal damage directly to robot — bessie_impact."** `BessieAnimal.OnCollisionEnter2D()` previously only fired the `Shockwave()` AoE when triggered AND the collision was `Ground`-tagged. Now, while `_slammed` (ability active): (1) hitting the ground OR any `BlockBase` ("crashes through a structure") fires the same shockwave — broadened from Ground-only; (2) a direct hit on a `RobotEnemy` calls `robot.TakeDamage(InstantKillDamage)` (a deliberately oversized constant, `999999f` — `TakeDamage()` already clamps `Health` at 0 internally, so this is a safe guaranteed kill without adding a separate "force kill" method to `RobotEnemy`'s public API). If the ability was never triggered, neither branch runs — `RobotEnemy`'s own generic `OnCollisionEnter2D` already applies the standard `TakeDirectHitDamage()` fraction to any `AnimalBase` collision (Bessie included), and `AnimalBase.OnCollisionEnter2D` (called via `base.` at the top of the override) already shows the normal `_sprImpact` (`Bessie_Impact.png`) pose — that pre-existing default behavior IS the "trigger not activated" case, so it needed no new code. **Not visually verified** (no Play-mode access here).
- **L07's match-up header art wired — 2026-07-10, user-supplied `level7.png` in `Assets/Sprites/UI/MatchUp/`.** `SceneSetup.WireMatchUpCards()`'s `_levelHeaderSprites` array (level-indexed, 18 slots, fallback to index 0/`LevelHeader1.png` for any unwired slot) now also sets index 6 (L07, 0-based) to `level7.png`. Index 5 (L06) still has no dedicated header art and falls back to `LevelHeader1.png` like every other unwired slot — no art supplied for it yet. Confirmed via `.\Run-Unity.ps1 setup` — `level7.png.meta` now exists (Unity imported it as a real `Sprite` asset the `AssetDatabase.LoadAssetAtPath<Sprite>` call could resolve).
- **L06 "Double Barrel" added 2026-07-10** via `LevelLayoutDumper`, built AFTER L07 (user request: "build L06") — nothing requires strict numeric level ordering since `GameManager` auto-discovers every `LevelData` asset in the folder regardless of gaps. 5 robots (2 Harvester + 3 SemiHarvester, same composition as L05 "The Gauntlet" but a distinct layout) + 9 blocks (4 vertical wood, 2 horizontal wood, 2 barrels placed apart at opposite ends rather than clustered, 1 haybale). `birds[]` stays 3x Cluck even though L07 already exists and introduces Bessie — L06 sits *before* L07 in level order/progression, so keeping her out here preserves L07 as her actual in-game introduction rather than spoiling it a level early just because of build order. Coordinates compacted from the raw dump (X 3.81-7.94) using the same pivot-and-compress method as every prior level, landing the widest element at X~7.4. Confirmed via `.\Run-Unity.ps1 levels` + `setup` — `L06_DoubleBarrel.asset` now exists and `GameManager` reports "wired 7 LevelData assets" (L01-L07, all present, no gaps). **Not visually verified** (no Play-mode access here) — worth a live check, particularly the two barrels' 2x robot-explosion-strength (see the barrel-strength entry below) against 2 Harvesters in the same level for the first time.
- **L07 "Bessie's Debut" added 2026-07-10** via `LevelLayoutDumper` (dumped layout pasted from `unity/Logs/level_layout_dump.txt`) — the actual introduction of Bessie (`birds: Cluck, Cluck, Bessie`), per the plan revised earlier the same session (see the L05 rename entry below). L06 is still intentionally skipped/removed — nothing requires strict numeric level ordering since `GameManager` auto-discovers every `LevelData` asset in `Assets/ScriptableObjects/Levels/` regardless of gaps. 4 robots (1 Harvester + 3 SemiHarvester), first level to use `BlockType.Stone` plus 3 barrels — a heavier layout than any built so far. Coordinates compacted from the raw dump (X 3.615-7.867) the same pivot-and-compress way as L03-L05, landing the widest element (the Harvester) at X~7.4 to match every other level's established right edge. Confirmed via `.\Run-Unity.ps1 levels` + `setup` — `L07_BessiesDebut.asset` now exists and `GameManager` reports "wired 6 LevelData assets". Bessie's match-up card slides in from the left exactly like every other animal's, mirroring the robot's right-side entrance — `MatchUpScreen.SlideCardsIn()` was already fully generic (not species-specific), and Bessie's card art (`Bessie_Cow.png`) was wired into `_animalCardSprites` weeks before this level existed, so no `MatchUpScreen` code changes were needed for this. **Not visually verified** (no Play-mode access here).
- **Exploding barrels now hit robots harder than Haybale, not identically — 2026-07-10, user report (after L07 introduced 3 barrels in one level): "these barrels are the explosive barrels — they must explode like the haybales, only with stronger strength — these barrels also appear in level 5 and [L0]7."** Both Haybale and `ExplodingBarrelBlock` already shared the exact same `RobotEnemy.TakeExplosionDamage()` fraction (`ExplosionDamageFraction = 0.55`) despite barrels reading as the bigger, more dramatic prop of the two (barrel's `_areaDamage=55` for block-to-block chain was already bigger than Haybale's shared `WoodBlock` default of `25`, but the robot-facing amount wasn't). `TakeExplosionDamage()` gained an optional `strengthMultiplier` parameter (default `1f`); `WoodBlock` gained a matching `_explosionStrengthMultiplier` field (default `1f`, so Haybale/plain explosive Wood are unchanged) that `DamageNearby()` now passes through. `ExplodingBarrelBlock.Awake()` sets its own to `2f` — a robot caught in a barrel's blast now always dies outright (`0.55 * 2 = 1.1`, i.e. ≥100% of any robot's max HP) regardless of its current health, vs Haybale's standard 2-hits-to-kill fraction. Lives entirely in the shared C# classes, not per-level `LevelData`, so every level already using `ExplodingBarrelBlock` (L03, L04, L05, L07) gets this automatically — no per-level changes needed for "these barrels also appear in level 5 and 7." **Not visually verified** (no Play-mode access here).
- **Robots now fall from any disturbance to their support, not just full destruction — 2026-07-10, same-day follow-up: "make sure that robots fall if structure under them are disturbed, even if one haybale or wood is fractured."** `BlockBase.CheckForRobotsOnTop()` used to run only inside `DestroyBlock()`, so a block that took real damage but survived (e.g. a tanky `StoneBlock` absorbing a hit without dying) left a resting robot untouched even though its support was visibly cracking. Moved the call into `TakeDamage()` itself — fires on every hit, lethal or not — and removed the old call from `DestroyBlock()` (would otherwise double-fire on the common one-hit-kill case, since Wood/Haybale/Barrel all die on the same hit that first damages them). `RobotEnemy.MakeDynamicFromSupportLoss()` gained a matching guard (`if (_rb.bodyType == RigidbodyType2D.Dynamic) return;`) so a robot that's already falling doesn't re-stack `TakeFallDamage()` from unrelated further hits on whatever block is now beneath it. **Not visually verified** (no Play-mode access here).
- **Sixth balance pass — Direct-hit and Explosion damage raised 0.34/0.28 → 0.55 each, 2026-07-10, same-day follow-up: "I am not able to destroy all robots within the 3 animals... retune the factors so 3 birds works."** The fifth pass's fractions (tuned to stop the "one hit chain-kills everything" bug) overcorrected into "3 birds can't mathematically clear a dense level" — `RobotEnemy.DirectHitDamageFraction`/`ExplosionDamageFraction` are now both `0.55` (2 hits to kill either way, matching this project's original "2 direct hits" design language, now shared by genuine explosions too). Blast radius (40% of sprite size) and wood's `_explodesOnRobots=false` default are untouched — the fix is the per-hit fraction, not reopening the chain-reaction radius that caused the original complaint. Egg (`0.12`) and Fall (`0.15`) fractions are unchanged, still the deliberately weaker supplementary categories. **Not visually verified** — worth a live check that L04/L05 (4-5 robots, 3 birds) are now clearable.
- **Match-up screen gained a Skip button — 2026-07-10, user request: "allow the player to skip the match up scene."** `MatchUpScreen` previously had no player input at all during its cards/countdown motion sequence (removed entirely 2026-07-21 — see the class's ANIMATION REDESIGN comment). New bottom-right corner button (`_skipGO`/`_skipButton`, plain dark box + "SKIP" TMP label, no dedicated art yet — same functional-placeholder approach as the robot fallback card label), built inside the same `SafeArea` every other corner-anchored element here uses. `OnSkipClicked()` stops the running `PlaySequence()` coroutine and both audio sources (countdown beep, Cluck-falling loop), then plays the same fade-to-opaque-black beat `PlaySequence()`'s own step 6 uses before calling `GameManager.Instance.ForceStartLevel(_levelIndex)` — so skipping still cuts to solid black first rather than jump-cutting straight into gameplay. Hidden/disabled whenever the level has no `LevelData` yet (`Show()`'s "COMING SOON" branch) since there's nothing to skip into. **Not visually verified** (no Play-mode access here) — worth confirming the button doesn't overlap the countdown numeral or second robot card at the smallest supported aspect ratio.
- **Robot damage model redesigned into 4 distinct factors ("damage-factor model v2") — 2026-07-10, fifth balance pass, user report: "from level 3 the gameplay and destruction has become only one hit... we have made this too easy with the presented math."** Root cause found: the fourth-pass model (see the entry below) let `WoodBlock.DamageNearby()` fire a flat, no-falloff `RobotEnemy.TakeExplosionDamage()` hit at every robot within a 1.6-2.0u radius on ANY block's death, including plain Wood — in a packed L03+ tower, one swing chain-breaking 4-5 neighbouring planks could land 4-5 "free" 20%-HP hits on a robot with zero further player input, i.e. it died to the first hit that started the chain. Redesigned per explicit user methodology:
  1. **Plain Wood breaking no longer damages robots at all** — it's purely structural now. `WoodBlock` gained `_explodesOnRobots` (`[SerializeField] bool`, default `false`); only `HaybaleBlock.prefab` (wired via `SceneSetup.EnsureHaybaleBlockPrefab()`) and `ExplodingBarrelBlock` (sets it `true` in its own `Awake()`) still call `RobotEnemy.TakeExplosionDamage()` on death. Block-to-block chain damage (`_areaDamage` with falloff) is unaffected for every type, Wood included — only the robot-damage branch is gated.
  2. **Blast radius is no longer an arbitrary flat constant** — `WoodBlock.EffectiveBlastRadius` now computes `Mathf.Max(_col.bounds.size.x, _col.bounds.size.y) * ExplosiveForceScale` (`ExplosiveForceScale = 0.4f`, i.e. exactly the requested "40% scale of actual sprite") fresh at the moment of death, replacing the old fixed `_areaDamageRadius` field (`1.6` for Wood/Haybale, `2.0` for `ExplodingBarrelBlock`) entirely. A ~1×1 block now blasts ~0.4u instead of 1.6-2.0u — this, not the damage amount, is what actually stops one break from cascading through an entire structure.
  3. **A robot dropping from a destroyed support (`RobotEnemy.MakeDynamicFromSupportLoss`) now calls a new, separate `TakeFallDamage()`** (`FallDamageFraction = 0.15`) instead of `TakeExplosionDamage()` — "wood breaking... is only a structural feature, where the robot falls due to crumbling structure under it, and in its falling presents fractional damage," per the user's own framing. Decoupled from the real-explosion fraction so the two can be tuned independently going forward.
  4. **Eggs converted from a flat `15` damage to a new `RobotEnemy.TakeEggDamage()`** (`EggDamageFraction = 0.12`) — previously a flat number meant eggs needed 3 hits on a Basic robot (HP 35) but only 2 on a Harvester/SemiHarvester (HP 28/26), an inconsistency the flat number introduced by accident. `EggProjectile`'s flat `_damage` field is now block-only; robot hits always go through the fraction. Eggs remain deliberately the weakest category (a Cluster Bomb supplement, not a solo-kill weapon).
  5. **Direct-hit and explosion fractions both raised in difficulty**: `DirectHitDamageFraction` `0.5 → 0.34` (2 hits → ~3 to kill via animal collision alone), `ExplosionDamageFraction` `0.20 → 0.28` (~4 hits, but now only from genuine Haybale/Barrel explosions, not every plank — net rarer even though the per-hit fraction went up).
  - Robot-vs-robot contact damage (`_robotContactDamage`, impulse+floor) is unchanged — still the one damage category not expressed as a clean fraction of max HP, flagged as a candidate for the next pass if contact kills still feel inconsistent.
  - **Not visually re-verified** (no Play-mode access here) — worth a live check, especially L03-L05's now-denser robot/block layouts, to confirm robots no longer die to a single swing and that Haybale/Barrel explosions still feel impactful at the smaller blast radius.
- **Bessie's debut moved from L05 to L07 — 2026-07-10, later the same session, user decision: "I am only introducing Bessie on level 7."** L05 (added earlier the same day as "Bessie's Debut", `birds: Cluck, Cluck, Bessie`) reverted to Cluck-only (`birds: Cluck, Cluck, Cluck`, matching L01-L04) and renamed to **"The Gauntlet"** (`LevelDataGenerator.cs`'s `Make(folder, "L05_TheGauntlet", ...)`, `id: "W1_L05"`) since its old name/theme no longer applies — layout (5 robots: 2 Harvester + 3 SemiHarvester, 3 haybale + 2 barrel + 3 wood blocks) is otherwise untouched. Old `L05_BessiesDebut.asset` deleted via `DeleteStaleAsset` and `.\Run-Unity.ps1 levels` re-run to confirm the rename actually took (`L05_TheGauntlet.asset` now on disk, old file gone). L06/L07 still don't exist — Bessie's actual debut is now planned for L07 once it's hand-built via `LevelLayoutDumper`, same as every other level. **Not visually verified** (no Play-mode access here) — worth a check, especially that removing Bessie from L05's `birds[]` doesn't leave anything else in that level referencing her (nothing found in code — grepped for `Bessie`/`L05` across `unity/`, only `LevelDataGenerator.cs` itself and the now-deleted asset matched).
- **Cluck's ability-trigger pose no longer sticks for the rest of the flight — 2026-07-10, user request: "cluck should not stay in trigger but once eggs have been shot to change back to inflight, if not hit, otherwise to damage."** `AnimalBase.Update()` previously set `_sr.sprite = _sprAbility` once on tap and never reverted it. New `RevertAbilitySprite()` coroutine (started right after the ability sprite is set) waits `AbilitySpriteHoldDuration = 0.3s`, then reverts to `_sprInFlight` — but only if the bird is still flying, undestroyed, and hasn't taken a real hit in the meantime (`!_contactStarted`); a genuine impact's pose (set separately in `OnCollisionEnter2D`) always wins and is never stomped back to InFlight.
- **Eggs possibly still invisible — new candidate found and fixed, 2026-07-10, user still reporting "no eggs at all" after both the sorting-order and Animal-layer fixes.** All 5 eggs from one Cluster Bomb burst spawn at the *exact same position* simultaneously and share the same Egg layer — the very next physics step would resolve them as 5 mutually-overlapping circle colliders, and `EggProjectile.OnCollisionEnter2D` destroys itself unconditionally on ANY collision, egg-vs-egg included — plausibly wiping out the whole burst before it ever visibly separates. Added `Physics2D.IgnoreLayerCollision(Egg, Egg, true)` in `GameManager.Awake()` alongside the existing Animal-Egg ignore. **Not yet confirmed as the actual fix** — if eggs still don't appear after this, the bug is somewhere not yet found and needs live Play-mode diagnostics (Console output, breakpoints) rather than further static-analysis guesses.
- **Explosion damage to robots reduced to 20% per explosion — 2026-07-10, immediate follow-up: "also reduce the damage from explosions to 20% to robots."** `RobotEnemy.ExplosionDamageFraction` lowered `0.25 → 0.20` — 5 explosions now needed to kill a robot via chain reaction alone (was 4). Direct-hit fraction (50%, 2 hits to kill) is unchanged.
- **Explosion VFX size settled back to half-size-smaller, 2026-07-10, final correction ("ok - change the explosions back to half size smaller").** Went `2.2/1.4(wood) → 0.5 → 1.5 (misread correction) → 0.5 (reverted)` across three quick rounds the same day. `BlockBase._explodeSizeMultiplier` and `RobotEnemy.SpawnDeathExplosion()`'s multiplier are both settled at `0.5` — every death-burst now renders at half the size of whatever died. Purely a visual change, not a damage-math change.
- **Eggs still invisible after the sorting-order fix — actual root cause found 2026-07-10, third pass.** The sorting-order fix (below) didn't resolve it; the real cause was one layer deeper: `AnimalBase.Awake()` never actually set `gameObject.layer` anywhere, so every animal instance (Cluck included) sat on whatever layer its prefab defaulted to (`Default`, 0) — never the `Animal` layer (7) the project's own layer convention assumes. That meant `GameManager.Awake()`'s `Physics2D.IgnoreLayerCollision(Animal, Egg, true)` call (added for the earlier "Cluck disappears on tap" bug, see below) silently did nothing, since Cluck was never actually ON the Animal layer to begin with. Eggs (layer 10, spawning AT Cluck's own position) kept colliding with Cluck immediately, and `EggProjectile.OnCollisionEnter2D` unconditionally `Destroy()`s itself on ANY collision (including a harmless hit against its own launching bird) — so every egg was self-destructing before its first visible frame, on every level, not just L04, regardless of the sorting-order fix. Fixed by explicitly setting `gameObject.layer = LayerMask.NameToLayer("Animal");` in `AnimalBase.Awake()` — now applies to every animal subclass, and makes the earlier `IgnoreLayerCollision` call actually take effect.
- **Eggs were spawning correctly but invisible — real bug found and fixed, 2026-07-10, user report: "no eggs are firing."** Narrowed down via a diagnostic question: Cluck's sprite DID visibly swap to the ability-trigger pose on tap (confirming `TriggerAbility()`/`SpawnEggs()` ran without error), but no eggs ever appeared — pointing at rendering, not triggering. Root cause: `Egg.prefab`'s `SpriteRenderer.sortingOrder` was `4`, but this project's sorting convention (see the "Scenery sortingOrder rule" note) puts animals at `6` — since every egg spawns AT the firing Cluck's own `transform.position` (`CluckAnimal.SpawnEggs()`), Cluck's own sprite drew directly over every egg at the exact moment it appeared, hiding them completely. Fixed by raising the egg's sorting order to `7` (`SceneSetup.EnsureEggPrefab()`), guaranteeing eggs always render in front of the bird that fired them. **This alone did NOT fully fix it — see the entry above for the deeper root cause found on retest.**
- **Robot damage replaced with a precise, deterministic percentage model — 2026-07-10, fourth balance pass, user report: "still too easy, increase the damage for the robots; if directly struck by an animal lose only 50% damage — hit directly twice; if indirectly hit, 25% per explosion — 4 things must explode around it... apply the math."** Previously all robot damage was impulse-derived (varying with impact speed/angle) or a flat floor added on top — not a countable system. Replaced the two specific cases named with exact fractions of `_maxHealth`: `RobotEnemy.TakeDirectHitDamage()` (new, `DirectHitDamageFraction = 0.5`) fires on any direct collision with an `AnimalBase` (a launched bird) — always exactly half HP regardless of impact speed, so always exactly 2 direct hits to kill. `RobotEnemy.TakeExplosionDamage()` (new, `ExplosionDamageFraction = 0.25`) fires from `WoodBlock.DamageNearby()`'s robot branch (no more distance falloff for robots specifically — a robot within blast radius always takes the full 25%, since a falloff-based amount couldn't guarantee "always exactly 4 explosions") and from `MakeDynamicFromSupportLoss()` (structural collapse now counts as one explosion-tier hit, replacing the old flat `_supportLossDamage` field entirely). Block-to-block chain damage (`_areaDamage` with falloff) is unchanged — this request only covered robot damage. Robot-vs-robot contact damage (`_robotContactDamage`, impulse + floor) is also unchanged — a third, unrequested category left as-is. Eggs (`EggProjectile._damage = 15`, flat) were also left unchanged — a direct player-aimed projectile, arguably "direct," but not explicitly covered by this request, so not touched pending explicit confirmation. User expects to test and iterate further from this baseline.
- **Cluck disappearing on tap — real bug found and fixed, 2026-07-10, user report: "when the player taps on screen, cluck disappears like he's hit something."** Root cause: `CluckAnimal.SpawnEggs()` instantiates each egg AT Cluck's own `transform.position`, and the project's Physics2D layer collision matrix has every layer pair enabled by default (`ProjectSettings/DynamicsManager.asset`, all-`ff`) — so every freshly-spawned egg immediately overlapped and collided with the very bird that fired it, and `AnimalBase.OnCollisionEnter2D` treated that as a real impact (freeze + impact pose + a 0.25s timer into `DestroyAnimal()`), making Cluck vanish moments after firing. Fixed with `Physics2D.IgnoreLayerCollision(Animal, Egg, true)` called once in `GameManager.Awake()` — eggs are a weapon, not a hazard to the bird that fired them (or any other bird), so this is disabled globally for the whole session.
- **Ability trigger now gated to L04+ — same report, second half: "this should not happen in the first levels... from Level 4 when the mouse is clicked mid flight, cluck must change to trigger and shoot the eggs."** `AnimalBase.Update()`'s tap-to-trigger-ability logic was previously unconditional on any level. Added `AnimalBase.AbilityIntroLevelIndex = 3` (0-based, L04, matching `MatchUpScreen.AbilityIntroLevelIndex`) — a tap mid-flight now does nothing before that level; the existing `TriggerAbility()`/`_sprAbility` swap only fires from L04 onward. Applies to the shared base class (all 8 animals), though only Cluck is reachable before L04 in practice.
- **Difficulty overcorrected, dialed back — 2026-07-10, user report: "we have now made it too easy, robots now seem to explode on the slightest of touches... it will take some tweaking to get the correct strength ratio."** The previous round's chain-reaction widening (radius/damage) plus lowered robot HP combined into overkill. Pulled back roughly halfway rather than fully reverting: `WoodBlock` area-damage `2.0→1.6` units / `35→25` damage; `ExplodingBarrelBlock` `2.5→2.0` / `80→55`; Harvester HP `22→28`; SemiHarvester HP `20→26`; `RobotEnemy._supportLossDamage` `18→14`; `_robotContactDamage` `25→18` (still additive with real impulse, not a floor — that part of the fix stands). User has explicitly flagged this will need further iteration — treat current numbers as a rough middle ground, not final tuning.
- **Match-up screen egg burst slowed down further, and fixed a real translucency bug — 2026-07-10, two follow-up reports the same day: "the eggs still disappear too quickly, delay this some more" and "they also seem translucent, not sure if you have opacity applied."** Duration raised again `0.9s → 1.6s` (first raised `0.45s→0.9s` earlier the same day). Translucency was a real bug, not a perception issue: `AnimateBurstEgg()`'s alpha was `1f - t`, fading linearly from the very first frame — every egg was semi-transparent for its ENTIRE flight, not just at the end. Fixed by holding `alpha = 1` (fully opaque) until 70% of the flight (`FadeStartFraction = 0.7f`), then fading only over the remaining 30% — eggs now read as solid for most of their toss and fade out cleanly at the very end.
- **Settings popup re-fitted to a new Scoreboard.png aspect + game font applied — 2026-07-10, user re-supplied the art with "right sizing" and asked for titles to "fit nicely" using "the game font."** New `Scoreboard.png` is 1600×1200 (4:3, ≈1.333:1) — was ~653:382 (≈1.71:1), noticeably taller per unit width. `MainMenuController.BuildSettingsPopup()`'s box resized `1600×938 → 1333×1000` to match the new ratio undistorted while still fitting the 1920×1080 reference canvas with margin; the title + 5 headings + Music Toggle + Close button repositioned to fit the now-taller board (5 evenly-spaced rows from y=220 down, was 4 from y=190). All text now uses a new `MainMenuController.StyleAsGameFont()` helper — bold + black outline + gold-to-orange gradient, the same "game font" recipe as `HUDController.StyleAsGameNumber()` (duplicated rather than shared cross-class since that one's private) — previously the settings text was plain flat-coloured TMP. `HUDController`'s Level Complete/Failed panels also reference `Scoreboard.png` at a now-stale aspect-tuned size; updated those too for consistency (`680×398→680×510`, `620×363→620×465`) even though `preserveAspect=true` there meant they weren't visually broken, just letterboxing more than intended.
- **LevelMarker_tick.png re-supplied at the correct size — 2026-07-10, user resized it to match `LevelMarker_Locked.png`.** Both are now 256×384 (identical aspect) — confirmed via file inspection. `LevelMarker.cs`'s existing stretch-to-fill `Image` (no `preserveAspect`, reverted to this in the previous round after `preserveAspect` caused a size mismatch) now needed no further code change: stretching two identically-aspected sprites into the same box is a no-op, so both already render undistorted and identically sized.
- **Chain-reaction infinite-recursion stack overflow crash, fixed — 2026-07-10, found via a live crash stack trace (`WoodBlock.DestroyBlock → DamageNearby → BlockBase.TakeDamage → WoodBlock.DestroyBlock → ...` repeating).** Root cause: `WoodBlock.DestroyBlock()` called `DamageNearby()` *before* `base.DestroyBlock()`, but `IsDestroyed` is only set `true` inside `BlockBase.DestroyBlock()` (its second line) — so for any two blocks placed within roughly 2x each other's blast radius (common in a packed level, and now much more likely after the same-day radius increases), block A's chain reaction could hit block B, whose own chain reaction looped back to A while A's `IsDestroyed` was *still false* (A hadn't reached that line yet, still mid-call-stack inside its own `DestroyBlock()`) — `TakeDamage()`'s `IsDestroyed` guard didn't catch it, so A's already-≤0 `Health` triggered `DestroyBlock()` on A again, calling `DamageNearby()` again, looping back to B, forever, until the call stack overflowed. Fixed by swapping the order: `base.DestroyBlock()` now runs *first* (setting `IsDestroyed = true` almost immediately), then `DamageNearby()` — safe because `Destroy(gameObject)` only marks the object for deferred end-of-frame destruction, so `transform.position`/`_col` stay fully valid for `DamageNearby()`'s `Physics2D.OverlapCircleAll` query even after it runs. `ExplodingBarrelBlock` needed no separate fix since it calls `base.DestroyBlock()` (→ `WoodBlock.DestroyBlock()`) rather than duplicating the logic.
- **Landing page settings popup rebuilt into a fuller menu screen — 2026-07-10, user request: "on navigate place [Scoreboard.png] stretch across the screen; list the following headings 'Player' 'High Scores' 'Worlds' 'Saved Game' 'Music Toggle' overlaying the scoreboard backdrop."** `MainMenuController.BuildSettingsPopup()` previously used a plain small colour box (420×260) with separate MUSIC/SFX toggle buttons. Now uses `Scoreboard.png` (same asset HUDController's Level Complete/Failed panels already use) at 1600×938 (preserves its ~653:382 native aspect while filling most of the 1920×1080 reference canvas). Lists 5 headings down the parchment area: `Player`, `High Scores`, `Worlds`, `Saved Game` render as plain static text labels — **no backing feature exists for any of them yet** (no player profile system, no leaderboard, no World 2+ map, no save-slot concept beyond the existing PlayerPrefs autosave), so wire real navigation/behaviour to each once those systems exist, don't assume they're functional. `Music Toggle` is the one real control, consolidated from the old separate MUSIC/SFX buttons into a single toggle that flips both `AudioManager.MusicEnabled` and `SfxEnabled` together (same combined semantics as `HUDController`'s top-right mute button). New `MainMenuController._scoreboardSprite` field wired via `SceneSetup.EnsureMainMenu()`.
- **LevelMarker tick/locked sizing — reverted to stretch-fill, 2026-07-10, second pass (user re-reported: "they still not the same sizing, ensure they are the same size").** The previous round's `preserveAspect = true` fix avoided distorting the square `LevelMarker_tick.png`, but since it and `LevelMarker_Locked.png` (256×384, 2:3) have permanently different native aspect ratios, `preserveAspect` made them render at genuinely different SIZES instead (tick letterboxed down to 80×80 inside the 80×120 box; locked filled it exactly) — worse for consistency than the original distortion. Reverted to the default stretch-to-fill: both markers now render at identical 80×120 (`MarkerSize`), at the cost of a mild vertical stretch on the tick art. No code fix can make two different-aspect source images both undistorted AND identically sized — this trade-off was chosen because 18 markers reading as inconsistently-sized along the path is a worse visual problem than one icon being mildly stretched.
- **Difficulty rebalanced again — 2026-07-10, user report: "I play every time from level 1 and I cannot get past level 2... we need to ease the damage requirements... what we're looking for is destruction and mayhem, one hit must cause a chain reaction... player should have no birds left but should be able to finish the level."** Two changes: (1) block-to-block chain damage, previously an `ExplodingBarrelBlock`-only special case, is now unified into `WoodBlock.DamageNearby()` (renamed from `DamageNearbyRobots()`) and applies to **every** wood-family block (Wood, Haybale, Barrel) — one block dying can now set off its neighbours as well as hurt nearby robots, not just the barrel. Radius/damage also raised: `WoodBlock` `1.3→2.0` units / `20→35` damage, `ExplodingBarrelBlock` `1.8→2.5` / `60→80`. (2) Robot HP substantially lowered: Harvester `40→22`, SemiHarvester `38→20` — low enough that the widened chain reaction plus the existing support-loss/robot-contact floors reliably finishes a robot off a single well-placed hit, rather than needing several precisely-landed birds. `ExplodingBarrelBlock.cs` simplified in the process — no longer needs its own block-damage method, just bigger inherited field values.
- **Match-up screen egg burst now gated to L04+ — 2026-07-10, same-turn follow-up (user: "the eggs are firing off from level 1, this must only come in on level 4 when the power is introduced").** The cosmetic egg-burst teaser (added minutes earlier) fired on every level's match-up screen regardless of index. Added `MatchUpScreen.AbilityIntroLevelIndex = 3` (0-based, i.e. L04) and gated `SpawnEggBurst()` on `_levelIndex >= AbilityIntroLevelIndex` — the burst now only appears from L04 onward, even though the underlying gameplay ability has always been usable on any Cluck.
- **Match-up screen's Cluck fly-by now fires a cosmetic egg burst at the halfway point — 2026-07-10, user request: "when cluck is halfway across the screen fire off the eggs so the player knows there is a power."** `MatchUpScreen.CluckFlyBy()` now fires exactly once, when its arc's progress `t >= 0.5`, calling a new `SpawnEggBurst()` that fans 5 small `Egg.png` UI icons outward from Cluck's current position and fades them out over 0.45s — same forward-and-down "cannon blast" cone shape as `CluckAnimal.SpawnEggs()`'s real gameplay ability (`-45°` centre, `80°` spread), reused purely as a preview/teaser image with no physics or damage (this screen has none). `_eggSprite` threaded through `MatchUpScreen.Init()` from a new `WorldMapController` field, wired by `SceneSetup.WireMatchUpCards()` from the same `Assets/Sprites/Characters/Cluck/Egg.png` gameplay already uses.
- **L04 "Egg Practice" built 2026-07-10** via `LevelLayoutDumper` — 14 blocks (4 haybale, 3 vertical + 6 horizontal wood, 1 barrel) + 4 robots (2 Harvester, 2 SemiHarvester), the densest level yet. Introduces Cluck's Cluster Bomb ability as a GDD milestone; `birds[]` stays 3x Cluck. Layout compacted toward the leftmost wood plank (pivot X=3.8, factor 0.805) to land at the same established safe X range (3.8-7.4) as L03, following the same precaution as that level's earlier off-safe-area fix. **Not visually verified** (no Play-mode access here). L05/L06 still need to be hand-built the same way (drag content in Scene view → `LevelLayoutDumper` → paste into `LevelDataGenerator.cs`) — no layout data exists for them yet.
- **Cluck's Cluster Bomb ability reworked — 2026-07-10, user request while designing L04.** Two real bugs found and fixed: (1) the egg prefab (`SceneSetup.EnsureEggPrefab()`) never had a sprite wired at all — eggs were rendering **invisible** in-game. Now uses `Egg.png` (user-supplied) at `localScale = 2.3` (Cluck's own PPU convention makes the art's native size a tiny 0.14×0.19 world units; 2.3x lands it at a visible ~0.32×0.44 footprint, proportionate to Cluck's own ~4.92x launched scale without matching it 1:1). Collider radius bumped `0.15→0.18` to match (kept in sync between `SceneSetup.cs` and `EggProjectile.Awake()` — the latter unconditionally re-sets it on spawn, so both must agree). (2) `CluckAnimal.SpawnEggs()`'s 5-egg spread was centred on Cluck's *exact instantaneous flight-velocity angle* at the tap moment — wildly inconsistent depending on where in the arc the ability fired (near-horizontal at the apex, steep near launch/landing). Replaced with a fixed forward-and-down cone (`_coneCenterDeg = -45°`, `_spreadDeg = 80°`, i.e. fanning from −5° to −85° measured from horizontal-forward) independent of the current velocity angle — "like being fired from a cannon" per the user's framing, always canted forward+down regardless of when in the arc it's triggered. `Cluck_Trigger1.png` (shown on tap) needed **no changes** — it was already correctly auto-wired to `CluckAnimal._sprAbility` via the existing `SpriteWiring.cs` keyword pipeline (`cluck_trigger1` matches its filename) and `AnimalBase.Update()` already swaps to it on tap; confirmed via the setup log showing zero `_sprAbility` warnings for Cluck.
- **"One hit away from exploding" critical pose — added for SemiHarvesterRobot 2026-07-10 (user request: "apply [Robot_SemiHarvest_Damage.png] to the semi_harvest robot when it is one hit away from exploding"), then extended to HarvesterRobot the same day (user: "let's keep it consistent across all robots, also that are still to come").** Distinct from the pre-existing `_robotDamagedSprite` (a brief 0.15s per-hit flash, unrelated to remaining health) — new `RobotEnemy._criticalSprite` field + `_isCritical` state: `TakeDamage()` checks `Health <= _maxHealth * CriticalHealthFraction` (`0.4`, i.e. ~40% HP or less — realistically one typical hit away given current damage values: a Cluck impact ~15-20, block area-damage up to 20, a robot-vs-robot "complete hit" 25+impulse) and, the first time it's crossed, sets `_sr.sprite = _criticalSprite` *before* `FlashDamage()` starts — that coroutine captures whichever sprite is currently showing as the pose to restore to after its own brief flash, so the critical pose naturally "sticks" through every subsequent hit with zero changes needed to `FlashDamage()` itself. **Consistency mechanism:** a new `SceneSetup.WireRobotDamagedArt(so, damagedSprite)` helper sets `_robotDamagedSprite` AND `_criticalSprite` together (reusing the same art for both — visually it already reads as "damaged/sparking", fitting either purpose) and is now the only way either field gets wired, replacing the old direct one-field assignment in both `EnsureHarvesterRobotPrefab()`/`EnsureSemiHarvesterRobotPrefab()` — so any future robot prefab's damaged-art wiring automatically gets the critical pose too with no extra step to remember. `HarvesterRobot.prefab` now has `_criticalSprite` = `HarvesterRobot_Damaged.png` (its own existing flash art). The plain `Robot.prefab` (`RobotType.Basic`) still has no damaged art at all (`WireRobotSprite()` never calls the new helper) and falls back to `RobotEnemy`'s plain white-tint flash with no critical pose — wire it here the same way once dedicated art exists.
- **LevelMarker_tick.png rendering distorted, fixed — 2026-07-10, user report: "the Level_Marker_tick is not rendering correct, it must be the same size as level_marker_lock."** `LevelMarker_Locked.png` is 256×384 (2:3, exactly matching `LevelMarker.MarkerSize` 80×120), but `LevelMarker_tick.png` is a square 500×500 asset — `Image.preserveAspect` was never set (defaults false), so the square tick art was force-stretched to fill the same 2:3 box, visibly warping it. Fixed by setting `preserveAspect = true` on the marker's `Image` — both sprites now render undistorted within the same `MarkerSize` footprint (locked fills it exactly as before since its aspect already matches; tick letterboxes to fit).
- **Robot idle look-around animation now stops the instant a robot takes damage — 2026-07-10, user report: "robot positioning is also moving when struck, they should remain static."** `RobotEnemy.IdleLookAroundRoutine()` (added the previous round) only checked `IsDestroyed`/body type, so a robot mid-idle-turn that took damage (without being knocked to `Dynamic`) kept rotating through its cycle — reading as an extra, unwanted wobble layered on top of the actual hit reaction. Added a `Health >= _maxHealth` condition (`IsIdle()` local function) so the coroutine stops the moment any damage is taken and resets rotation to neutral — a damaged robot now stays visually still (alert/braced) for the rest of its life rather than continuing to casually glance around.
- **Environmental block destruction now damages nearby robots — new fundamental gameplay rule, 2026-07-10, user report: "levels are too difficult, not able to destroy all robots within the three chickens... exploding wood, haybale, barrels should deduct damage points from the robots."** Root cause of the difficulty spike: only `ExplodingBarrelBlock` had ever damaged anything beyond itself on death — a plain wood plank or haybale breaking apart did literally nothing to a robot standing right next to it, so the only ways to hurt a robot were a direct Cluck hit or the flat support-loss/robot-contact floors, nowhere near enough to clear a level's robots within 3 birds. Generalised into `WoodBlock` itself (shared by both Wood and Haybale — same C# class, only the wired art differs) via a new `DamageNearbyRobots()` called from `DestroyBlock()`: `Physics2D.OverlapCircleAll` within `_areaDamageRadius` (default `1.3`), damage falls off linearly with distance, capped at `_areaDamage` (default `20`) at point-blank. `ExplodingBarrelBlock` now overrides these two fields to bigger numbers (`1.8` radius / `60` damage, unchanged from its original standalone implementation) and layers its own `DamageNearbyBlocks()` on top for the block-vs-block chain reaction (barrel-specific — plain wood/haybale only ever damages robots, not neighbouring blocks). Lives entirely in code, not per-level `LevelData`, so it automatically covers every level, current and future.
- **Robot-vs-robot contact damage raised and made additive, not a floor — same report, "robots that fall from structure should also damage robots, even make them explode — complete hits."** `RobotEnemy._supportLossDamage` raised `12→18`, `_robotContactDamage` raised `15→25`. More significantly, `OnCollisionEnter2D`'s robot-vs-robot branch used to take `Mathf.Max(realImpulseDamage, floorDamage)` — a hard collision never dealt more than whichever was bigger. Changed to `realImpulseDamage + floorDamage` (additive) so a genuinely forceful knock-together can stack past a robot's remaining HP and finish it outright, rather than always capping at a fixed minimum regardless of how hard the actual hit was.
- **Level-up star enlarged 130x130 -> 260x260 — 2026-07-10, user request ("enlarge the level-up star by 2", shown a world-map/LevelMarker screenshot for size reference).** `HUDController`'s Level Complete panel level-up star button (`LevelUpStarBtn`, `Levelup.png`) doubled. Didn't match `LevelMarker`'s exact box dimensions (80×120) — `Levelup.png` is a square 512×512 asset, so forcing a 2:3 box would letterbox or distort it; a plain 2x of the previous square size keeps it undistorted while making it comparably prominent. Not visually re-verified (no Play-mode access here) — worth checking it doesn't overlap the score text or panel edge at the new size.
- **Wood explosion/damage burst resized down — 2026-07-10, user report: "re-size the wood_damage smaller, its too oversized."** `BlockBase.SpawnExplosion()`'s death-burst size (`block footprint × multiplier`) used a shared hardcoded `2.2` for every block type, originally tuned against `Haybail_Damaged.png` (~92%×83% own-canvas content fill — some transparent padding). `WoodDebris.png` (wired to `WoodBlock._sprExplode` last round) is full-bleed (100%×100% fill, no padding at all), so the identical `2.2×` multiplier read as visibly bigger/more dominant than haybale's burst at the same nominal block size. Fixed by extracting the multiplier into a new `_explodeSizeMultiplier` field (default `2.2`, unchanged for every other block type) and lowering it to `1.4` specifically for `WoodBlock` via a new `SceneSetup.SetFloatField()` helper (generic prefab float-field override, same `LoadPrefabContents`/`SaveAsPrefabAsset` shape as the existing sprite-wiring helpers).
- **Robot-falls-when-support-destroyed detection made maximally generous — 2026-07-10, user report (after the previous round's fix): "the robot on top of structure is still not falling when structure is destroyed."** Re-derived the L03 geometry by hand against `BlockBase.CheckForRobotsOnTop()`'s box-overlap math and couldn't find a remaining logic bug — the 2.5-unit-tall detection column from the prior fix should mathematically already reach the Harvester's real 0.6×0.9 world collider. Rather than keep guessing a precise height against unverifiable live geometry, widened the detection column further to `8` units (`checkHeight`, comfortably taller than this level's entire play area) so any robot anywhere above a destroyed block's own X footprint falls, removing the need to tune this per robot scale/position ever again. Still X-limited to 90% of the block's own width, so it can't reach a robot resting on an unrelated block elsewhere in the level. **Not confirmed fixed** — if this still doesn't work, the bug is likely somewhere other than the detection box (e.g. robot layer/collider setup, or the block-destroy call path) and needs a live Play-mode session (Console output, Scene view with Gizmos, or a breakpoint) to pin down rather than further blind static-analysis fixes.
- **Robot hit sound no longer overlaps the death sound — 2026-07-10, user report: "the sound effect we attached for one hit on the robot should not play when the robot explodes, the two sounds are interfering with each other."** `RobotEnemy.TakeDamage()` played the hit sound (`Robot_Hit.mp3`/procedural fallback) unconditionally on every hit, including the killing blow, which then also triggered `Die()` → `DeathSequence()`'s own death sound — both firing on top of each other on the same frame. Fixed by computing `killed = Health <= 0f` up front and skipping the hit-sound/`FlashDamage()` reaction entirely when the hit is lethal; a lethal hit now goes straight to the death sound only.
- **Real collider-vs-visual mismatch bug found and fixed — 2026-07-10, root cause of the "wood not active when hit" / "wood moved off original positioning" reports.** Three rounds ago, `BlockBase.Initialise()` was changed to scale `transform.localScale` relative to the chosen sprite's *native* bounds instead of literally `(width, height)`, fixing a real sprite-squashing bug — but `BoxCollider2D.size` defaults to `(1,1)` local and shares that same `transform.localScale`, so the collider silently inherited the same correction it was never meant to need. For any non-1:1-native sprite (e.g. `Plank_Horizontal.png`, native 1.0×0.492) this desyncs the actual hitbox from the requested/visible footprint — a `1.3×0.64` wood block ended up with a ~1.3×1.3 collider, nearly double the visible sprite's height, likely explaining hits feeling misaligned/unregistered. Fixed by re-deriving `BoxCollider2D.size` = the sprite's native bounds every `Initialise()` call — algebraically cancels the scale correction back out, landing the world-space collider exactly on the requested `(width, height)` regardless of the chosen sprite's own aspect. Applies to every block type automatically, not just wood.
- **L02/L03 wood blocks reverted to plain 1×1 squares at their original coordinates — 2026-07-10, user report: "the wood is still not rendering as desired... wood has moved off original positioning as well, for all levels."** The previous round's fix (retagging 2 blocks per level to `WoodArtVariant.Horizontal` at custom `1.3×0.64` dimensions) didn't land — reverted entirely back to the original `1×1` squares at their original Y. Visibility is now addressed at the *source* instead: `WoodBlock._sprNormal` (the default/`Auto` art for square-aspect blocks) is re-pointed from `2D_Block_Wood_Flat.png` (which only fills 86%×21% of its own canvas — a near-invisible sliver, root cause of "not rendering as desired") to `Plank_Horizontal.png` (90%×50% fill). Its native aspect (1.0:0.492) means a plain `1×1` request now stretches it ~2x vertically via the existing native-bounds scaling — a mild, deliberately-accepted trade-off (reads as a slightly thicker plank) in exchange for actually being visible and not requiring any per-level coordinate changes. Combined with the collider fix above, every wood block's hitbox now also correctly matches its visible footprint.
- **Robots idle-animate ("look alive"), redesigned same day — 2026-07-10.** First pass (user request: "make the robots look alive... on the spot movement look left and right") used a continuous sinusoidal Z-rotation in `Update()`; user-reported it just read as "slightly shake," not deliberately alive. Redesigned into a coroutine-driven burst cycle (`IdleLookAroundRoutine()`): hold still for a random 1.2–3.0s pause, quick ~0.18s turn to one side (±14°, random per cycle), hold 0.35s, turn to the other side, hold, return to centre, repeat — reads as an active glance rather than a jitter. No new art needed either way. Stops itself the instant the robot goes `Dynamic` (support-loss fall or a hard hit) or is destroyed, so it never fights the real physics-driven tumble. Rotates the same transform the `BoxCollider2D` lives on rather than adding a separate visual-only child — at this angle/scale the collider wobble is gameplay-imperceptible, not worth the added hierarchy complexity for a handful of on-screen robots.
- **Robot hit sound replaced with Robot_Hit.mp3 — 2026-07-10, user request: "remove all current sound effects attached [to a robot taking initial damage] and wire in this sound."** `RobotEnemy.TakeDamage()` previously always played the procedural `AudioManager.Sound.RobotHit` DSP ping (a synthesized clip, `AudioManager.BuildRobotHit()`) on every hit. New `_hitSoundOverride` field (same shape as the existing `_deathSoundOverride`) — when wired, replaces that ping entirely rather than layering on top of it; wired on all 3 robot prefabs (Robot/HarvesterRobot/SemiHarvesterRobot) from `Assets/Audio/Robot_Hit.mp3` via the shared `WireRobotDeathFx()` helper (renamed scope in practice, still wires both death AND hit FX for all three prefabs in one pass). Falls back to the old procedural ping only if the field is somehow left unwired.
- **Wood/barrel blocks were far too tanky compared to haybale — 2026-07-10, user report: "the wood destruction should have the difficulty settings of the haybale... currently it is not damaging (this goes for any plank or wood)."** Design intent, per the user directly: early levels should be easy to clear structurally (haybale/wood/barrel all die in one hit) with the actual difficulty coming from robot HP plus the fall/contact damage mechanics (see the "guaranteed damage rules" entry below). Haybale already achieved its one-hit kill via an explicit `hp: 10f` override on every `B(...)` call in `LevelDataGenerator.cs` — but `WoodBlock`/`ExplodingBarrelBlock` never got a matching override anywhere, so they fell back to their (untuned) class defaults run through `BlockBase.Initialise()`'s area-scaling formula (`baseMaxHealth × area/StdArea`, `StdArea=0.48`): `WoodBlock`'s old `baseMaxHealth=80` computed to ~167 HP for a standard 1×1 block, `ExplodingBarrelBlock`'s old `40` to ~83 — both absorbing many hits before dying, reading as "not damaging." Fixed at the class-default level rather than adding per-instance overrides to every level (`WoodBlock`/`ExplodingBarrelBlock` `baseMaxHealth` both lowered to `5`, landing ~10.4 effective HP for a 1×1 block — matches Haybale's flat 10 closely) so the fix automatically applies to every existing and future level with no `LevelData` changes needed, satisfying "this goes for any plank or wood."
- **L03 barrel repositioned away from the second SemiHarvester — 2026-07-10, same-day follow-up to the layout-compaction fix below: "move the barrel slightly away from the Semi_harvester, they are now on top of each other."** The X-compression that fixed the barrel rendering off-screen also (over-)tightened the gap between the barrel (X=6.998) and the second SemiHarvester (X=6.507) to under half a unit. Nudged the barrel to X=7.4 — opens the gap without reintroducing the original off-safe-area problem. Not visually re-verified (no Play-mode access here).
- **Block destruction made fully independent per-block — 2026-07-10, user report: "the wood structure should not fall off screen when the haybale is struck, it should all be independent in its destruction."** Root cause: `BlockBase.TakeDamage()` called a level-wide `WakeAllStaticBlocks()` sweep on ANY block's first hit — it woke every *other* non-`_stayKinematic` Static block in the whole level too (`FindObjectsByType<BlockBase>()`), not just the one actually struck. `HaybaleBlock` had already been given `_stayKinematic=true` to opt itself out of this (2026-07-26, "hitting one haybale woke all four"), but `WoodBlock`/`ExplodingBarrelBlock` never got the same protection — hitting a haybale in L03 wobbled/collapsed the completely unrelated wood tower elsewhere in the level. Fixed at the root instead of patching another block type: `WakeAllStaticBlocks()` is deleted entirely; `TakeDamage()` now only ever flips *its own* Rigidbody2D to Dynamic (still gated on `!_stayKinematic`, so `HaybaleBlock` is unaffected either way). Every block's fall/collapse behaviour is now purely local — matches the same principle already applied to `RobotEnemy.MakeDynamicFromSupportLoss()`'s targeted per-robot check.
- **Robot "tumble on support loss" mechanic wasn't reaching L03's Harvester — 2026-07-10, user report: "L02 works, L03 doesn't."** `BlockBase.CheckForRobotsOnTop()`'s overlap-box (added 2026-07-09) was only 0.3 units tall, sitting just above a block's own top surface. `RobotEnemy`'s actual physics collider is always re-derived to a small fixed 0.6×0.9 world hitbox regardless of visual `transform.localScale` (see `LevelLoader.SpawnRobot`), so a heavily up-scaled robot (L03's Harvester: scale ~5.4×7.7, vs L02's ~4.3×5.6) needs its `transform.position` placed much higher to make the oversized sprite visually "stand" on the pile — the tiny collider ends up floating well above where the old thin strip could ever reach, even though it reads as clearly resting on the structure by eye. Fixed by widening the box to 2.5 units tall (still X-limited to 90% of the block's own width, so it can't reach a robot resting on an unrelated block elsewhere) — generously covers any plausible robot scale without per-level tuning.
- **L03 "The Tower" layout compacted — 2026-07-10, user report: "move all sprites closer together, the barrel is rendering off the safe area."** Original dump spanned X 4.16–8.54 (4.38 units) — well past L01/L02's established safe range (L02's widest content only reaches X=6.72) — so the barrel at X=8.54 rendered past the camera's visible/safe-area edge. Every block/robot X in `LevelDataGenerator.cs`'s `L03_TheTower` is now uniformly compressed toward the leftmost haybale (pivot X=4.16, factor 0.648), landing the whole structure at X 4.16–7.0 to match L02's known-good footprint; Y values untouched. A pure linear compression only shrinks gaps, never widens them, so any existing physical relationships (robots resting near/on the wood stack) got tighter, not broken. **Computed from recorded coordinates, not visually re-verified in the Editor** (no Play-mode access here) — worth a live check.
- **WoodDebris.png (broken-splinter burst art, user-supplied 2026-07-10) wired to WoodBlock's `_sprDamaged`/`_sprExplode`.** Previously neither was set, so every wood plank died via the generic 4-fragment fade — now matches the dedicated-art pattern `HaybaleBlock`/`ExplodingBarrelBlock` already use (brief hit-reaction flash, then the same art again as the death burst).
- **Exploding barrel block added — 2026-07-10, user request to "gradually introduce the exploding barrel," triggered by L03's WoodenBarrel-sourced block rendering as a plain plank.** `WoodenBarrel.png` had existed unused in `Assets/Sprites/Environment/World1Props/` — no block type ever referenced it. New `BlockType.Barrel` (`LevelData.cs`) + `ExplodingBarrelBlock : WoodBlock` (`Scripts/Blocks/ExplodingBarrelBlock.cs`) — lower HP (40, vs WoodBlock's 80, "meant to be popped") and, on death, an `Explode()` pass (`Physics2D.OverlapCircleAll`, 1.8u radius, damage falls off linearly with distance to 0 at the edge) that damages every other `BlockBase`/`RobotEnemy` in range before the normal destroy sequence runs — lets one barrel chain-react a neighbouring barrel, matching the classic destruction-game trope. Reuses `RobotEnemy`'s own death VFX/SFX (`Explosion.png` + `Explosion_Robot.mp3`) via the existing `_sprExplode`/`_destroyClipOverride` fields on `BlockBase`, rather than new dedicated art. Prefab created by `SceneSetup.EnsureExplodingBarrelPrefab()` (mirrors `EnsureHaybaleBlockPrefab()`'s shape) and wired into `LevelLoader._barrelPrefab`; `LevelLoader.SpawnBlock()` picks it for `BlockType.Barrel`. L03's barrel-art block switched from `BlockType.Wood` to `BlockType.Barrel` in `LevelDataGenerator.cs`. **Bootstrapping quirk, not a bug:** on the very first `Wire Scene References` run after adding this, `WireLevelLoader()` executes before `EnsureExplodingBarrelPrefab()` in `WireAll()`'s call order, so it logs `Prefab 'ExplodingBarrelBlock' not found` and leaves `_barrelPrefab` unwired — same thing happened historically for `HaybaleBlock.prefab` the first time that was added. Self-corrects on a second run once the prefab exists on disk; only matters once, not worth reordering `WireAll()` over.
- **Wood block ART (not just size) was wrong — 2026-07-10, second fix in the same investigation as the earlier block-sizing fix.** `BlockBase.Initialise()`'s aspect-ratio art-selection heuristic (`aspect < 0.72 → vertical, > 1.5 → horizontal, else → normal/flat`) can't distinguish a visually-vertical plank image from a visually-flat one when both happen to have a near-square *measured footprint* — `Plank_2DShork.png` is unambiguously a vertical plank bundle by eye, but its 250×266px canvas is close enough to 1:1 that it always landed in the "flat" bucket, so L03 rendered every wood block with the same flat `2D_Block_Wood_Flat.png` art regardless of which sprite was actually placed in the Scene view. Fixed with a new explicit `WoodArtVariant` enum (`Auto`/`Flat`/`Horizontal`/`Vertical`) on `LevelData.BlockSpawnData` — `Auto` (the default, used by every pre-existing hand-authored `B(...)` call) preserves the old aspect-guessing behaviour unchanged; `LevelLayoutDumper.cs`'s raw-sprite path now sets it explicitly via a new `InferWoodArtVariant()` keyword lookup (checks the actual design-time sprite's filename — `"vertical"`/`"shork"` → Vertical, `"horizontal"` → Horizontal, `"flat"` → Flat, anything else → Auto) instead of leaving orientation to the aspect guess. L03's 4 `Plank_2DShork` blocks are now tagged `WoodArtVariant.Vertical` in `LevelDataGenerator.cs`. Also fixed in the same pass: the dumper's barrel-keyword check (`"barrel"` in the sprite name) now runs *before* the wood/plank check, since `"WoodenBarrel"` contains `"wood"` as a substring and would otherwise always misclassify as `BlockType.Wood` first (same class of ordering bug as the existing SemiHarvester-before-Harvester fix).
- **World map/match-up bugs from earlier today confirmed fixed by the previous round of changes — 2026-07-10, user-verified ("it all rendered properly now").** No root cause was ever pinned down for the blank match-up screen / misplaced PLAY button (see the still-present entry below, kept for historical debugging context) — whatever fixed it was incidental to the wood-block-sizing fixes in the same `Wire Scene References` pass, not a targeted fix. Leaving the diagnostic notes below in case it recurs.
- **Landing page SETTINGS button restored — 2026-07-10, user request (re-reverses the 2026-07-07 removal).** `MainMenuController`'s gear icon + Music/SFX toggle popup (`BuildSettingsPopup`, `OnSettingsClicked`, `MakeToggleButton`/`MakeToggleLabel`) are back, restored verbatim from the pre-removal version (commit `b5040ad`'s parent) — same bottom-right corner-anchored gear icon, same centred popup with MUSIC/SFX/CLOSE toggle buttons sharing `AudioManager.MusicEnabled`/`SfxEnabled` state with the in-game top-right Mute button, so the two can't disagree. `Btn_settings.png` had been sitting unused in `Assets/Sprites/UI/Icon/` the whole time the button was gone — just needed re-wiring in `SceneSetup.EnsureMainMenu()`.
- **Robot fall/damage-on-support-loss and robot-vs-robot contact damage made into guaranteed "fundamental rules" — 2026-07-10, user request ("ensure this applies to all levels").** The fall-when-support-destroyed mechanic already existed (`BlockBase.CheckForRobotsOnTop()` → `RobotEnemy.MakeDynamicFromSupportLoss()`, added 2026-07-09) but only switched the robot to a falling `Dynamic` body — actual damage still depended on whatever impulse the subsequent landing/collision happened to produce via the normal `OnCollisionEnter2D` formula, which could be near-zero for a gentle drop onto the next block down (reads as "nothing happened"). Two new flat/guaranteed damage floors added on top of (not replacing) that existing impulse-based formula: `MakeDynamicFromSupportLoss()` now also calls `TakeDamage(_supportLossDamage)` (default 12) immediately, so structural collapse itself always hurts the robot regardless of how the fall physics plays out; and `OnCollisionEnter2D` now checks whether the collision partner has a `RobotEnemy` component and, if so, applies `Mathf.Max(impulse * multiplier, _robotContactDamage)` (default 15) instead of requiring the normal `_minDamageImpulse` threshold — any robot-vs-robot touch always counts as a hit, not just a hard one. Both live directly on the shared `RobotEnemy` component, so this is automatically in effect for every level/robot type with no per-level `LevelData` changes needed, satisfying "applied to all levels." Not yet visually verified in the Editor (no Play-mode access here) — worth a live check, particularly whether `_supportLossDamage`/`_robotContactDamage`'s default values feel right against real robot HP (Basic=35, Harvester=40) once seen in motion.
- **Cluck_falling.mp3 now plays under the match-up screen's Cluck fly-by — 2026-07-10, user request ("time with the countdown").** `MatchUpScreen.CluckFlyBy()` already timed Cluck's arc across the screen to exactly match the 3/2/1/READY countdown duration (`CountdownClipLength`, added 2026-07-09) but had no accompanying sound beyond the one-shot launch SFX at liftoff. Mirrors `AudioManager.PlayFalling()`'s existing gameplay approach (same clip, looped for the duration of flight) via a new dedicated `_cluckFallingAudioSrc` (can't reuse `_countdownAudioSrc` — `Countdown.mp3` is already playing concurrently on that source) — starts looping the instant the fly-by begins, fades out over the final 0.3s (`CluckFallingFadeDuration`) so it doesn't cut off abruptly right as "READY!" lands, matching the polish of the existing launch-SFX/countdown-beep timing already in this method. Threaded through `MatchUpScreen.Init()` (not a direct `[SerializeField]` on `MatchUpScreen` itself — same nested-runtime-component wiring gap as every other art/audio field on this class, see the existing class comment) from a new `WorldMapController._cluckFallingClip` field, wired by `SceneSetup.WireMatchUpCards()` from the same `Assets/Audio/Cluck_falling.mp3` file `AudioManager` already uses for actual gameplay.
- **World map (Sunrise Meadows) PLAY button and match-up screen reported broken 2026-07-10 — user says this regressed from a previously-working state; root cause NOT YET FOUND.** User report: tapping the bottom-left PLAY button on the world map opens a completely blank match-up screen (no console errors, no exceptions logged), and separately the PLAY button itself renders in the wrong place, visually overlapping the dense level-marker cluster instead of sitting in its intended bottom-left safe-area corner (screenshot on file). Investigated `WorldMapController.cs`/`MatchUpScreen.cs`/`GameManager.cs` in full — nothing in that code path was touched by the same-day L03 addition (`GameManager._levels` sizing is fully dynamic, not hardcoded to a level count), and `MatchUpScreen`'s background Image has a solid-color procedural fallback even with zero art wired, so a truly blank/invisible panel with no exceptions doesn't match any bug found by static reading alone. No live Editor/Play-mode access in this environment to inspect the Hierarchy or grab a definitive repro — needs a live session: check whether the `MatchUpScreen` GameObject and its child Images are actually active/enabled when this happens, and whether the PLAY button's actual screen-space rect matches its intended bottom-left anchor. Worth checking first: `WorldMapController.BuildUI()`'s `SafeArea` normalization (`ApplySafeArea`, ~line 433) against the actual device/Simulator aspect ratio in use, since that's the piece most sensitive to non-16:9 aspect ratios and wasn't otherwise touched.
- **Wood block art/sizing bug fixed 2026-07-10 (affected L02 "Harvest Yard" too, not just the new L03) — found while investigating a "planks render smaller than my scene layout" report.** Two compounding bugs: (1) `SceneSetup.WireBlockSprites()` pointed `WoodBlock._sprNormal` at `Block_Wood_Normal.png`, a file that never existed on disk — every roughly-square-aspect Wood block (which was *every* Wood block, see next point) silently fell back to a plain colour square instead of real plank art; fixed to point at the existing `2D_Block_Wood_Flat.png`. (2) `BlockBase.Initialise(width, height)` set `transform.localScale` directly to `(width, height)`, which only produces the intended world-space footprint if the chosen sprite happens to be exactly 1×1 Unity units at scale 1 — true only along the X axis under `WireBlockPrefab`'s "PPU = texture width" convention (documented there), so any non-square art (e.g. `Plank_Horizontal.png`, 250×123px) rendered squashed on the Y axis. Fixed by scaling relative to the chosen sprite's actual `Sprite.bounds.size`. (3) `LevelLayoutDumper.cs` compounded this at the data-capture layer: it recorded raw `transform.localScale` of the design-time object (always `(1,1)`, since nobody had touched the scale gizmo) instead of the sprite's real rendered world-space footprint — fixed to use `SpriteRenderer.bounds.size` for both the prefab-instance and raw-sprite (`LevelScratch`) dump paths, so future dumps capture true aspect/size automatically. L03's 4 blocks sourced from `Plank_2DShork` art (see `LevelDataGenerator.cs`'s `L03_TheTower` comment) were hand-corrected to that sprite's true `1.0 × 1.064` footprint; L02's existing `(1,1)` values needed no numeric change since they happened to already use the (square) `2D_Block_Wood_Flat` art. **Not yet visually re-verified in the Editor** (no Play-mode access here) — worth a live check next session. Separately, one L03 block sourced from `WoodenBarrel` art will still render as a generic wood plank, not a barrel — `WoodBlock` has no barrel-specific art slot; flagged, not fixed (no decision yet on whether it's worth a dedicated block art variant for one decorative-looking prop).
- **L03 "The Tower" added 2026-07-10** via the `LevelLayoutDumper` workflow (same as L02) — 10 blocks (3 haybale, 6 wood, 1 barrel-art wood) + 3 robots (1 Harvester, 2 SemiHarvester). Still `birds: 3x Cluck` — Bessie and Cluck's own ability (cluster-bomb tap) are intentionally **not** unlocked until L05, per user decision this session. Match-up screen needed zero code changes: its existing "second distinct `RobotType`" detection (`MatchUpScreen.cs` ~line 189) already generically shows a second robot card whenever a level's `robots[]` contains more than one type, so L03 automatically gets both the Harvester and SemiHarvester cards, same as L02.
- **Cluck_Celebration.mp4 re-exported with its sky backdrop baked in, chroma-key shader now skipped for it — 2026-07-08.** Per the "future celebration clips will skip green screen entirely" decision recorded 2026-07-07 (see the ghosting entry below), the user replaced `Assets/Video/Cluck_Celebration.mp4` in place with a version whose backdrop is already composited into the clip at generation time rather than shot on a `#00B140` green screen. `VideoChromaKey.Play(clip, audioClip, plainRender)` gained a third `plainRender` bool param — when true it skips assigning the `FarmFury/ChromaKeyVideo` material to the `RawImage` (renders the clip's own pixels as-is) and suppresses the separate `_background` sky layer (would otherwise sit uselessly behind an already-opaque frame); `Stop()` resets both so a later chroma-keyed clip (e.g. Robot's still-green-screened Level Failed taunt) isn't left on the plain material. `LevelCompleteManager` gained a matching `_celebrationPlainRender` bool array (indexed by `AnimalType`, same shape as `_celebrationClips`/`_celebrationAudioClips`, no Cluck-fallback — an unset slot just defaults to `false`/chroma-keyed) and passes `GetPlainRender(lastAnimal)` through to `VideoChromaKey.Play()`. `SceneSetup.EnsureLevelCompleteManager()` wires Cluck's slot to `true` via a new `WireBoolArrayElement` helper (mirrors `WireArrayElement<T>`'s shape but for a plain bool value, not an asset load). `LevelFailedManager`/Robot's taunt clip are untouched — still chroma-keyed. Not yet visually verified in the Editor (no Play-mode access in this environment); worth a live check next session.
- **HUD bird-queue cards were rendering as flat tinted squares instead of character art — 2026-07-07, fixed.** `SceneSetup.EnsureHUD()` only ever searched `Assets/Sprites/UI/Cards/` for card art (populated from `assets/FarmCards/*.png` by `EditorAutoSetup` — but `assets/` is currently deleted from disk, see below), so that folder was empty and `HUDController._cardSprites[]` stayed null for every animal, falling back to `HUDController`'s plain tinted-square placeholder. `Cluck_Chicken.png`/`Bessie_Cow.png` already existed at `Assets/Sprites/UI/MatchUp/` (used for the pre-match screen) with filenames matching the same keyword-lookup logic, so `EnsureHUD()` now searches that folder too as a fallback (`FindSpriteByKeyword` helper, checks `Cards/` first). Only Cluck/Bessie resolve today (2/8) since no MatchUp art exists yet for the other 6 animals — expected, matches which animals are actually used in World 1.
- **Audio/video source assets reorganized into dedicated folders — 2026-07-07, user-driven move.** All sound files now live in `unity/Assets/Audio/` (flat, no subfolders) and both celebration/taunt clips (`Cluck_Celebration.mp4`, `Robot_Celebration.mp4`) now live in `unity/Assets/Video/` — moved out of the old `Assets/Sprites/UI/Video_Sound/` (now deleted, was gitignored so nothing to clean up in git) and, briefly, out of `Assets/Audio` too (the .mp4s landed there first, then got their own folder). All `SceneSetup.cs` `AssetDatabase.LoadAssetAtPath` path strings were updated to match (`EnsureAudioManager`, `EnsureLevelCompleteManager`, `EnsureLevelFailedManager`, `EnsureCelebrationVideoBackground`, `WireMatchUpCards`). Scene references resolve by GUID, not path, so anything moved with its `.meta` file intact (confirmed for the audio moves) needed no scene changes; the two video files landed at `Assets/Video` without their original `.meta` (fresh copies), so Unity assigned them new GUIDs on next import — re-running **FarmFury → Wire Scene References** repointed `Game.unity` at the new GUIDs automatically (this is exactly what that command is for). If more audio/video assets move in the future, grep `SceneSetup.cs` for the old path and re-run Wire Scene References.
- **Countdown.mp3 added and synced to the MatchUpScreen countdown — 2026-07-07.** `MatchUpScreen.PlaySequence()`'s 3/2/1/READY beat previously ran on an arbitrary fixed 2s-per-numeral pace with no audio (see the AUDIO NOTE that used to be at the top of the file). Countdown.mp3's actual beep timing was measured by decoding it (ffmpeg) and analysing the energy envelope in Python: four beeps — three short taps at 0.02s/1.02s/2.02s (1.0s apart, for 3/2/1) then one longer confirmation tone at 3.02s running to the clip's end (~4.05s total, for READY). `MatchUpScreen` now plays the clip once via its own `AudioSource` right as "3" appears, and each numeral's pop-in/hold/pop-out (`CountdownBeat`) is paced to total exactly one beep interval so the next numeral lands on the next beep — constants documented at the top of the timing-constants block in `MatchUpScreen.cs`. The clip is threaded through `MatchUpScreen.Init()` (not a `[SerializeField]` on `MatchUpScreen` itself — see the existing class comment on why nested runtime-built components can't rely on Wire Scene References re-firing their `Awake()`) from a new `WorldMapController._countdownClip` field, wired by `SceneSetup.WireMatchUpCards()` from `Assets/Audio/Countdown.mp3` via a new generic `WireAudioClip(SerializedObject, ...)` helper (mirrors `WireSprite`'s shape).
- **Cluck's Level Complete celebration ghosting — 2026-07-07, resolved (mostly) via a multi-part fix; future celebration clips will skip green screen entirely.** Symptom evolved across the debugging session: invisible → showed the previous clip's stale frame → fully transparent → translucent "ghost" → solid but with a white edge fringe. Real, distinct bugs found and fixed along the way, all in the shared `VideoChromaKey`/`FarmFury/ChromaKeyVideo` pipeline (still shared with the working Level Failed robot taunt):
  1. `VideoChromaKey.Play()` used to activate the `RawImage` immediately, showing whatever the *previous* clip left in the shared `RenderTexture` until the new clip's first frame actually arrived (or forever, if it never did) — fixed by gating visibility on a new `frameReady` callback (`HandleFrameReady`), reset per `Play()`.
  2. `LevelCompleteManager`/`LevelFailedManager` started their fixed on-screen hold countdown the instant `Play()` was called, racing async decode — fixed with `VideoChromaKey.WaitUntilFirstFrame(timeout)`, awaited before the hold timer starts.
  3. **Root cause of the ghosting itself:** the project uses Linear colour space (`ProjectSettings.asset` `m_ActiveColorSpace: 1`), but `VideoChromaKey.HandlePrepared()` created the video's `RenderTexture` without specifying read/write mode, so Unity auto-linearized the sampled (already gamma-space) decoded video bytes against the shader's still-gamma-space hardcoded `_KeyColor` — a systematic colour-space mismatch on every pixel. Fixed by explicitly passing `RenderTextureReadWrite.Linear` (Unity's confusing naming for "skip the sRGB conversion"). This alone didn't fully solve it because Cluck's saturated orange/yellow palette sits only marginally above the shader's opaque threshold to begin with (measured directly against decoded frames: ~0.49-0.57 shader-distance vs. a 0.45 edge at the original tolerance) — `tolerance` was lowered `0.3 → 0.25 → 0.20` (both the C# default *and* explicitly re-synced in `SceneSetup.EnsureCelebrationVideoBackground()` every Wire Scene References pass, since it's a public non-serialize-only field vulnerable to the stale-value trap below) to give the character a safer margin.
  4. Lowering tolerance fixed the bulk of his body but left thin features (legs, comb, wingtips) still translucent — likely yuv420p chroma subsampling (half-resolution colour info, baked into Kling's own export, unfixable by re-encoding) diluting anything narrower than a couple of luma pixels toward the background colour.
  5. Once solid, a white "shiny lining" appeared around his silhouette: the shader's saturation gate (added earlier to stop dark shadow shading from being wrongly keyed transparent) was also force-protecting bright, near-white anti-aliased edge-blend pixels. Fixed in `ChromaKeyVideo.shader` by adding a brightness check (`brightWashout`) so only *dark* desaturated pixels get full protection — bright ones are now treated as if fully saturated and subject to the real distance-to-key check.
  - **Decision, not a bug fix:** rather than keep tuning shader parameters against an imperfect (non-uniform-lit) AI-generated green screen, future celebration/taunt clips for the remaining 6 animals (World 2-6) will have their actual in-game backdrop composited into the Kling AI generation itself, skipping chroma-keying entirely — see `docs/HISTORY.md`-style reasoning saved to project memory. When that clip arrives, `VideoChromaKey`/`LevelCompleteManager` need a per-clip flag to render plainly (no shader) for clips with baked-in backgrounds, while Robot's still-green-screened clip keeps the existing shader path.
  - Diagnostic scaffolding added to `VideoChromaKey` in the process (kept, not stripped): `errorReceived` logging (`HandleError`), `Prepared`/`Play() called`/`First frame ready` breadcrumbs in `HandlePrepared`/`HandleFrameReady`, and a one-shot `RenderTexture` pixel readback (`SampleRenderTexturePixels`) — useful if this class of bug recurs for a future clip.
- **Audio rework on Level Complete/Failed — 2026-07-07.** Gameplay music now stops the instant the state transitions to `LevelComplete`/`LevelFailed` (`AudioManager.OnStateChanged`), and the procedural `Win`/`Fail` DSP jingles (`AudioManager.Sound.Win`/`.Fail`) were removed entirely — previously both played on top of the celebration/taunt video's own accompanying audio clip and the still-looping gameplay music. Now the video's own `AudioClip` (played via `VideoChromaKey.Play`) is the only sound heard during that sequence.
- **Level Complete/Failed videos held 1s longer before fading — 2026-07-07.** `LevelCompleteManager._celebrationDuration`/`LevelFailedManager._tauntDuration` were `4f`, but the actual clips run ~4.04-4.05s — the hold was cutting off fractionally before the clip's own last frame finished and immediately fading, which read as "ends very abruptly" (user report). Bumped both to `5f` (clip length + a full 1s of held-still breathing room). **Both the C# defaults AND the already-serialized values in `Game.unity` were updated** — see the `[SerializeField]` stale-value trap below; changing only the code default would not have affected the live scene.
- **Haybail impact/destroy sound replaced with a dedicated explosion clip — 2026-07-07.** Every haybail hit is a guaranteed same-frame one-shot kill (hp=10, `passThrough=true` — see `CluckAnimal.OnCollisionEnter2D`'s pass-through branch), so the old sound stack for one "pop" was: the chicken's own explicit `AudioManager.Play(Sound.WoodHit)` punch sound, PLUS `BlockBase.PlayHitSound()`'s generic `WoodHit` (fired unconditionally inside `TakeDamage()`), PLUS `BlockBase.DestroyBlock()`'s generic `BlockDestroy` — three sounds stacked on one moment. Fixed by: (1) deleting the chicken's own punch-sound line in `CluckAnimal.cs` entirely; (2) adding two new generic per-prefab override fields directly on `BlockBase` — `_silentHit` (skips `PlayHitSound()` when true) and `_destroyClipOverride` (an `AudioClip` that plays via a new `AudioManager.PlayClip()` instead of the generic `BlockDestroy` sound when set) — same shape as the existing `_stayKinematic`/`_sprExplode` per-prefab overrides, so `WoodBlock`/`StoneBlock` are unaffected (both fields default to false/null); (3) wiring both fields on `HaybaleBlock.prefab` in `SceneSetup.EnsureHaybaleBlockPrefab()` from `Assets/Audio/Haybail_Exploding.mp3`. Unlike `AudioManager.Play(Sound, cooldown)`, `PlayClip()` has no cooldown gate — the user explicitly wants this sound to fire every single time, never throttled.
- **Landing page SETTINGS icon removed entirely — 2026-07-07, user request.** `MainMenuController`'s gear-icon button and its Music/SFX toggle popup (`BuildSettingsPopup`, `OnSettingsClicked`, etc.) are gone, not hidden — there is currently no way to toggle audio from the landing page. The equivalent toggle is still reachable in-game via `HUDController`'s top-right Mute button (same `AudioManager.MusicEnabled`/`SfxEnabled` state).
- **L03–L06 removed entirely 2026-07-09 (user decision); L03 hand-rebuilt 2026-07-10 as "The Tower," L04–L06 still removed.** They were coordinate-migrated 2026-07-27/2026-07-09 (same rigid delta as L02) but never actually hand-built or visually verified — auto-generated placeholder layouts the user decided to replace by hand-building every level individually going forward, the same way L02 was rebuilt (see next entry). `LevelDataGenerator.cs`'s `Make(...)` calls for them were deleted, and `DeleteStaleAsset()` calls added so their old `.asset` files get cleaned up on every `Generate All Level Data` run — this matters because `GameManager` auto-loads *every* `LevelData` asset it finds in `Assets/ScriptableObjects/Levels/`, regardless of what the generator currently produces, so removing the code alone wouldn't have removed them from the game. `GameManager` now wires 3 levels; World map slots 4+ show "COMING SOON" until built. Plan: build L04–L05 next session, introducing Bessie and Cluck's ability at L05.
- **L02 rebuilt as "Harvest Yard" 2026-07-09 via a new hand-authored level-design workflow (`LevelLayoutDumper.cs`), replacing the old coordinate-migrated "Stone Wall."** Workflow: drag real prefabs or raw sprites into the Scene view (raw sprites go under an empty GameObject named exactly `LevelScratch`), then run **FarmFury → Debug → Dump Level Layout To Log** — it reads back each object's actual transform and prints ready-to-paste `B(...)`/`R(...)` code for `LevelDataGenerator.cs`, type-detected from the prefab path or sprite filename (keyword matching, checked in most-specific-first order — e.g. `"semiharvest"` before `"harvester"` before `"robot"`, since `"Robot_SemiHarvest"` contains both weaker substrings). **`LevelScratch` must be deleted after dumping** — it isn't tied to `LevelLoader`'s per-level spawn/clear cycle, so left in place it renders on top of *every* level regardless of which is loaded (this caused a real "level 1 and 2 compiled over each other" bug); `SceneSetup`'s placeholder cleanup now auto-deletes it as a safety net, but don't rely on that. Introduced `RobotType.SemiHarvester` (third enum value, `Robot_SemiHarvest.png`, `SemiHarvesterRobot.prefab`, HP=38, MatchUp card art `Semi_Harvestor.png`) alongside the existing Basic/Harvester types.
- **Match-up screen ("deck of cards") now supports a second robot card** when a level's `robots[]` contains two distinct `RobotType`s (as L02 does) — renders in front of (later sibling than) the primary card, offset (130,−100) and rotated −22°, sliding in on its own beat 0.3s after the primary card's clash. Also: level header art (`_levelHeaderSprites[]`) is now level-indexed with fallback to index 0, instead of hardcoded to `LevelHeader1.png`; Cluck (`Cluck_InFlight.png`) now flies across the bottom of the screen behind the cards during the countdown, on a sine-arc path (not a straight line — raised to `CluckFlyArcHeight=260` after two rounds of "more arc" feedback), launching with the cannon-shot SFX and landing exactly as the countdown ends (same duration as `CountdownClipLength`).
- **Robot death and animal-impact VFX/SFX added 2026-07-09.** `RobotEnemy` now spawns an `Explosion.png` comic burst (sized off the robot's own `transform.localScale`, multiplier tuned down twice — 1.8 → 1.1 → 0.6 — after "oversized" reports) and plays `Explosion_Robot.mp3` instead of the procedural `RobotDeath` jingle when a dedicated clip is wired (`SceneSetup.WireRobotDeathFx()`, shared across all 3 robot prefabs). `AnimalBase` now spawns an `ImpactStars1.png` burst (sized off the animal's own collider diameter) on the first real collision only (gated on `_contactStarted`, not every subsequent roll/bounce) and freezes the animal in place immediately on that same first impact (zero velocity, switched to `Kinematic`) instead of letting it visibly roll/bounce for the rest of the `_contactTimeout` window (also lowered 1f → 0.25f) — freezing rather than destroying synchronously because `BessieAnimal.OnCollisionEnter2D` starts its `Shockwave()` coroutine immediately after calling `base.OnCollisionEnter2D()` on the same frame.
- **`SceneryBuilder`'s random decorative-prop scatter disabled for all levels 2026-07-09.** It only ever skipped its `System.Random`-seeded placement for `levelIdx==0` (L01's hand-authored scenery) — every other level got an undesigned scatter of leftover `World1Props` art, which kept surfacing as "old/random sprites still visible" bug reports on L02 even after its own gameplay content (blocks/robots) was correctly hand-built. Root cause was this separate system, not L02's own data. Disabled with an early `return` in `BuildForLevel()` (re-enable once there's an actual per-level scenery plan).
- **KlingAI watermark removed from both celebration videos 2026-07-09.** Both had a "KlingAI 3.0 Omni" logo burned into the bottom-right corner. Reprocessed frame-by-frame (OpenCV + ffmpeg, matching original H.264/yuv420p/24fps): `Cluck_Celebration.mp4` via `cv2.inpaint` (gradient sky background, no character overlap there); `Robot_Celebration.mp4` via flat-fill with the region's own per-frame-sampled green (solid chroma-key screen, no inpainting needed — matching the actual footage's green rather than the shader's nominal `#00B140` avoids a visible seam while staying well within keying tolerance). Neither video has an embedded audio track or is used as one in-game (`VideoChromaKey` always plays a separate `AudioClip`), so this was video-only.
- **`assets/` (raw art source) is permanently removed from this repo — 2026-07-14, user decision.** The 236 files now live on the Desktop, outside the project directory, not just deleted-on-disk-but-tracked as before; the git deletion was finalized (committed, not left dirty) and `unity/.gitignore` was NOT changed for this (there is no `assets/` gitignore entry needed since the directory no longer exists in the repo at all). `python tools/remove_backgrounds.py` will not work without pointing it at the new Desktop location.
- **`unity/Assets/Sprites/` (processed game art) is gitignored and exists in exactly one place: this machine's disk.** Not tracked by git in any commit. If lost, the only recovery path is restoring `assets/` and re-running the full art pipeline.
- **Monetisation/backend: 0% built.** `unity/Assets/Scripts/Monetisation/` exists but is empty — no Firebase, Unity IAP, AdMob, RevenueCat, or UGS references anywhere. Expected — this is Phase 6, correctly sequenced after content.
- **No level validator exists** (GDD §04 calls for one — a physics-sim script checking stability/solvability before a level ships). The L02–L06 coordinate-migration bug (now fixed, see above) would have been caught by this.
- **No "Perfect" star tier** — `ScoreManager` implements 1/2/3★ only; the GDD's 4th tier (3★ + all blocks destroyed) is not implemented.
- **All 18 World 1 levels now exist, no gaps** (L01–L18) — completed 2026-07-12, see the top Current Status entry for full detail (Commander boss, prefab-wiring bug fix, WoodArtVariant expansion, etc.). None visually verified in Play mode yet. Next session: World 2 (Frozen Tundra) content, or a live playthrough pass of L10-L18 first.
- **No ground/grass visual ever existed for L01 — fixed 2026-07-26 with a placeholder.** `EnsureGround()`'s physics collider is deliberately invisible (top edge Y=−6.60), and no one had hand-authored a replacement, so the sky backdrop ran straight to the bottom of the screen. Symptom reported by the user: haybails/HarvesterRobot/a landing Cluck all looked like they were sinking or "falling through the floor" near the bottom of the screen. Root cause confirmed NOT a physics bug (Ground's collider/Rigidbody2D/layer-collision setup all check out) — the camera's visible range at rest (Y −6.5 to +2.5) already clips 0.1 units above the true ground surface, so anything settling near true ground level visually vanishes with nothing to anchor it. `SceneSetup.EnsureGroundVisual()` now creates a tinted placeholder strip (top edge Y=−5.3, matching where props already rest) to close that gap — replace with real ground/grass art when available, then delete `GroundVisual_Placeholder` from the scene.
- **`LevelSelectController` removed entirely 2026-07-26** — user-reported the old "SELECT LEVEL" grid screen kept appearing instead of/behind the Sunrise Meadows world map. Root cause: it subscribed to `GameManager.OnStateChanged` and showed itself on `GameState.Idle` using the exact same Canvas `sortingOrder` (300) `WorldMapController` uses — the two were silently racing to show themselves on every Idle transition ever since `WorldMapController` superseded it for World 1 (2026-07-15); it was never actually disabled, just left running unused in the background until now. The script, its `SceneSetup.EnsureLevelSelect()` wiring, and the leftover `LevelSelect` scene GameObject (auto-cleaned by `WireAll()` on next run) are all gone. World 2+ needs its own map screen built following `WorldMapController`'s pattern when that content exists — not a resurrected grid select.
- **Pause access restored 2026-07-26** via a new top-right 3-button row (Quit/Mute/Pause, real icon art) — see HUDController.cs entry above. Briefly removed earlier the same day alongside the top-centre score readout ("0" box, still gone) before being reinstated in this fuller form.
- **L01 haybale pile Y-shifted +1.0 on 2026-07-26** (user-reported: pile sat below the camera's visible safe line — bottom edge was at Y≈−6.73 against a visible-camera-bottom of Y=−6.5). New positions put the lowest bale's bottom edge at Y≈−5.73, matching where the other hand-placed props visually sit rather than the (mostly off-screen) true physics ground line. All 4 bales are `_stayKinematic` (see BlockBase.cs entry), so once placed they never move again regardless of being struck.
- **Level Complete/Failed celebration videos added 2026-07-07** — see the "Video Chroma Key / Celebration System" section below for the full architecture. **Ghosting bug fixed same day**: the chroma-key shader originally computed pure RGB-Euclidean distance to the key colour, which can't distinguish a desaturated dark pixel (character outline/shadow shading) from a mid-tone green — both sit numerically "close" to `#00B140` in flat RGB space. User-reported symptom: Cluck's own body looked like a translucent "ghost" instead of a solid character, not just the greenscreen going transparent. Fixed in `ChromaKeyVideo.shader` with a saturation gate — pixels less than half as saturated as the key colour are forced fully opaque regardless of raw RGB distance. Also added an explicit sky-backdrop layer (`VideoChromaKey._backgroundSprite`, wired to `SkyPainting.png`) behind the keyed video, since the transparent areas previously fell through to whatever the frozen gameplay camera happened to be rendering (busy level art), which read as messy — a clean sky gives the character a consistent surface to stand on regardless of which level is paused.
- **L01 top haybale re-lowered 0.35u + recentred on 2026-07-06** — user reported it "still too high and off the mark": it previously balanced on a single point directly above one base-row bale (X=4.029, Y=−4.497). Moved to X=3.935 (base row's mean X), Y=−4.85, nesting it into the row instead of perching on one corner. The internal single-bale-height stacking math (0.7365u gap, from Haybail.png's measured trimmed content) was previously verified and is unaffected by this change — see `LevelDataGenerator.cs`'s comment above the `Make("L01_FirstContact", ...)` call. Unverified visually in this environment (no Unity render access) — re-check against a fresh screenshot.

---

## GDD Summary

### 8 Animals (tap mid-flight to trigger ability)
| Animal | Ability | World introduced |
|---|---|---|
| Cluck (chicken) | Cluster Bomb — 5 eggs scatter on tap | World 1 |
| Bessie (cow) | Ground Slam — radial shockwave on landing | World 1 |
| Percy (pig) | Bounce Roll — curls, bounces 3× gaining speed | World 2 |
| Woolly (sheep) | Triple Clone — splits into 3 at 15° spread | World 2 |
| Ducky (duck) | Skip Shot — skips flat surfaces 3× | World 3 |
| Horace (horse) | Rear Kick — kicks debris backward on impact | World 4 |
| Gerald (turkey) | Puff Up — inflates 3× on tap, wrecking ball | World 5 |
| Billy (goat) | Headbutt Through — penetrates first obstacle | World 6 |

Only Cluck and Bessie are used in World 1 so far; the other 6 are fully scripted (`AnimalBase` subclasses) but await their worlds.

### 6 Worlds, 6 Launchers (126 total levels)
| World | Launcher | Key mechanic | Levels |
|---|---|---|---|
| 1 — Meadow Ruins | Farm Cannon (drag angle+power) | Character weight affects range | 18 |
| 2 — Frozen Tundra | Ice Cannon (angle + ricochet) | Freeze zones slow flight | 22 |
| 3 — Watermill Village | Water Wheel (timing — tap to fire) | Fire spread on wood | 22 |
| 4 — Sky Islands | Airdrop Biplane (timing — tap to drop) | Updraft column steering | 24 |
| 5 — Sunken City | Torpedo Tube (angle + bubble pop) | Current lanes + lever switches | 22 |
| 6 — Robot Mothership | Gravity Sling (angle + gravity wells) | Zero-G ability modifiers | 16 |

World 1 is fully built. World 2's front-end shell exists as of 2026-07-19 (backdrop, landing/map screens, match-up art, robot-tier reskins — see Current Status) but has zero real levels yet. Worlds 3–6 are Phase 5, 0% started.

### Star System
- 1★ — all robots dead (progress gate)
- 2★ — all dead + 1 animal remaining (mastery)
- 3★ — all dead + 2+ animals remaining (prestige)
- "Perfect" — 3★ + all blocks destroyed (hardcore/leaderboard goal) — **not implemented**

---

## Commands

### Unity (Editor)
Open `unity/` in Unity Hub (Unity 6.5 / 6000.5.0f1). Open `Assets/Scenes/Game.unity`. Press Play — the ground, camera, and LevelLoader reference are all self-wired at runtime.

### Run-Unity.ps1 (batch automation)
Primary interface for batch Unity operations. Run from the repo root in PowerShell:

```powershell
.\Run-Unity.ps1 levels        # Generate/overwrite all 6 LevelData ScriptableObjects — safe for L01
                               #   (kept in sync since 2026-07-01), destructive-neutral for L02-L06
                               #   (they're already broken; regenerating won't fix or worsen them)
.\Run-Unity.ps1 setup         # Wire all Inspector references in Game.unity (= Wire Scene References)
.\Run-Unity.ps1 check         # Compile check — exits 1 on error
.\Run-Unity.ps1 build         # Windows 64-bit -> unity/Builds/Windows/
.\Run-Unity.ps1 build-webgl   # WebGL build  -> unity/Builds/WebGL/
.\Run-Unity.ps1 build-android # Android APK  -> unity/Builds/Android/
```

Logs written to `unity/Logs/batch_<command>.log`. Filtered output is printed to console.

### Editor Menu Items
| Menu | When to run |
|---|---|
| **FarmFury → Wire Scene References** | After adding a new prefab, level, or after clean checkout. Wires all Inspector refs in Game.unity. Also sets camera position/orthoSize and `_cameraRestOffset`. |
| **FarmFury → Generate All Level Data** | Recreate the L01/L02 LevelData assets (overwrites existing) and delete any stale `.asset` for a level whose `Make(...)` call was removed (see `DeleteStaleAsset`). L01 is kept in sync — safe to run. Run Wire Scene References after. |
| **FarmFury → Wire Sprites** | After adding new character art to `Assets/Sprites/Characters/`. Sets PPU and wires pose sprites into all 8 animal prefabs. |
| **FarmFury → Reimport Sprites** | Force-reimport all sprites in Launchers/, Characters/, and Enemies/ folders. Run if sprites look stale after the art pipeline. |
| **FarmFury → Reset World Map Progress** | Clears `ff_score_N`/`ff_stars_N` PlayerPrefs for all 18 levels — standalone from Wire Scene References since it's runtime save data, not scene/asset wiring. Run when leftover test progress (e.g. a level showing 3-star art from earlier testing) makes the world map's lock/star state confusing to read. |
| **FarmFury → Debug → Dump Level Layout To Log** | Preferred level-authoring workflow as of 2026-07-09 (see `LevelLayoutDumper.cs`). Drag real block/robot prefabs anywhere in the Scene view, or raw sprites under an empty GameObject named exactly `LevelScratch`, then run this — it writes ready-to-paste `B()`/`R()` code to `unity/Logs/level_layout_dump.txt` from the objects' actual transforms. **Delete the `LevelScratch` GameObject after dumping** — left in place it renders on top of every level regardless of which is loaded. |

### Art pipeline
After adding new Kling AI PNGs to `assets/` (currently absent from disk — see Current Status above):
```bash
pip install Pillow
python tools/remove_backgrounds.py   # strips white bg, writes to unity/Assets/Sprites/
```
Then open Unity — `EditorAutoSetup` and `SpriteAutoImporter` handle re-import automatically on compile. If sprites look stale, run **FarmFury → Reimport Sprites**.

`assets/` layout (per `tools/remove_backgrounds.py`'s `CHAR_MAP` and output paths):
```
assets/
  Backdrops/          sky painting, prop reference sheets
  <Name>_<Animal>/    per-character pose PNGs (e.g. Cluck_Chicken/ -> unity Characters/Cluck/)
  FarmCards/          HUD card portraits
  LevelCards/         6 world card images (all 6 exist)
  RobotEnemy/         Robot_Idle.png, HarvesterRobot.png
  Buildingblocks/     Cement.png, Metal.png, Wood.png — future block types (not yet wired)
  WorldProps/         per-world prop PNGs
  FrameSprites/       VFX spritesheets (DustCloud, EggSplat, Explosion, FeatherBurst, etc.)
```
World 1 props live directly in Unity (not `assets/`): `unity/Assets/Sprites/Environment/World1Props/`.

**Sprite sizing spec (Kling AI generation):** characters 1024×1024px, 75-80% canvas fill, white bg. Sky 1920×1080px, no alpha. Props/launchers 1024×1024px, white bg, element 75% of canvas.

---

## Auto-Compile Pipeline (non-obvious)

`EditorAutoSetup` (`unity/Assets/Editor/EditorAutoSetup.cs`) runs `[InitializeOnLoad]` on every compile:
1. Auto-generates levels if no `LevelData` assets exist in `Assets/ScriptableObjects/Levels/`
2. Auto-wires character sprites (PPU values) if the `Cluck/Loaded.png` sentinel is on wrong PPU or wrong sprite mode
3. Auto-copies card sprites: `assets/FarmCards/*.png` → `Assets/Sprites/UI/Cards/`, `assets/LevelCards/*.png` → `Assets/Sprites/UI/LevelCards/` (only copies files not yet present)
4. Auto-fixes the cannon sprite (Single mode, PPU=384)
5. Calls `AssetDatabase.Refresh()` to pick up files changed outside Unity (e.g. by `remove_backgrounds.py`)

`SpriteAutoImporter` (`unity/Assets/Editor/SpriteAutoImporter.cs`) is an `AssetPostprocessor` that enforces PPU and `Single` mode on import for launchers, UI cards, robot, and character sprites — **every category enforces `Single` mode on import; `Multiple` mode silently breaks `LoadAssetAtPath<Sprite>`/prefab wiring** (this exact bug cost an entire debugging arc — see `docs/HISTORY.md` Round 8).

---

## Architecture — Unity Port (`unity/`)

### Stack
Unity 6.5 (6000.5.0f1), URP 2D, Physics2D, New Input System, TextMeshPro.

### Coordinate System (current — all 6 World 1 levels now use this; do not use older values seen in stale comments)
- 1 Unity unit = 50 Phaser pixels (`x_unity = x_phaser / 50`, `y_unity = -(y_phaser - 770) / 50` — legacy conversion from the Phaser prototype).
- **Ground surface at Y = −6.60.** Launcher GO at (−2.327, −6.60, 0) — an abstract aim-math anchor only since the 2026-07-02 cannon swap; no visual GameObject corresponds to it any more.
- **Visual launcher is `FarmCannon`**, independent of the Launcher GO's position: at (−3.72, −4.7032, 0), scale (1.4711188, 1.3316845), sortingOrder=4. (Hand-repositioned twice since the 2026-07-13 "cannon too close" fix — moved closer to the barn door again during the 2026-07-17 parallax-backdrop session, then fine-tuned 2026-07-18 alongside the new decorative barrels/GnarledTree — see the Current Status entries for both. `SceneSetup.cs`'s `WireLauncher()` only stamps this position on first creation of the GameObject (fixed 2026-07-17, see below), so an already-existing hand-placed `FarmCannon` in `Game.unity` is the actual ground truth going forward; the fallback literal in code is re-synced to match whenever it's known to have drifted, but treat the live scene, not this doc line, as authoritative if the two ever disagree. `CatapultLauncher.ComputeOrthoSizeForLevel()`'s camera auto-zoom seeds its bounding box from this GameObject's real `SpriteRenderer.bounds`, not the abstract Launcher anchor.)
- Camera at (0, −2, −10), orthoSize = 4.5. `_cameraRestOffset = (2.327, 4.60)` → camera parks at (0, −2). Visible Y range at rest: −6.5 to +2.5 — **only 0.1 units above the ground line**, so any sizeable ground-standing sprite will clip the bottom of the screen unless deliberately raised with a small hover margin. This affects any future tall enemy in World 1, not just the current robot.
- **Level block/robot positions are stored as raw world-space coordinates in `LevelData`** — `LevelLoader.SpawnBlock`/`SpawnRobot` apply no offset; whoever authors a level is responsible for baking correct current-system coordinates directly.

```
Y convention: Ground surface = −6.60. Robot center (h=0.9, scale 0.6×0.9) → world y = −6.15 (sits on ground).
              Wood plank (h=0.4) resting on ground → center at world y ≈ −6.40. Stack upward by block height.
X convention: Launcher (abstract) at X=−2.327. Place structures to the right of it (positive X), reachable by the arc.
```

### Script Architecture
```
unity/Assets/Scripts/
  Core/
    GameManager.cs          — singleton (DontDestroyOnLoad); states: Idle/Playing/LevelComplete/LevelFailed
                              ForceStartLevel(int) boots a level without LoadScene (used for direct Editor play
                              and for Replay/Restart — see Runtime Event Flow below)
                              TryAutoLoadLevels() auto-discovers LevelData assets in Editor builds
                              BuildFallbackLevel() creates a hardcoded procedural level when no assets found
                              LoadMenu() is scene-optional: only loads MainMenu if it's in Build Settings
    BackgroundController.cs — sortingOrder=−100; cover-scale in Start(); LateUpdate() follows camera
    AudioManager.cs          — 7 DSP-generated SFX clips + 3 external MP3 clips (Assets/Audio/*.mp3):
                              _musicClip (loops from first GameState.Playing, never restarts across levels),
                              _cannonShotClip (replaces the procedural Launch clip at the same
                              AudioManager.Play(Sound.Launch) call site), _fallingClip (loops from Fire()
                              while a Cluck is airborne, fades 0.35s on AnimalBase.OnAnimalImpact — real
                              hits only, not CluckAnimal's hay pass-through punches). Lives on its own
                              dedicated scene GO (SceneSetup.EnsureAudioManager()), [DefaultExecutionOrder(-90)]
                              so it wins the singleton race against CatapultLauncher's fallback AddComponent.
                              SfxEnabled/MusicEnabled persisted via PlayerPrefs, toggled from both the
                              top-right Mute button and the Main Menu SETTINGS popup (same state, can't
                              disagree — the pause menu's own separate Music/SFX toggles were removed along
                              with the rest of that panel 2026-07-06).
                              Mix rebalanced 2026-07-26 (user-reported: cannon shot buried the Cluck falling/
                              scream loop, which starts in the same frame — see CatapultLauncher.Fire()):
                              FallingVolume raised 0.6->0.9, and Play()'s previously-uniform 0.8 PlayOneShot
                              scale is now a per-Sound VolumeScale[] array with Launch specifically dropped
                              to 0.5 — every other SFX (WoodHit/StoneHit/RobotDeath/Win/Fail/BlockDestroy/
                              RobotHit) keeps the original 0.8.
                              **Menu music added 2026-07-07**: a second _menuMusicClip
                              (SunriseMeadows_TransitionMusic.mp3) + dedicated _menuMusicSrc AudioSource,
                              independent of the gameplay loop above. OnStateChanged starts/stops the two
                              tracks so exactly one plays at a time: GameState.Idle (covers both the landing
                              page and the Sunrise Meadows world map — both react to that same state, so
                              hooking it once here plays under either screen without needing to know which
                              is visible) starts menu music and stops gameplay music; GameState.Playing does
                              the reverse. Start() syncs once against GameManager.Instance.State on
                              subscribe, since State defaults to Idle at boot without ever firing a
                              transition event for it (TransitionTo only fires on an actual change) — without
                              this, a fresh launch landing on the menu would never start any music at all.
                              Gameplay music, cannon fire, and the Cluck falling loop are all untouched by
                              this addition (explicit user request when adding it, to avoid regressions).
    LevelCompleteManager.cs  — [DefaultExecutionOrder(-40)] owns the Level Complete "reward" beat, added
                              2026-07-07. Old flow: GameState -> LevelComplete fires HUDController's panel
                              immediately. New flow: Time.timeScale=0.2 for 0.5s (WaitForSecondsRealtime) ->
                              Time.timeScale=0 (hard freeze) -> CatapultLauncher.LastAnimalUsed's celebration
                              VideoClip + AudioClip play via the shared VideoChromaKey overlay (see below) ->
                              holds 4s -> fades 0.3s -> Time.timeScale=1 -> HUDController.ShowLevelCompletePanel()
                              (now public — HUDController's own OnStateChanged no longer calls it directly,
                              this class is the only caller). _celebrationClips[]/_celebrationAudioClips[] are
                              both indexed by AnimalType (8 slots) with an empty-slot fallback to index
                              Cluck, so every animal gets a celebration before every clip exists — wired via
                              SceneSetup.EnsureLevelCompleteManager() from Assets/Video/Cluck_Celebration.mp4
                              + Assets/Audio/Cluck_CelebratingLaugh.mp3 (paths updated 2026-07-07 when the
                              user reorganized source assets — see Current Status). Self-bootstraps via
                              CatapultLauncher.Awake() (same null-safety fallback pattern as AudioManager) if
                              missing from the scene, so it works even before a Wire Scene References pass.
    LevelFailedManager.cs    — mirrors LevelCompleteManager.cs exactly (added same day), but keys its clip
                              off GameManager.CurrentLevelIndex (which robot the player is up against) rather
                              than which animal was last fired: Time.timeScale=0.3 for 0.5s -> freeze -> the
                              current level's robot taunt VideoClip + AudioClip play -> hold 4s -> fade 0.3s
                              -> HUDController.ShowLevelFailedPanel() (now public, same decoupling as above).
                              _robotTauntClips[]/_robotTauntAudioClips[] are indexed by level number
                              (0-based), any level past the array's end or an empty slot falls back to index
                              0 — wired via SceneSetup.EnsureLevelFailedManager() from Robot_Celebration.mp4 +
                              Robot_CelebrateSound.mp3 (index 0 = L01, the only pair that exists today). Also
                              drives the Level Failed panel's RETRY button pulse once shown (see
                              HUDController.RetryButtonPulse below).
    CameraShake.cs           — singleton; tracks its own per-frame contribution as a delta and subtracts it
                              back out, so repeated shakes always net to exactly zero (never permanently
                              drifts the camera — see docs/HISTORY.md Round 7 for the bug this fixed)
    ParallaxScroller.cs      — speed 0.0 (world-fixed) – 1.0 (camera-locked); offsets X by camDelta×speed
    SceneryBuilder.cs        — subscribes to OnLevelStarted; _useExactPlacement=true + levelIdx=0 skips
                              entirely (L1 scenery is hand-authored permanent scene GOs); other levels use
                              deterministic System.Random(levelIdx*137+42) layout. PlaceExact() corrects for
                              native sprite size. FarmSilo excluded from all placement paths.
  Level/
    LevelData.cs             — ScriptableObject; birds[], blocks[], robots[] arrays; par bird count;
                              BlockSpawnData has optional passThrough/healthOverride/massOverride
                              (0 = use BlockBase defaults); BlockType.Haybale (enum value 2) renders
                              via HaybaleBlock.prefab (WoodBlock component + Haybail.png art)
    LevelLoader.cs            — instantiates prefabs; TryConsumeBird / PeekNextBird; OnBirdConsumed event;
                              BirdQueueSnapshot; DelayedLevelComplete/Failed coroutines -> GameManager;
                              AutoLoadPrefabs() (Editor-only, Awake()) auto-finds prefabs from Assets/Prefabs/;
                              SpawnRobot() re-derives BoxCollider2D size (0.6/scaleX, 0.9/scaleY) when a
                              custom visual scale is applied, keeping the hitbox pinned regardless of scale
  Animals/
    AnimalBase.cs             — abstract; Kinematic until Launch(); Pointer.current (New Input System, covers
                              Mouse+Touch); 5 pose sprites (_sprIdle/_sprLoaded/_sprInFlight/_sprImpact/
                              _sprAbility); HasRealSprites property; DestroyAnimal() fires OnAnimalDestroyed;
                              OnAnimalImpact fires on real collision hits only; sortingOrder=6 on all 8 subclasses.
                              OnCollisionEnter2D shows _sprImpact (e.g. Cluck_Impact.png) for the rest of the
                              _contactTimeout window (fixed 2026-07-26 — it used to get assigned then hidden
                              on the very next line in the same frame, so the reaction pose was never actually
                              visible before DestroyAnimal() removed the GameObject); only falls back to the
                              old instant-hide when no impact art is wired at all (procedural fallback circle).
    CluckAnimal.cs             — 5-egg cluster bomb in 120° spread from _eggPrefab (must be the egg GameObject
                              itself — _eggPrefab is typed GameObject, not EggProjectile); pass-through: punches
                              WoodBlock._passThrough=true at 70% velocity, skips base.OnCollisionEnter2D (so
                              hay punches never trigger OnAnimalImpact / stop the falling SFX). Calls
                              wood.TakeDamage(wood.MaxHealth) directly on pass-through (2026-07-06 fix) —
                              previously relied on BlockBase's own physics-impulse-derived damage from the
                              same collision, which varies with impact speed/angle across the Cluck's arc and
                              could leave a passThrough=true block alive after a graze, needing extra birds to
                              finish it off (user-reported: took all 3 Clucks to clear one haybale pile). Pass-
                              through blocks now always die in exactly one hit, any contact.
    BessieAnimal.cs            — vy-18 slam; shockwave 3.6u radius on Ground-tagged landing
    PercyAnimal / WoollyAnimal / DuckyAnimal / HoraceAnimal / GeraldAnimal / BillyAnimal
                              — Bounce Roll / Triple Clone / Skip Shot / Rear Kick / Puff Up / Headbutt
                              Through respectively; scripted and ready, not yet used in any shipped level
    EggProjectile.cs           — layer 10; flat _damage=15 on first contact only
  Blocks/
    BlockBase.cs               — spawns Static; TakeDamage() wakes ONLY the block actually hit (flips its
                              own Rigidbody2D to Dynamic), gated on _stayKinematic=true blocks (e.g.
                              HaybaleBlock) which never transition at all. A level-wide wake-every-block
                              sweep (FindObjectsByType<BlockBase>(), Unity 6 API) existed 2026-07-26 through
                              2026-07-10 — added for HaybaleBlock's own cluster (fixed by giving Haybale
                              _stayKinematic), but the same bug then resurfaced for Wood (hitting a haybale
                              in L03 shifted/collapsed a completely unrelated wood tower), so the sweep was
                              removed entirely rather than patched per block type — every block's
                              fall/collapse is now purely local, matching RobotEnemy's per-robot support-loss
                              check. health =
                              baseMaxHealth × area/stdArea; tints at 50%/25%/0% health; damage = impulse ×
                              1.0; on death: _sprExplode (if wired) shows a death-burst sprite via
                              SpawnExplosion() instead of the default 4-fragments-fly-outward-fade-1->0-
                              over-0.6s (HaybaleBlock wires this to Haybail_Damaged.png — at hp=10 it
                              always dies on the hit that damages it, so this explosion is the reaction
                              players actually see). ApplyOverrides(health, mass) applies BlockSpawnData
                              overrides after Initialise() — called by LevelLoader. Optional _sprDamaged +
                              PlayDamageFlash() for a brief pre-death art swap on hit (only wired where
                              dedicated damaged-state art exists, e.g. HaybaleBlock) — in practice this
                              never gets seen on a one-hit-kill block like Haybale; _sprExplode above is
                              the one that matters there.
    WoodBlock.cs                — baseMaxHealth=5 (~10.4 effective HP for a 1x1 block via area-scaling —
                              see Known Issues), baseMass=5, bounciness=0.2; _passThrough (public);
                              _sprDamaged/_sprExplode both wired to WoodDebris.png — every wood plank
                              gets a dedicated hit-flash/death-burst instead of the generic 4-fragment
                              fade. **_explodesOnRobots (bool, default false, added 2026-07-10 fifth
                              balance pass)** — when false (plain Wood), DamageNearby() on death chains
                              damage to nearby BLOCKS ONLY, never robots; only Haybale (wired true via
                              SceneSetup.EnsureHaybaleBlockPrefab()) and ExplodingBarrelBlock (sets true
                              in its own Awake()) still call RobotEnemy.TakeExplosionDamage(). Blast
                              radius (EffectiveBlastRadius) is no longer a flat constant — it's
                              Mathf.Max(_col.bounds.size.x, _col.bounds.size.y) * ExplosiveForceScale
                              (0.4f, i.e. 40% of the block's own rendered footprint), recomputed at the
                              moment of death — see Known Issues for the "one hit" bug this fixes.
    ExplodingBarrelBlock.cs      — WoodBlock subclass, baseMaxHealth=5 (pops in one hit, same as
                              Haybale/Wood); _explodesOnRobots=true, _areaDamage=55 (bigger block-to-
                              block chain damage than plain Wood's 25 — blast radius is shared/derived,
                              see WoodBlock above, not a separate override any more). WoodenBarrel.png
                              art (SceneSetup.EnsureExplodingBarrelPrefab()), reuses RobotEnemy's own
                              Explosion.png/Explosion_Robot.mp3 death FX via the existing
                              _sprExplode/_destroyClipOverride fields rather than new art.
    StoneBlock.cs                — baseMaxHealth=220, baseMass=8, bounciness=0.1
  Enemies/
    RobotEnemy.cs                — HP=35 (class default; HarvesterRobot.prefab overrides to 28,
                              SemiHarvesterRobot.prefab to 26); scale=(0.6,0.9); BoxCollider2D.size=(1,1),
                              mass=20; 2 red eye child GOs; calls LevelLoader.NotifyRobotDestroyed;
                              Rigidbody2D.bodyType=Static set explicitly in Awake() (prefabs otherwise
                              default to Dynamic and free-fall under gravity — a real bug fixed
                              2026-07-18, see docs/HISTORY.md Round 12); _invincibleUntil =
                              Time.time+0.8f prevents instant death from fall-settling onto blocks at
                              level load; FlashDamage() swaps to _robotDamagedSprite briefly if wired,
                              else falls back to a white tint.
                              **Idle animation (2026-07-18)**: three mutually-exclusive modes, chosen
                              once in Awake(). (1) `_isTauntLoop` (= `_alertSprite != null`, unique
                              to CommanderRobot today) runs `TauntLoopRoutine()` — continuously
                              cycles `_robotSprite -> _alertSprite -> _criticalSprite -> repeat`
                              every 0.6s regardless of health, stopping only on death (Die() then
                              swaps to `_deathExplosionSprite` on the robot's own body). (2) a robot
                              with `_sprFacingRight` wired (Robot/HarvesterRobot/SemiHarvesterRobot)
                              runs `IdlePatrolRoutine()` — shuffles ±0.3 world units either side of
                              spawn X, swapping `_sr.sprite` between `_robotSprite` (left-facing) and
                              `_sprFacingRight` (right-facing) to match direction of travel; stops
                              instantly on any damage/Dynamic/destroyed. (3) neither field wired
                              falls back to the older in-place `IdleLookAroundRoutine()` (turn-and-
                              glance, no translation), same stop conditions as (2).
                              **Damage-factor model v2 (2026-07-10, fifth balance pass — see Known
                              Issues)**: every damage source is a fixed fraction of _maxHealth, no
                              impulse/speed dependency. TakeDirectHitDamage() (animal collision,
                              DirectHitDamageFraction=0.34, ~3 hits), TakeExplosionDamage() (a genuine
                              explosive prop — Haybale/ExplodingBarrelBlock — dying nearby,
                              ExplosionDamageFraction=0.28, ~4 hits), TakeEggDamage() (one connecting
                              Cluster Bomb egg, EggDamageFraction=0.12, ~9 hits — deliberately the
                              weakest), TakeFallDamage() (MakeDynamicFromSupportLoss — a robot's support
                              block destroyed, structural collapse only, FallDamageFraction=0.15, ~7
                              hits). Robot-vs-robot contact damage (_robotContactDamage, default 18) is
                              the one remaining impulse-plus-floor category, added to the real impulse
                              (not just whichever is bigger) via OnCollisionEnter2D's robot-vs-robot
                              branch — any robot-vs-robot touch always counts as a hit regardless of
                              _minDamageImpulse.
  Scoring/
    ScoreManager.cs               — Robot +1000, Wood +100, Stone +200, Egg +50, bird-left bonus +500;
                              PlayerPrefs keys: ff_score_N, ff_stars_N (0-based, plain star count)
  Launcher/
    CatapultLauncher.cs            — visual launcher is FarmCannon (see Coordinate System above). Aim math
                              (rewritten 2026-07-26 — direction and power are now independent axes, "Angry
                              Birds" style, per explicit user request after every shot looked identical
                              regardless of drag direction): click bird -> drag -> PivotPos()-relative pull
                              ANGLE clamped to [_armRestAngle=200°, 200°+MaxLoadAngle=60°=260°], mirrored
                              through the pivot (-180°) to a 20°-80° launch angle (200°->20° flat shot,
                              260°->80° near-vertical lob) — DIRECTION now actually changes the shot, unlike
                              the old trebuchet-arm scheme this replaced, which clamped the same way but only
                              ever read the clamped value as a single 0-1 "how far pulled" fraction driving a
                              barely-varying 52°-58° range, discarding the real direction entirely (see
                              docs/HISTORY.md). Pull DISTANCE from the pivot (clamped to _maxDragDistance=2.4)
                              independently drives POWER: LaunchVelocity() speed _minLaunchSpeed=3.8-
                              _maxLaunchSpeed=9.3 m/s (both [SerializeField] — _maxLaunchSpeed already existed
                              serialized in Game.unity at a stale 16.8 from an even earlier, never-wired
                              design; fixed at launch, then raised twice more since — 3.0-6.0 -> 3.5-8.5
                              (2026-07-11, "allow for high and strong firing") -> 3.8-9.3 (2026-07-13,
                              re-tuned after the FarmCannon reposition pushed the launch point ~4.5 units
                              further from every level's content — see Current Status). Each raise re-synced
                              directly in the scene YAML per the [SerializeField] stale-value trap below).
                              Numerically
                              verified (same substep model as DrawTrajectory) to span L01's play area across
                              multiple angle/power combinations rather than one fixed landing zone. Paired
                              with AnimalBase.Launch()'s gravityScale=0.18 for a slower, loopier arc (2026-07-14).
                              LastAnimalUsed (public static AnimalType, added 2026-07-07) is set the instant
                              Fire() actually consumes a bird — read by LevelCompleteManager to pick which
                              animal's celebration video plays. Static rather than an instance lookup since
                              there's exactly one cannon per scene and this outlives any single AnimalBase
                              instance (destroyed on landing). Awake() also self-bootstraps
                              LevelCompleteManager/LevelFailedManager onto their own scene GOs if missing,
                              same null-safety fallback pattern as the existing AudioManager bootstrap right
                              above it in the same method.
                              MinAimRadius=0.6f: drag ANGLE (only) freezes until the pointer has moved that far
                              from the pivot, preventing hair-trigger full-power swings from small movements
                              (mobile touch fix, 2026-07-18) — pull distance/power tracks the pointer from the
                              first frame, no deadzone. CannonBarrelOffset=(1.1,0.4) = _launchPoint
                              (trajectory-arc origin AND Fire()'s actual spawn point). CannonLoadedBirdOffset
                              =(0.6212,0.4223) = fixed ready-bird position (cannon body doesn't rotate).
                              BirdScale=(4.9204,4.9204) (uniform — InFlight is the only pose shown on this
                              GO); ApplyBirdScale() re-derives col.radius /= BirdScale.x to keep the physics
                              hitbox correct after the visual scale is applied. On fire: CannonFireSequence()
                              coroutine — recoil tween (no DOTween in this project), smoke burst
                              (sortingOrder=6, between cannon@4 and animals@... see sortingOrder rule below),
                              then waits out CannonResetDelay=1.80s total. EnsureGroundExists() validates
                              against the launcher's own Y (dynamic, not hardcoded).
  UI/
    HUDController.cs                 — Canvas built at runtime; card widgets anchored top-left (anchorMin
                              0.02,1); orange N damage badge (enlarged + restyled with StyleAsGameNumber()
                              2026-07-26, enlarged again 2026-07-06 — user reported it was still too small
                              to read, especially on the smaller queued cards; badge now 92x44/text 34f on
                              the active card, 74x36/text 26f on queued cards, up from 72x34/24f and
                              56x28/18f. Leading "⚡" dropped the same day — LiberationSans SDF (the only
                              font asset this project ships) has no glyph for U+26A1, rendering it as a
                              tofu box that read as "a square before the numbers"; same class of bug as
                              the missing ★ glyph fixed in MatchUpScreen (docs/HISTORY.md Round 14) — same
                              bold/black-outline/gold-orange-gradient treatment as the
                              Level Complete/Failed score numbers, per user request to "match the game
                              font"); Level Complete/Failed/Pause panels; SafeArea RectTransform wrapper
                              (Screen.safeArea-driven) for the bird queue.
                              **Top-centre score readout ("0" box) REMOVED 2026-07-26** (user request — read
                              as an unstyled placeholder rectangle over the sky art); `BuildScoreDisplay()`
                              and its fields are gone entirely, not hidden. Score is still shown on the Level
                              Complete/Failed panels via their own score text.
                              **Top-right button row (`BuildTopRightButtons`, added later the same day)**
                              replaces the single plain-grey-circle pause button that briefly got removed in
                              the same pass as the score readout above. Three real icon buttons, top-right
                              corner, 150x150 each (enlarged from 64x64 2026-07-06 — user-reported too small;
                              now matches MainMenuController's PLAY/SETTINGS button size) with a 14px gap,
                              order left-to-right Quit/Mute/Pause (Pause flush in the screen corner). **Quit
                              is sized up to 187.5x187.5 (`quitBtnSize = btnSize / 0.80f`), not the shared
                              150x150** (2026-07-06, user-reported the three icons "don't look like they're
                              the same size") — `Btn_quite.png`'s drawn button only fills ~87%x80% of its
                              own 256x256 canvas (pixel-measured content bbox 222x205), unlike
                              `Btn_pause.png`, whose art fills its canvas edge-to-edge (256x256), so
                              rendering both into an identical box left QUIT visibly smaller; the position
                              math (`pos`) still uses the shared `btnSize+gap` grid since QUIT's right-edge
                              anchor doesn't move, it only grows further left, away from Mute:
                              **Pause** (`Btn_pause.png` <-> `Btn_play.png`, swapped not tinted) calls
                              `OnPauseClicked()` -> `SetPaused()`, which just flips `Time.timeScale` 0/1 and
                              re-derives its own icon via `RefreshTopPauseIcon()` — no popup opens (the old
                              PAUSED panel was removed entirely 2026-07-06, see below). **Mute**
                              (`Btn_music.png` <-> `NoSound.png`, sprite-swapped) toggles BOTH
                              `AudioManager.MusicEnabled` and `SfxEnabled` together via
                              `OnTopMuteToggleClicked()`; `RefreshTopMuteIcon()` re-derives which sprite to
                              show from actual AudioManager state (not a locally-tracked flag) so it stays
                              correct even if the Main Menu SETTINGS popup's separate toggles are used
                              instead. **Quit** (`_quitButtonSprite`) no longer closes the app — since
                              2026-07-06 (user request) `OnQuitClicked()` un-pauses if needed and calls
                              `GameManager.LoadMenu()` + `WorldMapController.SkipToMainMenu()`, the same
                              "land directly on the main menu, skip the world map flash" pattern the Level
                              Complete/Failed Home buttons use. The whole row (`_topRightRoot`) hides via
                              `SetTopBarVisible` alongside the bird queue whenever a full-screen end-of-level
                              panel is up, same as the old score readout used to. `_birdQueueRoot` was
                              actually missing from that toggle until earlier the same day (user-reported
                              leftover animal cards
                              still visible top-left behind the Level Complete panel) — `_topRightRoot` was
                              added to it from the start, so it doesn't repeat that bug.
                              **Level Complete/Failed panels (2026-07-25 redesign, Level Complete
                              restructured again 2026-07-26)** both use Scoreboard.png
                              (Assets/Sprites/UI/MatchUp/) as the whole backdrop instead of a plain coloured
                              box (Level Complete's box enlarged 620x363 -> 680x398 2026-07-26, same 653:382
                              native aspect, to give the star row more headroom inside the board's parchment
                              area), with LevelComplete.png/LevelFailed.png as a sign-topper title
                              overlapping the board's top edge. Falls back to the old plain box/text-button
                              look if any sprite is unwired, so neither panel ever renders blank. Score
                              number uses StyleAsGameNumber() — bold + black outline + gold-to-orange vertex
                              gradient on the default TMP font, the closest approximation of the game's own
                              bubble-lettering achievable without importing a new font asset (this project
                              ships only LiberationSans SDF). Level Complete shows 3 real ScoreStars.png
                              images (dropped from 4 2026-07-26) — all three are now the actual star rating,
                              no forced always-pops slot. The old 4th "always pops, reveals a LEVEL UP! text
                              label" slot and the separate Btn_play icon were both removed in the same pass;
                              Btn_play's bottom-left slot is now a single _levelUpStarSprite (Levelup.png —
                              "LEVEL UP" baked into the art, no text label needed) that pulses continuously
                              (LevelUpStarPulse(), scale 1<->1.12 sine wave, starts in ShowLevelCompletePanel/
                              stops in HideLevelCompletePanel) and fires the exact same handler Btn_play used
                              to (OnLevelCompletePlayClicked -> GameManager.LoadMenu()) — it's now the
                              panel's one obvious, animated tap target back to the world map, rather than a
                              static icon next to a separate forced-star/text reveal. Btn_home is unchanged
                              (bottom-right, mirrors the level-up star's position/size). Level Complete's
                              level-up star calls GameManager.LoadMenu() and relies on WorldMapController's
                              own OnStateChanged listener to show itself and slide the position indicator to
                              the newly-unlocked next level (no extra plumbing needed — see WorldMapController
                              below); its Btn_home and Level Failed's Btn_home both call the new
                              WorldMapController.SkipToMainMenu() so tapping Home lands directly on the main
                              menu instead of flashing the world map first.
                              **PAUSED popup REMOVED ENTIRELY 2026-07-06** (user request — a separate
                              RESUME/RESTART/MENU/QUIT/MUSIC/SFX card popping up over the game was more
                              friction than it was worth once Quit and Mute already had their own dedicated
                              top-right buttons, below). `BuildPausePanel()`/`ShowPausePanel()`/
                              `HidePausePanel()`/`MakePanelButton()` and the panel's own
                              Resume/Restart/Menu/Music/SFX click handlers are all gone, not hidden. Tapping
                              the top-right Pause button now pauses/resumes directly (`SetPaused()` still
                              just flips `Time.timeScale` 0/1) and swaps its own icon between
                              `Btn_pause.png` and `Btn_play.png` (reusing `_playButtonSprite`, the same
                              sprite the Level Failed panel's Try Again button uses) via
                              `RefreshTopPauseIcon()`, so the icon itself always shows the next tap's
                              action instead of opening a menu. Restart/return-to-menu are still reachable
                              via the Level Failed panel's Try Again/Home buttons — only in-pause access to
                              them was removed, not the underlying `GameManager.RestartLevel()`/`LoadMenu()`
                              calls, which both panels still use.
                              **Panel triggers decoupled from OnStateChanged 2026-07-07**:
                              `ShowLevelCompletePanel()`/`ShowLevelFailedPanel()` are now `public` and no
                              longer called from this class's own `OnStateChanged` on the LevelComplete/
                              LevelFailed transitions — `LevelCompleteManager`/`LevelFailedManager` (see
                              Core/) call them directly once their slow-motion -> freeze -> celebration/taunt
                              video -> fade sequence finishes, so the panel is always the reward/defeat
                              beat's *second* step, never the first. `OnStateChanged` still handles
                              `HideLevelFailedPanel()`/`HideLevelCompletePanel()`/`SetTopBarVisible(false)`
                              on those same transitions, just not the show calls.
                              **Score count-up added 2026-07-07**: `ShowLevelCompletePanel()` now animates
                              the score text 0 -> final over `ScoreCountUpDuration` (1.6s, unscaled time),
                              timed to land right as the last star pop finishes (staggered 0.3/0.75/1.2s + a
                              0.38s pop each, see `AnimateStars`/`PopStar` above) — previously the score text
                              was set instantly.
                              **RETRY button pulse added 2026-07-07**: `ShowLevelFailedPanel()` now starts
                              `RetryButtonPulse()` on the Try Again button (`_lfTryAgainRT`) — scale eases
                              `1.0 -> 1.05 -> 1.0` on an 0.8s loop via `1 + 0.025*(1-cos(2πt))`, deliberately
                              *never* dipping below 1.0 (unlike the Level Complete level-up star's symmetric
                              ±0.12 `LevelUpStarPulse`) — meant to read as an inviting nudge ("come on, you
                              can beat that robot"), not a wobble. Stops and resets to scale 1 in
                              `HideLevelFailedPanel()`.
    MainMenuController.cs             — LandingPage.png background (title/character art baked in); PLAY
                              (bottom-left) and SETTINGS (bottom-right), both corner-anchored; PLAY opens
                              WorldMapController. SETTINGS opens a 7-tab popup (Audio/Gameplay/Stats/
                              Scores/Story/Account/About — see the 2026-07-13 Current Status entry for
                              full detail on each tab's real vs. placeholder content), REPLACING the old
                              flat 5-heading list. Audio's Music/SFX toggles + volume sliders still share
                              AudioManager's persisted state with the top-right Mute toggle
                              (HUDController's `OnTopMuteToggleClicked`) — same state, no separate
                              pause-menu toggle exists any more. New data files backing the tabs:
                              GameplaySettings.cs (Language/LeftHanded prefs), PlayerStatsTracker.cs
                              (lifetime cannonballs-fired/robots-destroyed/time-played counters,
                              self-bootstraps via CatapultLauncher.Awake()), StoryContent.cs (lore text,
                              also shared by About's Credits row via the same reader popup)
    WorldMapController.cs              — Sunrise Meadows (World 1) level map; ScreenSpaceOverlay Canvas,
                              sortingOrder 300; 18 LevelMarker instances positioned via PathPositions —
                              hand-traced against the full visible S-curve (pond -> up past the windmill/
                              ruins bend -> back down -> tail to the fortress), not just a graph-shortest-
                              path between the two ends. Every earlier automated trace (BFS-bin, skeleton
                              shortest-path) independently found the upper bend to be a topological dead-
                              end and excluded it — defensible by pure connectivity, but wrong for this
                              purpose: it rendered as markers bunched in a flat row hugging the bottom,
                              confirmed by rendering the old points back onto the source art and matching a
                              reported screenshot exactly (see docs/HISTORY.md Round 14). Unlock rule
                              (level 1 always unlocked, level N needs level N-1 >=1 star) reuses
                              ScoreManager.GetBestStars(); position indicator (90x90) slides then bobs to
                              the newest unlocked pin, hover offset +100 above the marker (single
                              IndicatorRoutine coroutine). NEXT LEVEL (600x150) and Home (150x150, matches
                              the landing page's PLAY/SETTINGS icon scale) both live inside a
                              Screen.safeArea-driven SafeArea child (same ApplySafeArea pattern as
                              HUDController/MatchUpScreen), corner-anchored with a small fixed inset —
                              real rendered sprites, not baked into the background. Owns MatchUpScreen's
                              art fields directly (see note below on why). Public SkipToMainMenu()
                              (2026-07-25) — called by HUDController's Level Complete/Failed Home buttons:
                              HidePanel() then MainMenuController.Show(). Needed because LoadMenu() always
                              transitions GameState to Idle, which this class's own OnStateChanged reacts to
                              by showing itself (it's the PLAY destination) — SkipToMainMenu() runs
                              immediately after in the same call stack, hiding the map again before a frame
                              ever renders it, so "Home" lands on the main menu instead of flashing the map.
    LevelMarker.cs                     — UI Image+Button (not SpriteRenderer — Button needs uGUI) + level
                              number TMP label; MarkerSize=80x120 (2:3 aspect, matches the 256x384 source
                              art). Refresh(unlocked,stars,...) picks locked/unlocked/star1/2/3 art (falls
                              back to 3-star art — no dedicated 2-star asset yet). LevelMarker_Unlocked.png
                              /LevelMarker_3stars.png have a static "1"/"3" baked into their own art (not a
                              dynamic level-index number) — a level showing the "3" badge means it was
                              previously 3-starred (real save data, e.g. leftover test progress), not a bug;
                              see FarmFury -> Reset World Map Progress above.
    MatchUpScreen.cs                    — full-screen animal-vs-robot transition, entirely non-interactive
                              (no tap-to-continue, no close button — removed 2026-07-21). Scripted sequence:
                              MatchUpBackground.png backdrop -> LevelHeader1.png pops in (top-anchored inside
                              its own SafeArea, level 1 only — no LevelHeader2/3/... exist, a known accepted
                              gap) -> animal/robot cards (560x560, tilted ±10° outward) slide in from both
                              edges while VS.png (220x220) slams in between them, timed to land exactly as
                              the cards arrive -> 2s hold -> countdown 3/2/1 (each ~2s: pop in, hold, pop
                              out) -> READY! -> fades to opaque black (NOT the whole panel to transparent —
                              see below) -> GameManager.ForceStartLevel(). Cards read fresh from
                              GameManager.GetLevelData(index).birds[0] / .robots[0].robotType on every Show()
                              — different levels show different matchups, not a static splash. Shows
                              "COMING SOON" and returns to the map (no launch) when the level has no
                              LevelData yet. Art sourced from Assets/Sprites/UI/MatchUp/ (dedicated folder,
                              distinct from the HUD's Assets/Sprites/UI/Cards/) — only Cluck/Bessie/Basic/
                              Harvester have art there today, matching L01-L06. **Fade-to-black, not fade-
                              to-transparent:** the screen used to fade its whole CanvasGroup to alpha 0
                              before launching, which revealed the World Map's own background/pins
                              underneath for the fade's duration (it's a sibling inside the same canvas) —
                              looked like "the game navigates back to the map before gameplay loads." Fixed
                              by fading an opaque black Image overlay IN instead; GameManager.ForceStartLevel()
                              then triggers WorldMapController.HidePanel(), which deactivates the whole map
                              canvas (MatchUpScreen included, since it's a child) atomically in one frame —
                              the last thing rendered before that cut is solid black, never stale map/card
                              content (see docs/HISTORY.md Round 15). Art fields are NOT [SerializeField]
                              here — passed into Init() by WorldMapController instead, because nested
                              runtime-built child components never get their Awake() re-fired by the batch
                              "Wire Scene References" editor pass, so any [SerializeField] living directly on
                              them silently never saves (see docs/HISTORY.md Round 11) — put art fields on
                              the top-level persisted component and thread down via Init() for any future
                              nested UI component.
    VideoChromaKey.cs                    — added 2026-07-07. Drives a `VideoPlayer` through the
                              `FarmFury/ChromaKeyVideo` shader onto a full-screen `RawImage`, so a green-
                              screen character/robot clip can play over the frozen game scene with the
                              #00B140 background keyed out (see "Video Chroma Key / Celebration System"
                              below). `[RequireComponent(typeof(VideoPlayer))]`; `FindOrCreate()` is a
                              static factory — finds the scene's existing overlay or builds the whole
                              Canvas(sortingOrder=150)+Background `Image`+`RawImage`+`VideoPlayer` hierarchy
                              from scratch, the same "nothing needs pre-wiring" pattern `HUDController` uses
                              for its own Canvas. Exactly one instance ever exists — shared by
                              `LevelCompleteManager` and `LevelFailedManager` since their celebrations never
                              play simultaneously. `Play(clip, audioClip, plainRender)`/`FadeOut(duration)`/
                              `Stop()` are the only playback API — `plainRender` (added 2026-07-08, see
                              Current Status) skips the chroma-key shader/sky backdrop entirely for clips
                              with their background already baked in, e.g. the current Cluck_Celebration.mp4;
                              owns an `AudioSource` for the accompanying one-shot
                              laugh/taunt sound, started and faded in lockstep with the video. The
                              `Background` `Image` (sky art, sits behind the `RawImage` in sibling/render
                              order) is shown/hidden and faded together with the video too — see the
                              ghosting-bug Known Issues entry above for why it exists.
                              **Updated 2026-07-07** (same ghosting fix): `RawImage` now stays hidden until
                              a new `frameReady`-driven `HandleFrameReady` callback confirms the current
                              clip's first real frame has landed (prevents showing the previous clip's
                              stale `RenderTexture` content); `WaitUntilFirstFrame(timeout)` lets callers
                              avoid burning their on-screen hold timer on decode latency;
                              `HandlePrepared()`'s `RenderTexture` is now created with
                              `RenderTextureReadWrite.Linear` explicitly (this project is Linear colour
                              space — omitting it caused a colour-space mismatch between the sampled video
                              texture and the shader's key colour, the actual root cause of the ghosting);
                              `tolerance` default lowered `0.3 -> 0.20` (re-synced in
                              `SceneSetup.EnsureCelebrationVideoBackground()` every Wire Scene References
                              pass — see the `[SerializeField]` stale-value trap, `tolerance` is a public
                              field so the same risk applies). `HandleError`/diagnostic logging in
                              `HandlePrepared`/`HandleFrameReady` and `SampleRenderTexturePixels()` were
                              added during debugging and left in place.

unity/Assets/Editor/
  SceneSetup.cs           — FarmFury > Wire Scene References; wires all Inspector refs; sets camera
                              (0,0,-10) orthoSize=4.5; launcher at (-2.327,-6.60,0); creates FarmCannon
                              at (-7.54,-5.03,0) scale (1.4711188,1.3868444,1) **only when the GameObject
                              doesn't already exist** (fixed 2026-07-17 — this used to run UNCONDITIONALLY
                              on every Wire Scene References pass, silently reverting any hand-placed
                              reposition; regressed at least twice, see the 2026-07-17 Current Status entry.
                              An existing FarmCannon's transform is never touched here again — move/scale it
                              directly in the Editor and it stays put); ground center (0,-6.85,0) scale
                              (60,0.5,1) -> top at Y=-6.60 (physics-collider-only, no visual — ground/grass
                              visuals are user-authored scene GameObjects or the Background backdrop,
                              not code-generated). **GroundVisual_Placeholder / EnsureGroundVisual()
                              removed entirely 2026-07-17** (it used to recreate a tinted stand-in strip
                              every time it was missing, fighting the user's own deletion of it once real
                              backdrop art existed) — WireAll()'s placeholder-cleanup loop now removes any
                              leftover instance from an older scene save.
                              **`EnvironmentDepthSystem`/`Layer_Midground` removed entirely 2026-07-18**
                              (see Current Status) — used to be a second frozen backdrop layer rendered in
                              front of `Background`, hand-placed/computed multiple times without ever
                              reliably covering the camera at every per-level zoom. `EnsureBackground()`
                              now points `Background`'s own sprite directly at ParallaxMidground.png
                              instead (see below); `CleanupLegacyEnvironmentDepthSystem()` removes any
                              leftover `EnvironmentDepthSystem` GO from an older scene save.
                              **EnsureBackground()**'s `GameObject.Find("Background")` fallback (2026-07-17)
                              no longer matches ANY object named "Background" — scoped to root-level,
                              non-RectTransform objects only, since `VideoChromaKey`'s own celebration-overlay
                              child is also literally named "Background" and the ambiguity could attach a
                              stray `BackgroundController` to that UI Image instead of the real world sky
                              (see Current Status for the "white square" bug this caused).
                              **EnsureLevelCompleteManager()/EnsureLevelFailedManager()/
                              EnsureCelebrationVideoBackground()** (2026-07-07) — create/find each manager's
                              scene GO and wire its video clip from `Assets/Video/` and audio clip from
                              `Assets/Audio/` (paths updated 2026-07-07 when the user split these out of the
                              old `Assets/Sprites/UI/Video_Sound/` — see Current Status) via the shared
                              `WireArrayElement<T>` helper (same shape as `WireAudioClip` above, generalised
                              to any `Object`-derived asset type and array index). `EnsureCelebrationVideoBackground()`
                              must run after `EnsureBackground()` so `Background_SkyV1.png`'s Sprite import
                              settings are already correct by the time it loads it for
                              `VideoChromaKey._backgroundSprite` (also fixed 2026-07-07 — this path was
                              previously hardcoded to a nonexistent `SkyPainting.png`, so the sky backdrop
                              was silently never wired; see Current Status). Also extended
                              `EnsureAudioManager()`/`WireAudioClip()` calls with `_menuMusicClip` the same
                              day (see AudioManager.cs above).
  LevelDataGenerator.cs    — FarmFury > Generate All Level Data; LXX_ filenames must sort alphabetically;
                              header comment documents which levels (L01 only) use the current coordinate
                              system vs. which (L02-L06) are still on the old one
  SpriteWiring.cs           — FarmFury > Wire Sprites; sets per-character PPU; wires pose sprites; keys off
                              folder names under Assets/Sprites/Characters/<ShortName> (must match
                              tools/remove_backgrounds.py's CHAR_MAP short names exactly, e.g. "Cluck" not
                              "Cluck_Chicken" — a folder-name mismatch here silently wires nothing, no error)
  BuildScript.cs             — batch-mode entry points called by Run-Unity.ps1
  EditorAutoSetup.cs          — see "Auto-Compile Pipeline" above
  SpriteAutoImporter.cs        — see "Auto-Compile Pipeline" above
  PanelPreview.cs              — FarmFury > Debug > Run Panel Preview; QA tool added 2026-07-25 since this
                              project has no test framework and no OS-level input automation is available in
                              this environment. Drives GameManager's public state API directly (ForceStartLevel
                              -> FailLevel/CompleteLevel -> SendMessage("OnPauseClicked")) to force the Level
                              Failed/Complete/Pause panels to show in Play mode without playing an actual
                              level, screenshotting each via ScreenCapture.CaptureScreenshot. Must be launched
                              as a real interactive Editor (no -batchmode/-nographics — Play mode needs a
                              rendering Game view), unlike every other Run-Unity.ps1 command. Untested end to
                              end in this environment — the one attempt OOM'd during a cold asset reimport on
                              a 7.7GB-RAM machine before ever reaching Play mode; retry once free memory allows,
                              or run it directly from the Editor's own menu.
```

### Runtime Event Flow

**Level start:**
`GameManager.ForceStartLevel(idx)` (Editor play, and Replay/Restart) or `GameManager.StartLevel(idx)` (menu, full scene load) → `TransitionTo(Playing)` → fires `OnLevelStarted` → `LevelLoader.HandleLevelStarted(data)` → `LoadLevel()` spawns blocks/robots/birds → `ScoreManager.InitLevel()` resets counters → `CatapultLauncher` loads the first bird → `SnapCameraToRest()`.

**Level complete:**
All robots destroyed → `LevelLoader.NotifyRobotDestroyed()` → `_spawnedRobots.Count == 0` → `DelayedLevelComplete()` (2s wait) → `ScoreManager.FinaliseLevel()` → `GameManager.CompleteLevel()` → `TransitionTo(LevelComplete)` → `LevelCompleteManager.HandleStateChanged` runs the celebration sequence (slow-motion → freeze → `CatapultLauncher.LastAnimalUsed`'s video+audio via the shared `VideoChromaKey` → fade) → only then calls `HUDController.ShowLevelCompletePanel()`. `HUDController`'s own `OnStateChanged` no longer shows the panel directly on this transition (see HUDController.cs above).

**Level failed:**
`CatapultLauncher` detects no more birds and calls `LevelLoader.NotifyBirdsExhausted()` → `DelayedLevelFailed()` (1.5s wait) → `GameManager.FailLevel()` → `TransitionTo(LevelFailed)` → `LevelFailedManager.HandleStateChanged` runs the taunt sequence (slow-motion → freeze → the current level's robot taunt video+audio via the same shared `VideoChromaKey` → fade) → only then calls `HUDController.ShowLevelFailedPanel()` (same decoupling as above).

**Replay / Try Again / Restart:** all route through `GameManager.RestartLevel()` → `ForceStartLevel(CurrentLevelIndex)` — the same no-reload path Editor play uses, not a full `SceneManager.LoadScene()`.

### Video Chroma Key / Celebration System (added 2026-07-07)

Green-screen character/robot clips play directly over the frozen game scene between the moment a
level ends and its result panel appearing — Cluck (or the robot) appears to stand in the actual
paused level environment rather than on a plain title card.

- **`unity/Assets/Shaders/ChromaKeyVideo.shader`** — URP unlit UI shader, `FarmFury/ChromaKeyVideo`.
  Keys out `_KeyColor` (default `#00B140`) with a `_Tolerance`-driven soft edge. Includes standard
  Unity UI stencil/clip-rect boilerplate so it still respects a `RectMask2D`/`Mask` ancestor if ever
  placed under one. **Saturation-gated** (fixed 2026-07-07, see Known Issues above) — pixels less
  than half as saturated as the key colour are forced fully opaque no matter how numerically close
  their raw RGB sits to the key, which is what makes plain RGB-distance chroma keying mistake dark/
  desaturated character shading for green in the first place. **Brightness-gated too, added
  2026-07-07** (see the Cluck ghosting Known Issues entry) — the saturation gate alone couldn't
  distinguish genuine dark shadow shading (must stay protected/opaque) from a bright, near-white
  anti-aliased edge-blend pixel right at the character's silhouette (was showing up as a "shiny
  lining" traced around the whole character once the ghosting itself was fixed). `brightWashout`
  now treats sufficiently bright pixels (`value > ~0.75`) as if fully saturated regardless of
  actual saturation, subjecting them to the real distance-to-key check instead of blanket
  protection, while dark desaturated pixels keep full protection unchanged.
- **`VideoChromaKey.cs`** (`Scripts/UI/`) — the runtime driver; one instance ever exists in a scene
  (`FindOrCreate()`), shared by `LevelCompleteManager` and `LevelFailedManager`. Owns the
  `VideoPlayer` (switched to `RenderTexture` mode so its output can feed the shader as `_MainTex`),
  the `RawImage` the shader renders onto, a `Background` `Image` behind it (sky art, fixes the
  ghosting/messy-backdrop issue), and an `AudioSource` for an accompanying one-shot laugh/taunt
  clip. `Play(clip, audioClip)` starts both; `FadeOut(duration)` fades video alpha + backdrop alpha
  + audio volume together over unscaled time (safe to call while `Time.timeScale == 0`); `Stop()`
  hides everything and resets state for the next `Play()`. Note: `VideoPlayer` and `AudioSource`
  both run on their own wall-clock timeline regardless of `Time.timeScale`, which is exactly why
  they can play correctly during the freeze-frame below.
- **`LevelCompleteManager.cs`** / **`LevelFailedManager.cs`** (`Scripts/Core/`) — orchestrate the
  actual sequence per state transition: brief slow-motion (`Time.timeScale` 0.2/0.3 for 0.5s
  real-time) → hard freeze (`Time.timeScale = 0`) → `VideoChromaKey.Play(...)` with the
  animal's/level's clip → hold 4s → `FadeOut(0.3s)` → `Time.timeScale = 1` →
  `HUDController.ShowLevelCompletePanel()`/`ShowLevelFailedPanel()`. Both self-bootstrap onto their
  own scene GO via `CatapultLauncher.Awake()` if missing (same pattern as `AudioManager`), and both
  have their video/audio clip arrays wired by `SceneSetup` (`EnsureLevelCompleteManager()`/
  `EnsureLevelFailedManager()`) from `Assets/Video/` (clips) and `Assets/Audio/` (accompanying
  sounds) — run **FarmFury → Wire Scene References** after adding new clips there. See the Core/ script tree entries above for the exact
  indexing/fallback rules for each manager's clip arrays.

### Key Implementation Rules
- **Input System ONLY:** `using UnityEngine.InputSystem;` — `UnityEngine.Input` is incompatible. Use `Pointer.current` (shared base class for Mouse/Pen/Touchscreen), not `Mouse.current` — this already covers touch input without needing the `EnhancedTouch` module. `Pointer.current.press.wasPressedThisFrame`/`.isPressed`/`.wasReleasedThisFrame`, `pointer.position.ReadValue()` → `Vector2`.
- Blocks spawn `RigidbodyType2D.Static`; first `TakeDamage()` calls `WakeAllStaticBlocks()` → `FindObjectsByType<BlockBase>()` (Unity 6 API). **`RobotEnemy` must also be explicitly `Static`** (set in `Awake()`) — it does not default that way, and a `Dynamic` robot free-falls under gravity from its spawn Y (real bug, fixed 2026-07-18).
- Animals start Kinematic → Dynamic on `Launch(velocity)`.
- Never destroy a physics body inside a collision callback — defer with a coroutine.
- **SpriteRenderer is auto-added:** both `BlockBase.Awake()` and `AnimalBase.Awake()` add one if null — prefabs don't need SpriteRenderer pre-added.
- **[SerializeField] stale value trap:** changing a `[SerializeField]` default in code does NOT affect already-serialised components. Use `private const` for values that must not be overridden.
- **Nested runtime-built components can't rely on the batch "Wire Scene References" pass re-firing their `Awake()`.** If a component is instantiated inside another component's `BuildUI()`/`Awake()`, an editor script that merely *finds* the already-serialized parent GO in a later session will not re-trigger the child's `Awake()`, so any `[SerializeField]` on the child silently never gets wired (no error — see `docs/HISTORY.md` Round 11). Put art/reference fields on the top-level persisted component and thread them down via an explicit `Init(...)` call instead.
- Ground collider: `localScale=(60,0.5,1)`, `BoxCollider2D.size=(1,1)` → world collider 60×0.5, top edge at Y=−2.5 (physics-only; visuals are hand-authored scene GOs, not code-generated). Never set both scale AND size to large values.
- **Effective mass formula:** both dynamic → `(mA × mB) / (mA + mB)`; one static → `movingBody.mass × 0.6`. Damage threshold impulse > 1.5. Damage = `impulse × 1.0` (no multiplier — blocks and robots both use ×1.0).
- **Robot spawn invincibility:** `RobotEnemy.Initialise()` sets `_invincibleUntil = Time.time + 0.8f`; `OnCollisionEnter2D` returns early while invincible.
- **Scenery sortingOrder rule:** decorative props (SceneryBuilder) must use `sortingOrder ≤ 1`. Blocks=2, robots=3, FarmCannon=4, CannonSmoke=5, animals=6. Any tie between two of these falls back to camera Z-distance sorting, which has caused multiple real invisible-object bugs in this project (trebuchet-vs-ground, animals-vs-cannon) — always assign a distinct explicit order for anything new.

### Physics Values
| Entity | mass | bounciness | linearDrag |
|---|---|---|---|
| CluckAnimal | 8 | 0.4 | 0.008 |
| BessieAnimal | 28 | 0.15 | 0.016 |
| WoodBlock | 5×area/std | 0.2 | — |
| StoneBlock | 8×area/std | 0.1 | — |
| RobotEnemy | 20 | 0.15 | — (Static body, doesn't fall) |

Physics2D Gravity Y: −20. Layers: Ground=6, Animal=7, Block=8, Robot=9, Egg=10. Animal `gravityScale`=0.18 (re-tuned for a slower/loopier arc — see `docs/HISTORY.md` Round 10).

### Scene Structure (Game.unity)
```
Main Camera           (0, −2, −10; orthoSize=4.5)
Global Light 2D
GameManager           (DontDestroyOnLoad)
LevelLoader
ScoreManager
HUD
Background              (BackgroundController.cs; SpriteRenderer, sortingOrder=−100. `_skySprite`
                        is ParallaxFarHills.png — a defensive fallback sky, normally fully hidden
                        behind EnvironmentDepthSystem's stack below (Background_SkyV1.png, the
                        original file here, was found deleted from disk 2026-07-18 — see Current
                        Status). `ScaleToFillCamera()`/`LateUpdate()` auto-covers the camera every
                        frame regardless of orthoSize/aspect, unrelated to any of the same-day
                        parallax rebuilds. GroundVisual_Placeholder, which used to sit at
                        sortingOrder=−1, is gone permanently.)
EnvironmentDepthSystem  (EnvironmentDepthSystem.cs; 3 children — Layer_FarHills/Layer_Midground/
                        Layer_Foreground, sortingOrder −40/−30/−20, each ParallaxFarHills/
                        Midground/Foreground.png via the shared Assets/Materials/
                        ParallaxBandClip.mat (FarmFury/ParallaxBandClip shader), each revealing
                        only its own bottom [0, ClipAbove] UV band (theatre-flats stacking) — see
                        the 2026-07-18 Current Status entry for full history/reasoning.
                        RescaleToCamera() re-fits every layer to the camera's ACTUAL current
                        orthoSize/aspect/position on Awake() and every GameManager.OnLevelStarted —
                        NOT frozen/hand-placed, unlike this project's two earlier same-day attempts
                        at a single-painting version of this backdrop. _farHillsClipAbove=1.0 (top
                        layer fully uncropped, its own sky fills the rest) /
                        _midgroundClipAbove=0.55 / _foregroundClipAbove=0.30 are live-tunable
                        Inspector sliders, not baked into the shared material.)
Launcher               (CatapultLauncher.cs; abstract aim-math anchor, no visual meaning)
FarmCannon              (visual launcher; single static SpriteRenderer, Cannon.png, recoils on fire
                        via CannonFireSequence()/RecoilTo(), otherwise stays put — no rotation.
                        Position/scale are likewise hand-placed and frozen — SceneSetup.cs only
                        sets them when first creating this GameObject, never on an existing one.)
Scenery                 (SceneryBuilder.cs; _useExactPlacement=true skips L1)
[L1 scenery GOs]         OldBarn_Right, OakTree, WoodenFence×3, Rock×2, GrassTuft (variable count,
                        user has been adding more by hand), WildFlowers×2, WoodenBarrel×3 (added
                        2026-07-18, clustered near the barn at roughly X=-1.1 to -1.7), GnarledTree
                        (re-added 2026-07-18 at (8.22, -3.94) scale (2.675, 3.602), far right side
                        of the level — removed 2026-07-17 when the windmill silhouette moved into
                        ParallaxMidground.png itself, but the tree shape alone was brought back as
                        a separate hand-placed foreground prop); decorative Haybail×4 were deleted
                        earlier (superseded by gameplay HaybaleBlock instances at the same spots).
                        None of these are code-spawned or code-tracked by name — SceneSetup.cs's
                        cleanup passes never target scenery by name, so hand-added/moved/deleted L1
                        props are always safe from a Wire Scene References run.
BlockParent              (empty holder for code-spawned blocks)
RobotParent              (empty holder for code-spawned robots)
Ground                  (tag="Ground", layer=6; top edge at Y=−6.60; physics collider only)
```

### Prefabs
```
Prefabs/Animals/     CluckAnimal, BessieAnimal, PercyAnimal, WoollyAnimal,
                     DuckyAnimal, HoraceAnimal, GeraldAnimal, BillyAnimal, Egg
Prefabs/Blocks/      WoodBlock, StoneBlock, HaybaleBlock (WoodBlock component + Haybail.png art)
Prefabs/Enemies/     Robot (0.6×0.9 scale, red eye child GOs), HarvesterRobot (custom scale/HP, own art)
Prefabs/Environment/ Ground (static)
```

### Adding a New Animal
1. Create class extending `AnimalBase`; override `Awake()` (set colour/radius/mass before `base.Awake()`), implement `TriggerAbility()`
2. Add prefab to `Prefabs/Animals/`
3. Add `AnimalType` enum value to `LevelData.cs`
4. Handle in `LevelLoader.CreateNextAnimal()` switch expression and `GetAnimalIdleSprite()`, `CatapultLauncher.NextBirdFA()` drag constant
5. Run **FarmFury → Wire Scene References**

### Adding a New Level
Add `LevelData` ScriptableObject in `Assets/ScriptableObjects/Levels/` with filename `LXX_<Name>.asset` (alphabetical order = load order). Run **Wire Scene References**. Or add a `Make(...)` call to `LevelDataGenerator` and run **FarmFury → Generate All Level Data**. Use the current coordinate system (see above) — L01 and L02 are both safe references. Preferred workflow as of 2026-07-09: hand-build the level in the Scene view and use `LevelLayoutDumper` (**FarmFury → Debug → Dump Level Layout To Log**) to generate the `B()`/`R()` calls directly, rather than hand-typing coordinates — see the L02 Known Issues entry above.

---

## Development Roadmap (6 Phases)

- **Phase 1 — Core Feel:** ✅ done (slingshot→cannon aim, trajectory arc, destruction feedback, audio, camera follow).
- **Phase 2 — UI/UX Shell:** ✅ done (HUD, Level Complete/Failed panels, pause/resume toggle, Sunrise Meadows world map, main menu).
- **Phase 3 — Character Roster:** ✅ done (all 8 animals scripted and art-wired).
- **Phase 4 — World 1 Completion:** ✅ done — L01–L18 all exist, no gaps, including a boss level (L18, `RobotType.Commander`). No level validator yet. None of L10-L18 visually verified in Play mode.
- **Phase 5 — Worlds 2–6:** not started (0%).
- **Phase 6 — Polish & Release:** not started. 7 revenue streams per GDD §05 (Rewarded Ads/Starter Pack/Season Pass/Coin Shop/Power-Ups/Cosmetic Shop/Ad Removal). Infra stack **adjusted from the GDD's original §06 plan** as of 2026-07-16 (planning only, nothing built yet — see the Current Status entry above and `monetisation_plan.md` in this project's memory): Supabase (Postgres/Auth/Edge Functions/Storage) in place of Firebase for the economy/data layer, Firebase Crashlytics kept standalone for crash reporting, AdMob + AppLovin MAX + Unity IAP + RevenueCat unchanged from the GDD. Guardrails: no ads in first 20 levels, no pay-to-win, no energy system, cosmetics never randomised. Recommended sequencing: economy/auth foundation first (while save data is still small), then ads, then core IAP + server-side validation together, then season pass/cosmetics, then liveops/leaderboards last.

**Practical read:** World 1 (L01–L18) is fully built with no gaps, redesigned 2026-07-19 against the rebuilt parallax backdrop. World 2's front-end shell (backdrop, landing/map screens, match-up art, robot-tier reskins) also exists as of 2026-07-19, but has zero real levels — building L19+ via the same `LevelLayoutDumper` workflow is the natural next step now that the shell exists to receive them. Neither World has been visually verified in Play mode yet (no Play-mode access in this environment). Monetisation is correctly sequenced last.

---

## Phaser Prototype (`index.html`)

Frozen reference artifact — not under active development. Kept for physics-constant reference only.

### Running
```
npx serve .
# or
python -m http.server 8080
```

### Stack
Phaser 3.60 (CDN), Matter.js (bundled), WebAudio API (procedural sound), localStorage.

### History
`PROGRESS.txt` at the repo root is the prompt-by-prompt changelog for the Phaser prototype (Prompts 1–12). Read it for precise physics constants, level coordinate calculations, and tuning rationale (e.g. why MAX_VEL was reduced from 18→14 in Prompt 12).

### World Constants
| Constant | Value |
|---|---|
| World size | 1400 × 800 px |
| Viewport | 800 × 600 px |
| Ground Y | 770 |
| Gravity | 1.4 |
| MAX_VEL | 14 px/frame |

### File Order (class order matters in this single-file build)
1. Module helpers: `getStoredLevel`, `drawStarShape`, `genHillPts`
2. `LEVELS` array
3. `Block` → `Animal` → `CluckAnimal` → `BessieAnimal` → `Robot`
4. `LevelLoader` → `ScoreManager` → `Trebuchet`
5. `MenuScene` → `SandboxScene` → `new Phaser.Game(...)`

### Physics Critical Notes (Phaser)
- Never destroy a physics body inside a collision callback — use `scene.time.delayedCall(0, ...)`
- `body.blockRef` / `body.robotRef` must be stamped AFTER `setCircle()` (which replaces the body)
- Collision damage: `impulse = relSpeed * effMass * 0.15`, then `dmg = impulse * 2.5`. Threshold: `impulse > 2`
- Static-vs-static skipped (`bodyA.isStatic && bodyB.isStatic`); bird-vs-static-block must register
