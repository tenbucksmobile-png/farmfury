================================================================================
  FARM FURY — Development Progress
  Last updated: 2026-06-22
================================================================================

FILE: index.html (single-file game, no build step)
STACK: Phaser 3.60 (CDN), Matter.js physics (bundled), vanilla JS

WORLD
  Size:         1400 × 800 px
  Viewport:     800 × 600 px
  Camera start: scroll (300, 200) → center at world (700, 500)
  Ground:       y = 770 (static body + visual rect)
  Gravity:      1.5 px/frame² (adjustable with ↑ ↓ keys)

================================================================================
  PROMPT 1 — Physics Sandbox                                        [COMPLETE]
================================================================================
  - Matter.js gravity, ground, side walls
  - Wood blocks: 60×20, mass 10; Stone blocks: 60×30, mass 30
  - Animal circle placeholder (radius 18)
  - Generated textures via make.graphics
  - Grid background, FPS counter, gravity HUD

================================================================================
  PROMPT 2 — Block Health System                                    [COMPLETE]
================================================================================
  - Block class; body.blockRef back-reference (NOT body.gameObject)
  - HP bar (green→orange→red), crack overlay at 50% HP
  - 5 triangle fragments on destroy; blockDestroyed event
  - Impulse-based damage; delayedCall(0) for body removal

================================================================================
  PROMPT 3 — Trebuchet Launcher                                     [COMPLETE]
================================================================================
  - Base world (420,770); arm tip ~(352,634)
  - Drag AWAY from tip to pull sling; release fires
  - Step-simulated trajectory arc (20 dots, exact Matter.js physics)
  - Arm tween: Power3 snap + Bounce.Out return
  - Camera follow (lerp 0.08), pan back to (700,500) after 2s

================================================================================
  PROMPT 4 — CluckAnimal + Animal Base Class                        [COMPLETE]
================================================================================
  - Animal base; CluckAnimal extends Animal
  - static FA = 0.008 shared with Trebuchet.PROJ_FA
  - Cluster Bomb: 5 eggs at ±60°/±30°/0°, speed = max(v×0.6, 5)
  - body.eggRef = true stamped AFTER setCircle (new body replaces old)
  - Eggs bypass global impulse threshold via direct takeDamage calls
  - White flash 120ms; crack particle 6-ray 9-step fade
  - Contact timer: 3s after first collision → destroy

================================================================================
  PROMPT 5 — Robot Enemies                                          [COMPLETE]
================================================================================
  - Robot class: 30×40 texture (#444441, red eyes, grill)
  - body.robotRef (no setCircle — rect body kept); HP 150
  - Red flash (separate Graphics), 6 square fragments, robotDestroyed event
  - Global handler: eggRef guard prevents double damage
  - scene.robots[], removeRobot(), spawnRobot()

================================================================================
  PROMPT 6 — LevelLoader + 3 Levels                                [COMPLETE]
================================================================================

  LEVEL DATA FORMAT
  { id, name, par_birds, birds:[], blocks:[{type,x,y,w,h}], robots:[{x,y}] }
  Defined as LEVELS constant at top of file (before all classes).

  THREE LEVELS
  W1_L01 "First Contact" (par 1, 3 birds)
    · 4 wood 60×20 blocks stacked: x=800, y=490/470/450/430
    · 1 robot at (800, 510)

  W1_L02 "Stone Wall" (par 2, 3 birds)
    · 3 stone 60×30 at x=760/820/880, y=490 (wall)
    · 1 wood 60×20 at (820, 460) on top of center stone
    · 1 robot at (820, 540) behind wall

  W1_L03 "The Tower" (par 3, 4 birds)
    · Wood 120×20 at (820, 490) — wide base
    · Stone 120×20 at (820, 470) — wide middle
    · Wood 80×20 at (820, 450) — narrower top
    · Robot at (760, 510) beside tower
    · Robot at (820, 430) on top

  BLOCK._ENSURETEXTURE(scene, type, w, h) — static
    · Standard 60×20 wood → 'wood'  (pre-built, no overhead)
    · Standard 60×30 stone → 'stone' (pre-built)
    · Any other size → generates 'type_WxH' texture on first use
    · Wood pattern: grain lines every 15px; Stone: mortar scaled to w/h
    · Mass and maxHP scaled proportional to area vs standard size

  LEVELLOADER CLASS (between Robot and Trebuchet in file order)
    loadLevel(idx)
      · _clearLevel() → scoreManager.initLevel() → spawn blocks/robots
      · birdsLeft=[...data.birds] → _buildQueueDisplay() → reset camera
    _clearLevel()
      · destroy activeAnimal, all blocks (img+crackGfx+hpGfx), all robots
      · _destroyQueueDisplay(); birdsLeft = []
    _destroyQueueDisplay()
      · destroys _baleGfx + _queueCircles only — does NOT touch birdsLeft
    _buildQueueDisplay()
      · Hay bale at screen (70,554): brown #7A5218, width=count×28+14
      · Per-bird Graphics circles at local (0,0), positioned at screen coords
    consumeBird()
      · Shifts birdsLeft; tweens first circle → trebuchet tip
      · Slides remaining circles left; fades bale after last bird

================================================================================
  PROMPT 7 — ScoreManager + Full HUD + Level Screens               [COMPLETE]
================================================================================

  SCOREMANAGER CLASS (between LevelLoader and Trebuchet in file order)

  Scoring
    · Robot destroyed: +1000 pts
    · Wood block destroyed: +100 pts
    · Stone block destroyed: +200 pts
    · Egg hit: +50 pts
    · Level complete bonus: birdsRemaining × 500 pts
    · Stars: 1=all robots dead; 2=all robots+1 bird left; 3=all+2+ birds left
    · High score and best stars persisted per level to localStorage
      key: 'farmfury_best_N' → { score: N, stars: N }

  Level complete trigger
    · robotDestroyed event: if robotsDestroyed >= totalRobots → _levelEnded=true
      + delayedCall(2000) → _triggerLevelComplete()
    · _triggerLevelComplete: adds bird bonus, saves to localStorage, shows screen

  Level fail trigger
    · update() polls each frame: if !activeAnimal && !hasMoreBirds && robots remain
      → _levelEnded=true + delayedCall(1500) → _showLevelFailed()

  State tracking
    · initLevel(idx): resets all counters, cancels pending _endTimer, clears overlay
    · onBirdFired(): decrements birdsRemaining, redraws HUD icons
    · onEggHit(x,y): +50 pts (guarded by _levelEnded)
    · update(): fail-condition poll (skips if _levelEnded or totalRobots=0)
    · _levelEnded flag: prevents double-triggering complete/fail

  HUD (top bar, 56px, scroll factor 0, depth 19-21)
    · Left: levelNameText — white 13px, at screen (12, 11) — "W1_L01  First Contact"
    · Centre: "SCORE" label 10px grey at (VW/2,5); score value 26px white at (VW/2,17)
      Score pulses scale 1→1.35→1 (80ms) on each award
    · Right: bird icons (yellow circles r=9, orange beak) drawn right-to-left from
      x=VW-23; updates on each consumeBird via _birdHudGfx.clear()+redraw
    · FPS: tiny 10px grey text at (VW-4, VH-38), above hint bar
    · Hint bar: gravity value integrated into bottom bar text (no separate gravText)

  Floating score text
    · "+N" in gold #FFD700, stroke black, spawned at world→screen coordinates
    · Tweens y-55 + alpha=0 over 950ms; depth 25

  Level complete screen (depth 30-33, inputBlocked=true)
    · Black overlay 62% opacity full-screen
    · "Level Complete!" in orange #FF8C00, 34px
    · Three 5-pointed star polygons (outer r=32, inner r×0.42) centered at
      VW/2±90 y=188 — filled #F5C542 if earned, dark outline if not
    · Bonus line: "+N birds remaining ×500 bonus" in green (if birdsRemaining>0)
    · Score + Best (or "★ New Best" in gold if new record)
    · Buttons (Phaser interactive Graphics, fillRoundedRect r=8, hover highlight):
      "Next Level" orange at (VW/2-88, 358) 155×42 → loadLevel(currentIdx+1)
      "Retry" grey at (VW/2+88, 358) 110×42 → loadLevel(currentIdx)
      If last level: "Next Level" label becomes "First Level" (wraps back)

  Level failed screen
    · Black overlay 65% opacity
    · "Level Failed" in red #FF3333, 36px
    · "All birds used — no robots destroyed" subtitle
    · Score shown in grey
    · "Retry" button in red at (VW/2, 328)

  inputBlocked flag (SandboxScene.inputBlocked)
    · Set true by _showLevelComplete/_showLevelFailed
    · Cleared by _clearOverlay (called by initLevel on each loadLevel)
    · Checked in Trebuchet.setupInput pointerdown and pointerup
    · Checked in scene's pointerdown wood-spawn handler
    · N/R keyboard shortcuts bypass flag (intentional: always work)

  Overlay cleanup
    · All overlay objects stored in _overlayObjects[]
    · _clearOverlay() destroys all, sets inputBlocked=false
    · Called by initLevel() → triggered by any loadLevel() call
    · 50ms delayedCall before loadLevel in button callbacks (prevents
      pointerup/pointerout firing on already-destroyed Graphics objects)

  TECHNICAL NOTES (Prompt 7)
    · ScoreManager created AFTER trebuchet but BEFORE levelLoader in create(),
      so scoreManager exists before first loadLevel(0) call
    · LevelLoader.loadLevel calls scoreManager.initLevel(idx) first (before
      spawning objects), so counters are clean before new level data arrives
    · _endTimer stored as TimerEvent; .remove() cancels it in initLevel
    · Phaser fillPoints(pts, true) / strokePoints(pts, true) used for star polygon
    · fillRoundedRect(x, y, w, h, 8) for button backgrounds
    · setInteractive(new Phaser.Geom.Rectangle(...), Phaser.Geom.Rectangle.Contains)
      on Graphics for button hit detection (no HTML elements)

================================================================================
  PROMPT 8 — Levels 4 & 5 + Full Polish Pass                        [COMPLETE]
================================================================================

  NEW LEVELS

  W1_L04 "Egg Practice" (par 2, 4 birds — all cluck)
    · 5 wood 50×30 blocks: x=720/770/820/870/920, y=490 (edge-to-edge wall)
    · 4 robots at x=745/795/845/895, y=510 (tucked behind/between block seams)
    · Strategy: direct shots hit the wall; egg scatter ability clears gaps

  W1_L05 "The Fortress" (par 3, 5 birds — all cluck)
    · Stone outer wall: 3 × 20×80 blocks at x=760, y=490/410/330 (tall pillar)
    · 3×3 wood grid (40×40 blocks): x=820/860/900, y=490/450/410 (inside)
    · 3 robots at x=820/860/900, y=510
    · Strategy: breach stone pillar then collapse wood grid, or arc eggs over

  Block._ensureTexture extended for 50×30 wood and 40×40/20×80 stone/wood sizes.
  All sizes scale mass and maxHealth proportional to area vs standard.

  BACKGROUND + PARALLAX

  Removed old grey rectangle and debug grid from SandboxScene.create().

  _buildBackground() adds (all scrollFactor 0):
    · Sky: #7EC8E3 fill + subtle upper tint #5BACE0 at 22% opacity, depth -5
    · Far hills: #6B8F47 sine-wave polygon (genHillPts -400..1400, baseY=336, amp=38)
      depth -3; shifts at 12% of camera scrollX in update()
    · Near hills: #4A7A2B polygon (baseY=442, amp=26) depth -2; shifts at 35% scrollX

  genHillPts(x0, x1, baseY, amp, phase) — module-level helper
    · Returns closed polygon (starts/ends at VH+10 for fill)
    · Two overlapping sine waves: sin(x/170+phase) + 0.45×sin(x/80+phase×1.6)

  Parallax in update(): farHillGfx.setPosition(-scrollX*0.12, 0), nearHill 0.35

  MenuScene uses same genHillPts for static (non-parallax) background.

  MENU SCENE

  MenuScene class (before SandboxScene in file order).
  Scene key: 'MenuScene' — first scene in Phaser.Game.scene array.

  Layout:
    · Sky + 2 hill layers + ground strip + 48% dark overlay
    · "FARM FURY" — monospace 52px orange #FF8C00, stroke #000 6px, centered y=44
    · "Select a Level" — 17px grey, y=108
    · 5 cards: 128×168, gap 12px, centered row; cards center at y=320
    · Footer: "Click a card to play" 11px at bottom

  Each card (_makeCard):
    · Background: dark blue-grey #1E2E3E, rounded 10px; border #3A5A7A
    · Hover: #3A5A7A fill, #FF8C00 border
    · Level ID (10px #7799BB), level name (12px white, word-wrap)
    · 3 stars via drawStarShape() — filled gold if earned vs dark outline
    · Best score (gold if > 0, grey dash if none)
    · Par info: "par: N bird(s)"
    · Orange "PLAY" strip at card bottom, highlights #FFAa22 on hover
    · Card.setInteractive(Geom.Rectangle) → scene.start('SandboxScene', {levelIdx})

  SandboxScene.init(data) reads levelIdx and passes it to loadLevel(_startLevelIdx).
  'M' key in SandboxScene → scene.start('MenuScene').
  'Menu' button added to both level-complete (3 buttons) and level-failed (2 buttons)
  overlay screens.

  SHARED MODULE-LEVEL HELPERS

  drawStarShape(g, cx, cy, r, earned)
    · 10-point polygon (5 outer r, 5 inner r×0.42)
    · Used by both ScoreManager and MenuScene (no duplication)

  getStoredLevel(idx)
    · localStorage.getItem('farmfury_best_N') → { score, stars }
    · Used by ScoreManager._saveStored/getStoredLevel and MenuScene card rendering

  LEVEL COMPLETE SCREEN UPDATED (3 buttons)
    · "Next Level" orange 0xFF8C00 at cx=220, w=148, h=42
    · "Menu" blue-grey 0x334466 at cx=400, w=108, h=42
    · "Retry" grey 0x555566 at cx=578, w=112, h=42
    · Layout shifts -16px vertically when no bird bonus (dynamic)

  LEVEL FAILED SCREEN UPDATED (2 buttons)
    · "Retry" red 0xFF3333 at cx=VW/2-85, w=140, h=44
    · "Menu" blue-grey at cx=VW/2+85, w=120, h=44

  SOUNDS

  Robot._playDeathSound(): sawtooth sweep 500→150 Hz over 200ms, gain 0.18→0.
    Called immediately in Robot.explode() after destroyed=true.

  SandboxScene._playThud(impulse):
    · Sine wave 75–100 Hz random, 60ms, vol = min(0.22, impulse/700)
    · Throttled 85ms between calls via this._lastThudTime
    · Triggered in collision handler when impulse > 14 and either body is a Block

  Both sounds: raw WebAudio API (OscillatorNode + GainNode), try/catch guarded.

  SCREEN SHAKE

  cameras.main.shake(150, 0.004) on any collision with impulse > 50.
  Throttled: min 180ms between shakes via this._lastShakeTime.
  Prevents spam from multi-body pile-ups.

  WINDOW RESIZE (Phaser Scale)

  Phaser.Game scale: { mode: Phaser.Scale.FIT, autoCenter: Phaser.Scale.CENTER_BOTH }
  CSS: html,body { height:100%; margin:0; overflow:hidden; background:#111; }
  Canvas fills and centers in browser window at any aspect ratio.

  SCENE LIFECYCLE

  SandboxScene.init(data): sets _startLevelIdx; resets all mutable state cleanly.
  SandboxScene.shutdown(): calls scoreManager.cleanup() (cancels pending timer).
  ScoreManager.cleanup(): removes _endTimer + clears overlay (safe before scene stop).

================================================================================
  NEXT — Not Yet Started
================================================================================
  Prompt 9+: TBD

================================================================================
  TECHNICAL NOTES (cumulative)
================================================================================
  - body.blockRef / body.robotRef — safe custom refs; body.gameObject Phaser-owned
  - setCircle replaces physics body — stamp body.* AFTER
  - body.robotRef assigned directly (rect body, no setCircle)
  - delayedCall(0) for body removal — never inside collision callback
  - Egg effective mass 2.3 < impulse threshold → explicit takeDamage in egg handler
  - eggRef guard in global handler prevents double damage
  - destroy flash = separate Graphics (outlives destroyed physics body)
  - Camera pan center: world (700,500) = scroll (300,200) + viewport (400,300)
  - instanceof in closures: resolved at runtime, safe for forward class refs
  - Per-bird Graphics circles at (0,0) locally, positioned via setPosition —
    tweening .x/.y moves them in screen space (scroll factor 0)
================================================================================
