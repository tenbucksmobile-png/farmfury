# FarmFury — Development History

This is the round-by-round debugging/audit log for the Unity port (`unity/`), moved out of
`CLAUDE.md` to keep that file a lean architecture reference. Read this when you need the
reasoning/root-cause behind a current value or convention in `CLAUDE.md` — it explains *why*,
not just *what*. `CLAUDE.md` always reflects current truth; this file is the narrative that got
there and is not kept "correct" retroactively — later rounds supersede earlier ones where they
conflict.

---

## Audit Findings (2026-07-01)

A full audit was run against CLAUDE.md, the GDD, and the actual project state. This block is the
standing summary of that audit — re-verify anything here before relying on it if significant
time has passed.

**Critical bug — L02–L06 spawn floating in mid-air.** `LevelDataGenerator.cs` still carries a header comment for the *pre-rebuild* coordinate system (`ground = -2.5`, `launcher X = -5.5`) and L02–L06 were generated under that system (block/robot Y ≈ −1.5 to −2.3). The live scene was recalibrated to `ground Y = −6.60`, `launcher X = −2.327` (see Phase 4), but **L02–L06's baked Y values were never migrated** — `LevelLoader.SpawnBlock`/`SpawnRobot` place objects at the raw stored position with no offset applied, so these five levels currently spawn their entire structure ~4.4–4.9 units above the actual ground, disconnected from it, and their X positions are calibrated to the old launcher position rather than the current one. **Only L01 (hand-edited directly in the asset, not via the generator) uses correct current-system coordinates.** L02–L06 need their baked positions recalculated (roughly: old Y − 4.1, and X re-checked against the new launcher at X=−2.327) before they're playable. Still true as of the latest check (2026-07-19, Round 12) — `LevelDataGenerator.cs`'s L02–L06 `Make()` calls still use Y ≈ −0.9 to −2.3, the old system. Treat L02–L06 as non-functional despite existing as assets.

**`LevelDataGenerator.cs` L01 landmine — RESOLVED 2026-07-01.** The generator's L01 entry was rewritten to match reality and extended with a 6-bale hay-pile obstacle (see "L01 gameplay pass" below); it's now safe to run **FarmFury → Generate All Level Data** again — it reproduces the current L01 design instead of destroying it. L02–L06 are untouched and still on the old coordinate system (see bug above) — regenerating will not make them worse, but won't fix them either.

**L01 gameplay pass — 2026-07-01.** Fixed two real bugs found while wiring up the drag/aim/pass-through mechanics against the user's request:
1. `CatapultLauncher.DrawTrajectory()` was fully implemented but **never called** — the dotted aiming-arc preview (`_trajDotRenderers`, built in `Awake()`) never actually drew during drag. Fixed: now called from the drag-hold branch of `HandleInput()`, and its start position corrected from the fixed rest `_launchPoint` to `BucketWorldPos(_dragAngle)` (the actual point `Fire()` spawns the animal from).
2. `LevelLoader.SpawnBlock()` never applied `BlockSpawnData.passThrough` / `.healthOverride` / `.massOverride` to the spawned block — `WoodBlock.cs`'s own comment claimed LevelLoader did this; it didn't. This silently broke the Cluck pass-through mechanic for any level using it. Fixed: added `BlockBase.ApplyOverrides(health, mass)`, called from `SpawnBlock()` along with setting `WoodBlock._passThrough` directly.

Added a new `BlockType.Haybale` (enum value 2, `LevelData.cs`) and a `HaybaleBlock.prefab` — mechanically identical to `WoodBlock` (same component, `SceneSetup.EnsureHaybaleBlockPrefab()` creates it if missing), just wired to render `Assets/Sprites/Environment/World1Props/Haybail.png` instead of the generic wood-plank art. **No new art was needed** — `Haybail.png` was already imported (used for the decorative scenery), and `BlockBase`'s destruction visuals (procedural crack overlays at 50%/25% health, tint shift, 4 fading fragments) are entirely sprite-agnostic, so haybale destruction "just works" the same as any other block once the sprite is wired.

**L01 now has an actual obstacle**, not just a bare robot: 4 haybale blocks (`passThrough=true`, `healthOverride=60`, `massOverride=3` — lighter/weaker than standard wood so the tutorial stays easy). Positions were originally guessed, then corrected in the round-2 pass below to match the user's real hand-placed decorative haybales exactly. Requires the user to run **FarmFury → Wire Scene References** once (materializes `HaybaleBlock.prefab` and wires `_haybalePrefab` into `LevelLoader`) before it'll spawn correctly.

**Visual-feedback round 2 — 2026-07-01, from user screenshot.** Play-test report: HUD cards overlapping the screen edge, trebuchet arm launch looking wrong, no visible Cluck in flight, HarvesterRobot floating in mid-air instead of nestled behind the hay, plus a request to use the real hay-pile positions instead of the guessed ones. Diagnosed and fixed by reading `Game.unity` directly (no render/play-test access from this side):
- **Robot floating — root cause found.** `RobotEnemy.Awake()` sets `BoxCollider2D.size=(1,1)`, sized for the default `(0.6,0.9)` scale. L01's HarvesterRobot uses a custom `(4.3565,4.69371)` visual scale for a bigger, more imposing enemy, but nothing shrunk the collider to compensate — it inherited the full scale, becoming a ~4.36×4.69-unit hitbox that overlaps the ground (Y=−6.60) by roughly 2 units at spawn. Unity's physics forcefully separates deep overlaps on the first frame, launching the robot upward. Fixed in `LevelLoader.SpawnRobot()`: when a custom scale is applied, the collider size is now re-derived (`0.6/scaleX, 0.9/scaleY`) to keep the world-space hitbox pinned at the original 0.6×0.9 regardless of visual scale.
- **Trebuchet likely invisible — root cause found.** The hand-placed `Trabuchet_Body`/`Trabuchet_Arm`/`Trabuchet_Swing` scene GameObjects were never given a `sortingOrder` (default 0), tied with Ground/scenery (also ~0). `Trabuchet_Body` additionally sits at `Z=2`, deeper than Ground's `Z=0`. Unity's default 2D sort falls back to camera distance on sortingOrder ties, so the trebuchet was very likely rendering behind the terrain. Fixed in `SceneSetup.WireLauncher()`: now sets sortingOrder 3/4/5 (body/arm/swing) and flattens Z to 0. Self-heals on the next **Wire Scene References** run.
- **Hay pile repositioned to the user's real placement.** Read the exact transforms of the 4 hand-placed decorative `Haybail`/`Haybail (1-3)` GameObjects directly from `Game.unity` (positions ~X 3.65–4.31, Y −4.88 to −5.65) and used those exact coordinates for the gameplay `HaybaleBlock` entries, replacing the earlier guessed grid. **The 4 decorative scene GOs should be deleted** once this is live (done in Round 6, below).
- **HUD safe-area gap.** Score/bird-queue/pause elements were anchored to the raw screen rect with no accounting for device notches/rounded corners. Added a `SafeArea` RectTransform wrapper (`Screen.safeArea`-driven) in `HUDController.BuildCanvas()`.
- **Trebuchet arm position/scale not sticking in the Scene view — by design, not a bug.** `CatapultLauncher.DrawArmAt()` forcibly repositions arm/swing sprites to the pivot-derived position every frame at runtime. Manually dragging in the Scene view outside Play mode is edit-time-only reference positioning. (Note: this whole trebuchet-arm subsystem was removed in the 2026-07-02 cannon swap, below — kept here for historical context only.)

**Visual-feedback round 3 — 2026-07-01, from 3 more user screenshots.** User: "stop guessing." Diagnosed by pixel-measuring the actual sprite art and by numerically integrating the actual flight physics.
- **Arm/swing pivots were genuinely wrong — precisely re-measured and fixed.** `Trabuchet_Arm.png`'s bolt hole is centred at pixel (41, 29) of 183×203px, i.e. sprite pivot **(0.224, 0.857)** — not the previous guessed (0.40, 0.56). `Trabuchet_Swing.png` (512×512px) had no custom pivot at all despite being positioned at the arm tip every frame; fixed to pivot **(0.434, 0.828)**.
- **Trajectory arc was landing at X≈7-9, not X≈5.16 as the old code comment claimed.** Numerically integrated the actual flight rather than trusting the stale comment. Recalibrated to speed **6.0-7.0 m/s**, angle **48°-42°**: min pull lands ~X 3.1, max pull lands ~X 4.9. (Superseded by Round 10's further recalibration to 4.0-4.9 m/s / 58°-52°, below.)
- **HarvesterRobot moved from X=5.15509 to X=4.8** — the old position was at/past the edge of the camera's visible frame.
- **Haybale HP dropped from 60 to 10** — 60 HP was surviving multiple hits instead of one-shotting.
- **HarvesterRobot HP raised from 35 (class default) to 40**, set specifically on `HarvesterRobot.prefab` only (not the shared `RobotEnemy` class default, which would've affected `Robot.prefab` too).

**Trebuchet → Farm Cannon visual swap — 2026-07-02.** User-requested visual-only replacement; aiming math (drag detection, `LaunchVelocity()`, `DrawTrajectory()`'s physics) explicitly required to stay unchanged.
- **Removed:** `DrawArmAt()`, `ArmSnap()`, `BuildTrebuchetBody()`, `UpdateCounterweight()`, `ShortArmEnd()`, `BucketWorldPos()`; all trebuchet sprite/GO fields; the counterweight-pendulum state; dead arm-length constants.
- **Kept unchanged:** `PivotPos()`, `_pivotHeight`, `_armRestAngle`, `MaxLoadAngle` (now purely abstract aim-math), `LaunchVelocity()`, `DrawTrajectory()`'s physics loop, the trajectory dot pool, `_rubberBandLine`.
- **Added:** `BuildCannon()`, `BuildSmokeParticles()`, `SpawnSmoke()`, `CannonFireSequence()` + `RecoilTo()` coroutines (no DOTween in this project — plain coroutine tween), `CannonBarrelOffset`, `CannonLoadedBirdOffset`.
- `SceneSetup.WireLauncher()`: deletes `Trabuchet_Body`/`Arm`/`Swing` scene GOs if found, creates/positions `FarmCannon`, wires `Cannon.png`. sortingOrder=4 set explicitly (Z alone isn't enough — Unity's 2D sort only falls back to Z-distance on sortingOrder *ties*).
- Not visually verified at the time — recoil/smoke/offset numbers came directly from the user's spec.

**Cannon position bug + ParticleSystem error — 2026-07-03.**
1. **`ParticleSystem.main.duration` threw at runtime.** `AddComponent<ParticleSystem>()` starts playing immediately — configuring `main` right after hit a live system. Fixed: `_smokePS.Stop(true, ParticleSystemStopBehavior.StopEmittingAndClear)` immediately after `AddComponent`, before touching any module.
2. **A duplicate cannon appeared in-game.** The user had manually placed a second, plain SpriteRenderer-only GameObject named `"Cannon"` (not `"FarmCannon"`) directly in the scene, at what turned out to be the "correct" placement (X=-3.0012, Y=-5.1223, Z=0, scale 1.4711188×1.3868444). `BuildCannon()` searches for `"FarmCannon"` specifically, found none, and created its own at the old default — hence two cannons. Fixed: deleted the stray `"Cannon"` GO, and updated both `CatapultLauncher.BuildCannon()`'s and `SceneSetup.WireLauncher()`'s default creation position/scale to the user-verified values **(-3.0012, -5.1223, 0), scale (1.4711188, 1.3868444)** — this is the value still live in both files as of Round 12; treat as ground truth.
3. **Robot "still in the wrong position" — a knock-on effect of the cannon move, not a separate bug.** `_launchPoint = cannon position + CannonBarrelOffset` moved with the cannon; re-integrating showed max-pull overshooting to X≈5.75 against a robot at X=4.8. Retuned `LaunchVelocity()`'s speed range to 5.5-6.5 m/s so max pull again lands at X≈4.9. (Superseded again by Round 10.)
4. Flagged but not resolved this round: whether the barrel offsets still visually land on the barrel mouth now that the cannon sprite is ~33% smaller than the swap round's assumption.

**Broken scene PPtr + sortingOrder tie — 2026-07-04.**
- **`Game.unity` had a broken PPtr** after the Cannon-duplicate deletion above — removing the GameObject/SpriteRenderer/Transform blocks by hand missed that Unity's internal `SceneRoots.m_Roots` array still listed that Transform's fileID. **Lesson: when hand-deleting a root-level GameObject from a `.unity` file, also grep for its component fileIDs across the whole file afterward.**
- **All 8 animal subclasses overrode `_sr.sortingOrder = 4` in their own `Awake()`**, tying exactly with `FarmCannon`'s `sortingOrder=4` — ties fall back to Z-distance, so birds could render behind the cannon both loaded and in flight. Fixed to `sortingOrder = 5` at the time (moved to 6 in Round 7).
- Added `AnimalBase.SetInFlightPose()` — sets `_sprInFlight` without touching physics/launch state, used by `PrepareNextBird()` instead of the seated loaded pose.

**Round 4 — 2026-07-04. Smoke texture + robot Y-clipping (both confirmed by computation); chicken/fire still unresolved.**
- **Smoke rendered as squares.** `BuildSmokeParticles()` assigned a shader Material with no texture — a bare quad with no texture renders as a plain square. Added `MakeSoftCircleTexture()` (soft radial falloff) as `mat.mainTexture`.
- **Robot "still outside the screen" — X was never the problem; camera math was.** Confirmed via a scenery object visible at X=7.24 that the camera's horizontal range covers X=4.8. Computed the vertical viewport instead: camera rest Y=-2, orthoSize=4.5 → visible Y range -6.5 to +2.5. Ground line is at Y=-6.60 — only 0.1 units below the camera's own bottom edge. Measured the robot sprite's real art bounds and found it extended 0.23 units below the camera's bottom edge; raised to Y=-5.8215. **This camera/ground-line relationship (0.1u clearance) affects any future tall ground-standing enemy in World 1, not just this one robot.**

**Round 5 — 2026-07-05. `_eggPrefab` UnassignedReferenceException.** Confirms firing/rendering from Round 4 actually works (the crash trace shows a full launch → flight → tap-to-trigger sequence; the failure was purely in egg spawning). Root cause: `SceneSetup.EnsureEggPrefab()` assigned `eggGO.GetComponent<EggProjectile>()` to `CluckAnimal._eggPrefab`, but that field is typed `GameObject` — a genuine type mismatch. `SerializedProperty.objectReferenceValue` silently rejects the wrong type rather than erroring. Fixed the wiring code to assign `eggGO` directly, and hand-fixed the already-broken `CluckAnimal.prefab` asset directly.

**Round 6 — 2026-07-06/07. Decorative haybails removed, smoke obscuring the flight path, robot Y insufficient (2nd attempt).**
- **Smoke sortingOrder was covering the flying bird.** At `sortingOrder=6` (above animals at 5), a 1.2s smoke cloud against a ~1.3-1.4s flight would cover the bird nearly the whole time. Dropped to `sortingOrder=1`. (Revised again in Round 7 — this made smoke invisible behind the cannon.)
- **Robot Y=-5.8215 (Round 4's fix) was still insufficient.** Re-verified camera numbers directly against the saved scene. Halved the robot's scale (4.3565×4.69371 → 2.17825×2.346855) and moved to Y=-5.5607. (Superseded again in Round 7 — user gave much larger ground-truth values; the "shrink it" instinct here was backwards.)
- **Deleted all 4 decorative `Haybail`/`Haybail (1-3)` scene GameObjects** — superseded by the gameplay `HaybaleBlock` instances at the same positions. Proactively cleaned up `SceneRoots.m_Roots` for all 4 fileIDs (learned from the Round 4 dangling-PPtr incident).

**Round 7 — 2026-07-08. `CameraShake` bug found (real, confirmed); smoke/cannon sortingOrder conflict (self-inflicted by Round 6); robot Y/scale fix STILL not enough — abandoned further guessing in favour of asking the user for exact values.**
- **"Screen shifts upward as haybails are destroyed" — confirmed real bug.** `CameraShake.DoShake()` did `cam.transform.position += <random point>` every frame with no corresponding subtraction — every call permanently random-walked the camera. Fixed to track the shake's own contribution as a delta each frame and subtract it back out, netting exactly zero.
- **"No smoke comes out of the cannon anymore" — self-inflicted by Round 6.** Moving smoke to `sortingOrder=1` put it behind the opaque cannon (order 4). Animals moved to **6**, smoke took **5** — current live values.
- **Robot Y/scale — resolved 2026-07-09 with user-provided exact values: position (5.6, -5.25), scale (5.7065, 7.009).** Notably *larger* than any of Claude's own attempts — confirmed the "shrink it to fit" instinct from Round 6 was backwards. (Updated once more in Round 10 — current value is in CLAUDE.md.)
- **Chicken invisible — root cause found in Round 8**, not explained by any sortingOrder fix.

**Round 8 — 2026-07-10. ROOT CAUSE FOUND: every character sprite, for all 8 characters, was imported in Multiple sprite mode.** User pointed at `Cluck_InFlight.png` "not allowing me to drop into scene." Its `.meta` showed `spriteMode: 2` (Multiple), auto-sliced into 9 disconnected sub-sprites; `CluckAnimal.prefab`'s `_sprInFlight` was wired to a 53×8px sliver fragment, not the actual 294×302px art. This is the real explanation for "chicken invisible" — not the sortingOrder theories from Rounds 4-7 (those were real bugs too, just not *this* bug).
- **Scope: every pose, every character** — 100% of pose PNGs were Multiple mode.
- **Root cause of the root cause:** `SpriteAutoImporter.cs`'s character-sprite import branch was the only one that never enforced `SpriteImportMode.Single`. Fixed.
- **Compounding bug: the auto-heal safety net had been silently dead the whole time.** `EditorAutoSetup.AutoWireCharacterSprites()` gated on a sentinel file `Cluck/Loaded.png` that never existed (actual file: `Cluck_Loaded.png`), so the guard always short-circuited. Fixed the path, broadened the trigger condition.
- **Confirmed live:** user reported the "Create New Animation" dialog when dragging character sprites — Unity's own tell for Multiple-mode sheets.
- **2026-07-11, once fixed and re-tested: `BirdScale`/`CannonLoadedBirdOffset` needed retuning** since both were calibrated against the broken sliver. `CannonLoadedBirdOffset = (0.6212, 0.4223)` is still current. (`BirdScale` retuned again in Round 10.)
- `ForceReimportAll()` had no access modifier (defaulted `private`) but was called cross-class — `CS0122`. Made `public`.

**Round 9 — 2026-07-12. SECOND, deeper root cause: character sprite folders were misnamed, so `SpriteWiring.cs` was wiring nothing.** After the Round 8 fix, the bird rendered as "a yellow dot" (AnimalBase's procedural-circle fallback, tinted by CluckAnimal when `!HasRealSprites`). Root cause: `unity/Assets/Sprites/Characters/` contained `Cluck_Chicken/` and `Bessie_Cow/`, not `Cluck/`/`Bessie/` — `SpriteWiring.cs`'s dictionaries are hardcoded to the short names, so `AssetDatabase.FindAssets` found nothing and every sprite field silently stayed null. Confirmed the short-name convention is intended (`tools/remove_backgrounds.py`'s `CHAR_MAP` maps long→short). Fixed by renaming the folders (`.meta` files moved along to preserve GUIDs).

**Round 10 — 2026-07-13/15. Cannon-fire audio + Cluck falling SFX + looping music; replay fix; trajectory raised/slowed; then a second round of Cluck/haybail/robot bugs — root-caused from measured pixel/physics data.**
- **Audio added:** looping background music (starts once, never restarts across levels), `CannonShot.mp3` (replaces the procedural launch clip), `Cluck_falling.mp3` (loops from Fire(), fades 0.35s on `AnimalBase.OnAnimalImpact` — real hits only, not hay pass-through punches). `AudioManager` moved to a dedicated scene GO with `[DefaultExecutionOrder(-90)]` to win the singleton race.
- **Replay/Try Again/pause-Restart fixed** — `GameManager.RestartLevel()` switched from `StartLevel()` (full scene reload) to `ForceStartLevel(CurrentLevelIndex)` (the already-proven no-reload path). Added explicit `SnapCameraToRest()` in `OnLevelStarted()`.
- **Trajectory raised + slowed considerably**, re-integrated to keep the same landing zone: `gravityScale` 0.4→0.18; `LaunchVelocity()` speed 5.5-6.5→**4.0-4.9 m/s**, angle 48°-42°→**58°-52°**. Flight time ~1.4s→~2.3-2.5s. **Current live values.**
- **Cluck_InFlight invisible — measured, not guessed.** Real content trimmed to 444×301px of a 500×500 canvas, nowhere near the spec assumption. Recalculated `BirdScale = (4.9204, 4.9204)` to hit the 0.72u collider target. **Current live value.**
- **New bug from that fix: Cluck destroyed haybails before any visible contact** — `CircleCollider2D.radius` scales with `localScale`, so the ~18x `BirdScale` bump scaled the physics hitbox too. Fixed with `ApplyBirdScale()`, re-deriving `col.radius /= BirdScale.x`.
- **Haybail "still too high" — the Round 9 fix (-4.7) was itself the bug** — it assumed nominal size equalled rendered size. `Haybail.png` at PPU=512 renders only ~0.7365 units tall at scale 0.9. Recomputed to Y=-4.87 (landing back near the original -4.876).
- **Robot position updated again to user-provided (5.7, -5.36)**, scale unchanged (5.7065, 7.009). **Current live value.**

**Sunrise Meadows world map — 2026-07-15.** New level-select screen for World 1, replacing the grid-based `LevelSelectController` as PLAY's destination (kept in place, unwired, for World 2+). Two deliberate deviations from the spec: (1) Canvas/UI markers instead of world-space SpriteRenderers, for Button support and menu consistency; (2) reused `ScoreManager.GetBestStars()` instead of a separate 1-based star scheme — trade-off: can't distinguish "never played" from "played and failed." **Superseded in most particulars by Round 11.**

**Round 11 — 2026-07-16. World map + landing page restyle to new mockup art; new full-screen MatchUpScreen; fixed a real pre-existing sprite-wiring bug; fixed corner buttons clipped outside the safe area.**
- **World map restyled to `SunriseMeadows_New.png`.** 18 `PathPositions` re-measured via automated color-blob detection against the new 1280×720 mockup, converted via its 1.5× scale into the 1920×1080 reference resolution. **Pin ordering was a best-guess left-to-right traversal, not verified.** (Superseded by Round 12's full re-trace against clean art.)
- **Position indicator now slides to the new pin before bobbing** — a single `IndicatorRoutine()` coroutine (SmoothStep ease, then resumes bob); deliberately kept to one coroutine after an earlier draft had two fighting over the same RectTransform.
- **`LevelPreviewCard` replaced by `MatchUpScreen`** — full-screen animal-vs-robot transition, cards read fresh from `GameManager.GetLevelData(levelIndex)` on every `Show()` (not a static splash). Tap-anywhere-to-continue plus a ✕ close button (mockup had no visible continue button).
- **Found and fixed a real pre-existing bug: nested runtime-built components' `[SerializeField]`s never get wired by the batch "Wire Scene References" pass.** `MatchUpScreen` is built inside `WorldMapController.BuildUI()`, which only runs on `Awake()` — an editor script re-finding an already-serialized `WorldMap` GO does not re-fire that `Awake()`, so `GetComponentInChildren<MatchUpScreen>()` in the editor always found nothing. Confirmed by grepping the saved scene for `m_Children: []` on `WorldMap`'s Transform even right after a from-scratch `AddComponent`. **General lesson: nested runtime-built components can't rely on the batch Wire pass re-running their `Awake()` — put `[SerializeField]` art fields on the top-level persisted component instead and thread them down via `Init()`.** Fixed by moving `MatchUpScreen`'s art fields onto `WorldMapController`.
- **Fixed the Main Menu PLAY button rendering as a plain orange square** — was wired to a `Play.png` that never existed; rewired to `Btn_play.png`. Added a SETTINGS button reusing the same `AudioManager` toggle state as the pause menu.
- **Fixed corner buttons rendering outside the visible safe area** — centre-anchored + large fixed offset only holds for one exact aspect ratio. Switched to corner-anchoring (anchorMin/Max pinned to the corner, small fixed inward offset), same reasoning as the pre-existing `SafeArea` wrapper.

**Round 12 — 2026-07-18/19. L01 robot free-fall + floating haybails (both root-caused); mobile aim-drag hypersensitivity fixed; MatchUp and WorldMap "baked mockup + duplicate code overlay" bugs found and fixed; new pin-free SunriseMeadows.png generated and all 18 path positions re-traced from real pixel data; robot/haybail hit-reaction art added.**
- **HarvesterRobot "starts right, then falls" — real bug.** `RobotEnemy` never set `Rigidbody2D.bodyType` — the prefab shipped `Dynamic` (Unity default), unlike every `BlockBase` type, which explicitly sets `Static`. Every robot free-fell from its spawn Y under gravity from frame one. Fixed by setting `_rb.bodyType = RigidbodyType2D.Static` in `RobotEnemy.Awake()` — the damage formula already special-cases a `Static` partner, so only the fall stopped, not hit detection. **Current live behavior.**
- **L01 haybail pile "still too high" — distinct from the Round 10 stack-gap fix.** Round 10 correctly fixed the *internal* gap between the top bale and base row. What nobody had checked: the base row's Y values were copied from the user's decorative props without validating against the ground line — the whole pile floated ~0.63 units above where it should sit. Fixed by shifting all 4 Y values down by 0.627u — preserves the base row's intentional unevenness while anchoring the lowest bale to the ground. **Current live positions are in `LevelDataGenerator.cs`/CLAUDE.md.**
- **Mobile aim-drag "feel" — root cause measured, not guessed.** The loaded bird sits only ~0.055 world units from the aim pivot, so the first sliver of pointer movement swings the drag angle across most of the 50° pull range — worse on touchscreens. Fixed with `MinAimRadius = 0.6f`: the drag angle freezes until the pointer has moved at least that far. Confirmed `Pointer.current` was already correctly covering touch — this was never an input-API gap.
- **MatchUpScreen double-frame bug** — `_matchUpBackgroundSprite` was wired to `MatchUp_Background.png`, which turned out to bake in the sky, both picture frames, the chicken/robot, and the VS graphic all in one image — not a plain backdrop as its own comment claimed. The real modular cards drawn on top produced two overlapping frames. Fixed by rewiring to `Background_SkyV1.png` (plain sky, nothing else baked in).
- **WorldMapController had the exact same "baked mockup used as live background" bug** for pins and NEXT LEVEL/Home buttons (title already fixed in Round 11). `SunriseMeadows.png` baked in 18 placeholder pins plus both buttons; the code drew separate versions on top of all three.
  - Asked the user via `AskUserQuestion` how to proceed (regenerate art / hand-edit PNG / keep guessing) — got no response in time, so only dropped NEXT LEVEL/Home to invisible click-zones over the baked art and left the pins untouched.
  - **User then generated a clean replacement `SunriseMeadows.png` via Kling AI** with nothing baked in, 1920×1080 (1:1 pixel-to-canvas mapping). This made the invisible-click-zone fix a regression — reverted NEXT LEVEL/Home back to real rendered, corner-anchored sprites.
  - **All 18 `PathPositions` re-traced from the new art's real pixel data**: masked the path color, isolated its largest connected blob (plain-Python BFS flood-fill, no scipy available), built a graph and BFS'd geodesic distance from the pond/barn end for an *ordered* centerline (needed because the path S-curves back on itself), smoothed (moving-average window 11), then walked 18 evenly arc-length-spaced points. Verified by rendering the points back onto the source image (three iterations needed before the tightest bend stopped jumbling). Level 1 = pond/barn end, level 18 = fortress end. One known rough spot: levels 7-11 crowd at the path's tightest bend (~60-90px wide there vs. each marker's 56px width) — close to unavoidable given the art.
- **Added hit-reaction art for HarvesterRobot and Haybail** — brief sprite swap (0.15s) instead of a plain tint, wired only on `HarvesterRobot.prefab`/`HaybaleBlock.prefab` (plain `Robot.prefab`/Wood/Stone keep the old tint fallback).

**Round 13 — 2026-07-20. WorldMapController.PathPositions re-traced for even marker spacing (user review request).** Round 12's geodesic-distance-bin centerline trace produced uneven spacing (consecutive-point gaps ranging ~38-90 canvas units) with a visible jumble/doubling-back right at the path's one hairpin bend (markers 7-10) — the bin-centroid approach breaks down there because pixels from the incoming and outgoing strands of the hairpin land in the same distance bin. Re-traced using `skimage.morphology.skeletonize` (proper 1px-wide medial-axis skeleton, not a distance-bin approximation) on the same masked path blob, pruned short spur branches (iteratively strip degree-1 endpoints shorter than 40px, which removes brush-stroke antialiasing noise and small texture-mark artifacts without touching the two true path ends), took the shortest path between the two surviving endpoints (the pruned skeleton has no genuine alternate route, so "shortest path" = "the one path"), smoothed (moving-average window 15), then resampled at 18 evenly arc-length-spaced points. Result: consistent ~54.4-54.6 unit gaps throughout (verified by printing consecutive-point distances), confirmed visually correct by re-rendering all 18 points plus the traced centerline back onto the source PNG and zooming into both the pond-end curve and the fortress-end straightaway — every point sits cleanly on the tan path.
- **A first attempt at this re-trace produced a badly wrong result and was caught before being applied.** Running shortest-path Dijkstra directly on a version of the skeleton built with an extra `binary_closing` preprocessing step found a path that skipped the entire upper bend near the stone ruins archway, cutting straight across through what should have been empty grass — a large, obviously-wrong shortcut, not a subtle error. Root cause never fully pinned down (suspected: closing bridged a thin gap created by faint brush-stroke wear-mark texture on the path art, creating a false cycle in the skeleton graph that let shortest-path "cut a corner"), but confirmed wrong by cropping and visually inspecting the actual path art at that location — the tan path does NOT extend up near the ruins archway at all; a similarly-colored patch of dirt/rubble ground near the archway's base matched the color mask but isn't part of the walkable route (a false-positive region, not a bug in the trace logic once identified). Dropping `binary_closing` (fill-holes only) fixed it. **Lesson: when an automated trace produces a suspiciously "efficient" shortcut, don't trust it — crop and visually verify against the actual art before applying, the same discipline as pixel-measuring sprite bounds elsewhere in this project's history.**
- Also confirmed via this investigation that the marker-locked/unlocked sprite selection (`LevelMarker.Refresh` — `!unlocked ? lockedSpr : ... : unlockedSpr`) and the position indicator's `_highestUnlocked` targeting already produce "level 1 = Unlocked art, all others = Locked art, indicator floats over level 1" by default on a fresh save (no stars recorded anywhere) — no code change was needed for that part of the request, it falls out of the existing `IsUnlocked()`/`RefreshMarkers()` logic once level 1 is the only unlocked level.

**Round 14 — 2026-07-21/22. MatchUpScreen rebuilt from a static tap-to-continue popup into a scripted, non-interactive pre-match sequence, per a direct shot-list from the user; then substantially retimed and re-laid-out after review.**
- **Initial build (2026-07-21):** replaced the old "backdrop + two static cards + tap anywhere to continue + ✕ close" screen with a scripted `PlaySequence()` coroutine: `MatchUpBackground.png` backdrop -> `LevelHeader1.png` pops in -> animal card slides in from the left / robot card from the right while `VS.png` pops in between them -> hold -> countdown 3/2/1 (pop in, hold, pop out) -> READY! -> auto-launch via `GameManager.ForceStartLevel()`, no tap required. The ✕ close button and the invisible full-screen tap-catcher were removed entirely per explicit instruction — there is now no way to back out once a marker is tapped. Audio was intentionally left out (the user will add it later); every beat has a comment marking it as a future `AudioManager.Play(...)` hook. Art was repointed from the HUD's shared `Assets/Sprites/UI/Cards/` to a new dedicated `Assets/Sprites/UI/MatchUp/` folder (`SceneSetup.WireMatchUpCards()`), which needed exact-filename matching rather than the generic keyword search `WireArrayByKeyword()` uses elsewhere — `Robot.png` (Basic) and `Harvestor_Robot.png`/`Harvestor_Robot1.png` (Harvester) all contain the substring "robot", so a naive keyword match would have hit all three off either keyword; the newer `Harvestor_Robot1.png` is preferred when both exist.
- **Scope-limited per explicit instruction ("concentrating on level 1 only")**: the level-header art is hardcoded to `LevelHeader1.png`, not swapped per level — if `Show()` is ever called for a different level index, the header still reads "LEVEL 1", a known and accepted inaccuracy rather than something to chase with placeholder/fallback text.
- **Retiming (2026-07-22), after user feedback "too fast" / "VS does not seem to appear":** no hard bug was found in the VS visibility logic itself on inspection (sprite, `enabled`, and the scale animation all checked out statically) — the actual problem was pacing: the whole sequence ran in well under 4 seconds, with the VS pop a lone 0.20s beat sandwiched between two other fast beats, easy to miss entirely. Fixed by (a) slowing every beat down significantly (header pop 0.35s->0.6s, full sequence now ~12-13s total vs. under 4s before), and (b) restructuring cards+VS from three sequential steps into one continuous `CardsAndVsClash()` beat — cards slide over 0.9s, VS's own pop starts partway through that slide timed to finish exactly as the cards land, and got a bigger overshoot (peak 1.4x vs. the header's 1.15x) so it reads as a "slam," not a fade. Added an explicit 2s hold after the clash and made each countdown numeral display for ~2s total, both per direct instruction. Caught and fixed a real bug along the way: `AnimateScale(rt, 0f, 1f, duration, bounce: true, peak)` mixed a named argument (`bounce: true`) followed by a positional one (`peak`) — invalid C# (positional args can't follow named ones in the same call); fixed by naming both.
- **Layout pass (2026-07-23), after a phone-frame screenshot showing the screen looking sparse ("make it fuller") plus the three-squares-under-the-header report:** header enlarged 380x190->560x280 and switched from a centre-anchored guess to properly top-anchored inside a new `SafeArea` wrapper (same `Screen.safeArea`-driven `ApplySafeArea()` pattern `HUDController` already uses, freshly added to this screen too) — the old countdown position, centre-anchored against the flat 1920x1080 reference canvas math, was rendering outside the actual safe region on a real phone aspect ratio; bottom-anchoring it inside the SafeArea with a small fixed inset fixes that regardless of device. Cards enlarged 480x480->560x560, brought together, and tilted ±10° outward so their inner edges exactly touch the also-enlarged VS (160x160->220x220): with VS half-width 110 and card half-width 280, card X = ∓(110+280) = ∓390. The "three squares under the header" turned out to be the replay `_starsText` label rendering `★` (U+2605), a glyph the LiberationSans SDF font asset doesn't have — removed the stars display entirely per direct instruction (`StarText()` helper deleted as now-dead code) rather than chasing a font-fallback fix.
- **Fade-to-black fix (2026-07-24), after a report that the game "navigates back to the meadows sunrise scene" before gameplay starts:** root-caused, not a scene reload — `MatchUpScreen` is a child of `WorldMapController`'s own canvas (added last purely so it renders on top), and the final step was fading the whole screen's `CanvasGroup` to *transparent* before calling `ForceStartLevel()`, which made the World Map's background/pins directly visible underneath for the entire 0.4s fade. Replaced with a dedicated opaque black `Image` overlay that fades IN (0->1 alpha) instead of the panel fading out; `ForceStartLevel()` still triggers `WorldMapController.HidePanel()`, which now atomically deactivates the whole map canvas (this screen included, since it's a child) in the same frame the black overlay is fully opaque — so the cut to gameplay never exposes stale map content, by construction rather than by timing luck.

**Round 15 — 2026-07-24. WorldMapController: NEXT LEVEL/Home buttons clipped outside the safe area (again — see Round 11), markers reported not following the path, marker/indicator sizing, and a genuinely deep re-investigation of the path trace itself.**
- **Buttons enlarged + moved into a SafeArea**, matching the exact fix already applied to `MatchUpScreen` this same session: `ApplySafeArea()` added to `WorldMapController` too, NEXT LEVEL (was 300x64, centre-relative offset) and Home (was 84x84) reparented inside it, corner-anchored with a small fixed inset. Home is now 150x150, exactly matching the landing page's `Btn_play.png`/`Btn_settings.png` scale (same 1:1-aspect asset category). NEXT LEVEL's own art (`Btn_nextlevel.png`) is a 512x128 (4:1) pill, not square, so "same size" was interpreted as matching height (150, same visual prominence) rather than forcing a distorting 150x150 box — width follows the 4:1 aspect via `preserveAspect`.
- **Marker size** (`LevelMarker.MarkerSize`) enlarged 56x84 -> 80x120 (same 2:3 aspect as the 256x384 source art). Player position indicator enlarged 56x56 -> 90x90, hover offset above the marker increased 70 -> 100 to match.
- **The "3 / 3 / 1" badges reported as a bug were not one.** `LevelMarker_Unlocked.png` and `LevelMarker_3stars.png` both have a static digit baked into their own art (a "1" and a "3" respectively) — these are NOT dynamic level-index numbers, they're fixed design elements of the unlocked-badge and 3-star-badge art. A marker showing the "3" badge means `ScoreManager.GetBestStars()` genuinely returned 3 for that level index — in the reported screenshot, leftover `PlayerPrefs` from earlier testing sessions (2 levels previously 3-starred), not a logic bug in `IsUnlocked()`/`RefreshMarkers()` (both already correct, re-verified). Added **FarmFury -> Reset World Map Progress** (clears `ff_score_N`/`ff_stars_N` for all 18 levels), standalone from Wire Scene References since it's runtime save data, not scene/asset wiring.
- **Markers "not following the entire path" — a real bug, and by far the deepest rabbit hole this session.** The PathPositions shipped in Round 13 (and every automated trace attempted before or during this round — geodesic BFS at multiple bin sizes, skeleton-shortest-path with and without extra `binary_closing`) independently agreed that the visible upper bend of the path (rising up past the windmill/ruins, peaking around source-image y≈505-530) is a *topological dead end* — every one of them routed the "shortest connected path" between the two true endpoints through only the lower dip, excluding the bend entirely. That answer is defensible by pure graph connectivity but was confirmed WRONG for this purpose by directly rendering the already-shipped Round 13 points back onto the source art: they reproduce, pixel for pixel, the same flat-row-hugging-the-bottom look a user screenshot had just reported as broken. Root cause of the topological disagreement was never fully resolved (multiple per-column/per-row pixel scans, mask-overlay visualizations, and a from-scratch skeleton-with-closing attempt all pointed at a genuinely narrow near-self-tangent waist in the art around x≈975-1110 where the bend's descending strand runs close enough to the main line that any pure shortest-distance method treats cutting across it as equivalent to or better than tracing the full loop) — resolved instead by hand-tracing 33 waypoints directly against the art (each candidate point's RGB spot-checked against the same tan-path colour test the automated attempts used, after an earlier manual attempt in this same investigation turned out to have mis-clicked coordinates that were actually green grass) and resampling to 18 evenly arc-length-spaced points (~100-107 units apart — larger than before since the true path is longer once the bend is included). Confirmed by rendering the new points back onto the source image: they now visibly follow pond -> up past the windmill/ruins bend -> back down -> tail to the fortress. **Lesson reinforced from Round 13's own postmortem, now doubly confirmed: an automated trace that looks clean and internally consistent can still be systematically wrong for a task like "match what a human viewer would call the path" — rendering the result back onto the actual art and comparing against a real screenshot is the only check that actually catches this class of error, not just checking that consecutive points are evenly spaced or that the algorithm "ran successfully."**

---

## Standing notes (not superseded by a later round)

**Raw art source (`assets/`) is currently absent from disk.** All 236 files under `assets/` were deleted from the local filesystem outside of any Claude Code session (cause unknown). They remain fully tracked in git at HEAD and are recoverable with `git checkout -- assets/`. The processed/imported sprites Unity actually uses (`unity/Assets/Sprites/`) are untouched and unaffected. The user chose to leave `assets/` deleted for now — the documented art pipeline (`tools/remove_backgrounds.py`, adding new Kling AI PNGs) will not work until it's restored.

**`unity/Assets/Sprites/` is entirely gitignored (`unity/.gitignore:37`) and is the only copy of all processed game art.** It is not tracked by git in any commit. Combined with the point above, this means processed sprite art currently exists in exactly one place: this machine's disk. If it's lost, the only recovery path is restoring `assets/` from git and re-running the full art pipeline — there is no direct git-history recovery for `Sprites/` itself. Worth a deliberate decision (git-LFS, commit it anyway, or accept the risk) rather than leaving it implicit.

**Cleanup performed 2026-07-01:** deleted `unity/Assets/LevelData.asset` + `.meta` (an orphaned, empty template LevelData asset outside `Assets/ScriptableObjects/Levels/`, unreferenced by any code). Deleted `unity/CLAUDE.md` (stale duplicate describing a pre-rebuild scene/script state). Both recoverable from git history if needed.

**No repo-root `.gitignore`.** `unity/.gitignore` correctly covers `Library/`, `Temp/`, `Logs/`, `Builds/`, etc. *inside* `unity/`. Not currently a problem, just noted as a gap if root-level tooling is added later.

**Monetisation and backend: 0% built.** Confirmed via full-tree search (still true as of 2026-07-03: `unity/Assets/Scripts/Monetisation/` exists but is empty) — no Firebase, Unity IAP, AdMob, RevenueCat, or UGS package or code references exist anywhere in the project. Expected per the roadmap (Phase 6) — see CLAUDE.md's Gap Analysis for the current read on sequencing.
